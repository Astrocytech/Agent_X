# L0 tests

This folder contains unit tests for the L0 seed kernel.

Rules:
- Tests must not mutate L0/CODE source files.
- Tests should exercise kernel contracts and governance boundaries.
- Cross-layer integration tests belong in root `tests/integration/`.
