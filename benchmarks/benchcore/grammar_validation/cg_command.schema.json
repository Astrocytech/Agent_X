{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CGCommand",
  "description": "Schema for *CG commands. Enforces *CG prefix and proper argument structure.",
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "pattern": "^\\*CG",
      "description": "The command string. Must start with '*CG'."
    },
    "arguments": {
      "type": "object",
      "patternProperties": {
        "^[a-zA-Z_][a-zA-Z0-9_]*$": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "description": "Arguments object. Each value must be a string following *CG argument syntax."
    }
  },
  "required": ["command", "arguments"],
  "additionalProperties": false
}
