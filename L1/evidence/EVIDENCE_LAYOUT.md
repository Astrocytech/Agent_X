# L1 Evidence Layout

Canonical evidence path:

`L1/evidence/<fic_id>/<utc_timestamp>_<artifact_type>.<ext>`

Bootstrap evidence may live under:

`L1/evidence/bootstrap/`

Unit-based paths are aliases only and should not be used for new evidence unless a FIC explicitly requires them.
