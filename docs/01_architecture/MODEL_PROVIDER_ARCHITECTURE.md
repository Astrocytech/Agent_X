# Model Provider Architecture

See `tools/agentx_evolve/model_adapter/` and `tools/agentx_evolve/model_runtime/` for the model provider implementation.

Architecture is documented inline in `MODEL_ADAPTER_IMPLEMENTATION_SPEC_v4.md` under `tools/docs/Model Adapter Layer/`.
