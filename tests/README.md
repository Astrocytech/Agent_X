# Root Tests

This folder is only for tests that do not belong to one single component.

Use:
- `integration/` for cross-component behavior.
- `system/` for full CLI workflows.
- `regression/` for bugs that must not return.
- `smoke/` for import/help/startup checks.

Do not place component-specific unit tests here.
