# Regression tests

Tests that prevent reintroduction of known past failures.

Rules:
- Each test should reference the issue or commit that introduced the fix.
- These tests run in CI to catch regressions early.
