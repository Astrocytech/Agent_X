# L2 tests

This folder contains unit tests for L2 profiles, blueprints, and evaluation specs.

Rules:
- Tests should validate profile contracts and cross-standard consistency.
- Cross-layer integration tests belong in root `tests/integration/`.
