## Small Improvement: Add a utility function

Add a new function `clamp_temperature(temp: float, min_val: float, max_val: float) -> float` to `examples/clothing_advice_agent/helpers.py`.

The function should clamp a temperature value between min_val and max_val using built-in max/min.

Add it after the module docstring, before RECOMMENDATION_LABELS.

Target: `examples/clothing_advice_agent/helpers.py`
