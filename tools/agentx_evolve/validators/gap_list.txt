1. Anti-false-PASS audit must validate the exact corrupted report directory.
2. Every anti-false-PASS attack must first prove that the intended corruption was actually applied.
3. Every attack must fail for the intended reason, not because files are missing from the wrong location.
4. Anti-false-PASS audit must fail if any validator rejection is caused by wrong report-dir, empty report-dir, import error, missing validator, missing working directory, or other infrastructure error.
5. Anti-false-PASS audit must run validators against isolated copied reports using the exact --report-dir path passed by the caller, without appending .agentx-init/reports a second time.
6. Anti-false-PASS audit must record the validator error text and match it against the expected corruption reason for each attack.
7. Anti-false-PASS audit must require at least one intended validator to reject each attack, and must fail if only unrelated validators reject it.
8. Anti-false-PASS audit must include a clean-control run proving that all validators pass against the uncorrupted copied reports before attacks are applied.
9. Anti-false-PASS audit must fail if any attack is skipped because the target report, row, field, or manifest did not exist.
10. Anti-false-PASS audit must store before-attack and after-attack hashes for every mutated report.
11. Anti-false-PASS audit must fail if an attack changes no bytes.
12. Anti-false-PASS audit must verify that the validator was run against the attacked copy, not the original report directory.
13. Anti-false-PASS audit must not count generic missing-file failures as successful rejection unless the attack was specifically a deletion attack.
14. Anti-false-PASS audit must include attacks against the proof bundle, acceptance matrix, traceability matrix, evidence manifest, replay report, replay manifests, command transcript, compatibility report, source mutation report, reuse map, and idempotency report.
15. Anti-false-PASS validator must verify that attacks_rejected equals attacks_tested, not only that attacks_accepted is empty.
16. Anti-false-PASS validator must fail if attacks_with_weak_rejection is greater than zero.
17. Anti-false-PASS validator must fail if any expected_rejectors entry is missing from rejected_by.
18. ReplayProof must be built from replay_execute.py output only.
19. Proof bundle must not be allowed to infer match=True.
20. Proof bundle must not copy final_verdict into original_verdict and replay_verdict without a real replay result.
21. Replay report must include original verdict, replayed verdict, match flag, replay command, replay exit code, and replay output hash for each scenario.
22. Replay validator must fail if replay_execute.py is missing, not executed, returns no output, or returns a mismatch.
23. Replay manifests must include state_records_hash and event_log_hash, and validators must fail if either is missing or mismatched.
24. Replay manifests must not use report-only data as replay proof; they must point to persisted state/event records from the actual scenario run.
25. Replay executor must reconstruct the scenario from the manifest content, not from hardcoded scenario-specific fallback parameters.
26. Replay executor must fail if required runtime context fields are missing from the manifest.
27. Replay executor must verify that state records and event logs referenced by the manifest are loaded and used during replay.
28. Replay validation must compare not only final verdicts but also required event types, state transitions, safety events, invariant results, promotion decisions, and artifact hashes.
29. Replay proof must record the replay executable hash so the proof identifies exactly which replay implementation was used.
30. Replay proof must fail if replay artifacts point outside the final report directory unless those external files are copied and hashed into the report bundle.
31. Replay proof must fail if temporary scenario directories are deleted before the referenced state, event, or artifact evidence is copied into durable reports.
32. OPEN discovered gaps must block FUNCTIONAL_RUNTIME_MVP.
33. Every discovered gap must be classified as FIXED, BENIGN, OUT_OF_SCOPE, or BLOCKED; OPEN must not pass for FUNCTIONAL_RUNTIME_MVP.
34. Every FIXED gap must include closure evidence with file paths, hashes, and validator/test proof.
35. Every OUT_OF_SCOPE gap must include a rationale explaining why it is outside FUNCTIONAL_RUNTIME_MVP scope.
36. Gap discovery validator must reject FUNCTIONAL_RUNTIME_MVP if any finding remains MVP_BLOCKER, NEEDS_REVIEW, UNKNOWN, UNCLASSIFIED, or OPEN.
37. Gap discovery must not treat the mere presence of a resolution field as sufficient proof of closure.
38. Gap discovery must include a machine-readable closure_status for every known, new, and suspected gap.
39. Gap discovery must fail if scan findings are suppressed, filtered, or excluded without an explicit rationale.
40. Gap discovery must include the exact files inspected, file count, search patterns, and hashes of inspected files.
41. Gap discovery must fail if search scope excludes code paths involved in the Functional Runtime MVP proof.
42. Gap discovery must fail if the generator discovers MVP_BLOCKER or NEEDS_REVIEW findings and the final classification still claims FUNCTIONAL_RUNTIME_MVP.
43. Final transcript must be validated after all validators are recorded.
44. Final transcript must include validate_functional_runtime_mvp_reports and validate_functional_runtime_mvp_traceability with exit_code 0.
45. Final transcript must include the final proof-bundle collection step or another verifiable record that the final proof bundle was regenerated after the last validator ran.
46. Command transcript validator must reject static, synthetic, hand-edited, missing, or non-subprocess transcript entries.
47. Command transcript validator must verify provenance_id, stdout_hash, stderr_hash, git_commit, timestamp, duration_seconds, and exit_code for every command.
48. Command transcript validator must fail if any required command is missing or has nonzero exit_code.
49. Command transcript must store hashes of full stdout and full stderr, not hashes of truncated summaries only.
50. Command transcript must either store full command logs or store separate log files with matching hashes.
51. Command transcript provenance_id must include command, cwd, environment, git_commit, timestamp, stdout_hash, stderr_hash, and exit_code, not only the command string.
52. Command transcript validator must verify that every command git_commit matches the final proof bundle commit.
53. Command transcript validator must fail if the working tree was dirty during any proof command unless the dirty files are only generated reports and are explicitly allowed.
54. Command transcript must include cwd for every command.
55. Command transcript validator must reject duplicate required command entries when an earlier failing command is hidden by a later passing duplicate.
56. Command transcript validator must require final validation commands to occur after report regeneration commands in the correct order.
57. Validators must reject forced PASS or self-referential temporary PASS rows.
58. Traceability generation must not force PASS for self-referential rows in the final matrix.
59. Any first-pass or bootstrapping PASS row must be replaced by a second-pass evidence-backed row before FUNCTIONAL_RUNTIME_MVP is claimed.
60. Acceptance matrix must be derived from the final proof bundle and final traceability matrix, not from hardcoded PASS rows.
61. Acceptance review must fail if the acceptance matrix contains PASS rows without evidence_refs.
62. Acceptance matrix must include a schema_version and validator must reject unsupported schema versions.
63. Acceptance matrix validator must reject duplicate component rows unless duplicates are explicitly allowed and reconciled.
64. Acceptance matrix validator must reject components with conflicting statuses across proof bundle, traceability matrix, and acceptance matrix.
65. Acceptance review verdict must be computed from acceptance matrix rows, not passed in as a caller-supplied argument.
66. Evidence refs must point to existing files and must include matching SHA-256 hashes.
67. Evidence refs must use stable repo-relative or report-relative paths unless an absolute path is explicitly justified and durable.
68. Evidence refs must fail if they point to files outside the report bundle that will not exist in a clean checkout.
69. Evidence manifest must include every final report that is used as proof evidence.
70. Evidence manifest validator must reject orphan evidence files that are referenced by reports but missing from the manifest.
71. Evidence manifest validator must reject manifest entries whose files exist but are not referenced by any proof object when those files affect the verdict.
72. Proof bundle report_hashes must include every final report that is used as evidence, excluding only files that are mathematically self-referential and explicitly justified.
73. Proof bundle git_commit must match the current commit, and validators must reject stale short or full commit mismatches.
74. Evidence manifest git_commit must match the proof bundle git_commit and current commit.
75. Proof bundle must include schema_version and generator metadata.
76. Proof bundle must include generator script path and generator script hash.
77. Proof bundle must fail validation if any proof object has status PASS while its evidence_refs are empty.
78. Proof bundle must fail validation if any proof object contains errors but still has PASS status.
79. Proof bundle must fail validation if any required proof object is missing, null, empty, stale, or marked UNKNOWN.
80. Proof bundle must not be rewritten after final validation unless final validation is rerun afterward.
81. Compatibility report must compute its verdict from real checks and must ignore any caller-provided forced verdict argument.
82. Compatibility validator must fail if the report verdict is PASS while any contained check is FAIL, BLOCKED, UNKNOWN, or missing.
83. Compatibility report must include the exact required checks and fail if any required check is absent.
84. Compatibility report must include hashes for the files it cites as evidence.
85. Compatibility validator must fail if required Makefile targets merely exist but are not proven to run successfully where execution proof is required.
86. Idempotency proof must compare all semantically relevant generated reports, not only a reduced transcript hash.
87. Idempotency proof must normalize only legitimate nondeterministic metadata such as timestamps and durations, not commands, statuses, hashes, or evidence paths.
88. Idempotency proof must fail if run directories are missing expected final reports.
89. Idempotency proof must fail if either run did not actually execute make prove-functional-runtime-mvp successfully.
90. Idempotency proof must include per-report stable hashes for both runs and list any differences.
91. Idempotency proof must not assign exit_code 0 by assumption; it must use recorded command results.
92. Clean-checkout reproducibility must be proven from tracked source after deleting generated reports, not from leftover .agentx-init state.
93. Clean-checkout reproducibility proof must fail if generated reports are required as input before the proof target runs.
94. Clean-checkout reproducibility proof must document the exact cleanup performed before regeneration.
95. Source mutation proof must compare real before/after source manifests around the safe scenario and fail if files changed, were added, or were removed.
96. Source mutation validator must fail if before/after manifests are missing, empty, stale, or hash-mismatched.
97. Source mutation proof must include the source scope and fail if the source scope is empty or excludes critical runtime paths.
98. Source mutation proof must fail if before and after manifests are generated back-to-back without wrapping the actual scenario execution.
99. Source mutation validator must verify that before_manifest_hash and after_manifest_hash match the manifest files.
100. Source mutation validator must verify that source mutation report git_commit matches the current commit.
101. Artifact safety proof must exercise overwrite denial behavior directly, not only cite the existence of artifact-store tests.
102. Artifact overwrite tests must prove default overwrite denial, versioned writes preserve original content, path traversal is rejected, and symlink escape is rejected.
103. Artifact safety proof must include direct command/test evidence showing the overwrite-protection tests ran and passed.
104. Artifact safety proof must include hashes of the artifact store implementation and artifact store tests.
105. Artifact store must reject unknown overwrite_policy values instead of silently writing.
106. Artifact store must fail if content_addressed mode would overwrite an existing content-addressed artifact with different content.
107. Unsafe self-promotion proof must include denial evidence from invariant engine, promotion gate, circuit breaker, event log, and persisted state records.
108. Unsafe self-promotion validator must fail if any required denial mechanism is absent, even if the final verdict says DENIED_AND_RECORDED.
109. Unsafe self-promotion proof must include the exact unsafe actor id, reviewer id, target agent id, and action id.
110. Unsafe self-promotion proof must fail if reviewer and generated agent are the same but promotion gate evidence is missing.
111. Unsafe self-promotion proof must fail if no_self_promotion invariant is absent or passed=True.
112. Unsafe self-promotion proof must fail if circuit breaker did not record unsafe_self_promotion_attempt.
113. Unsafe self-promotion proof must fail if a promoted artifact exists for the denied action.
114. Validators must fail closed: missing evidence, unreadable JSON, missing fields, unexpected schema, or unknown status must all fail.
115. All validators must support --report-dir consistently and must never silently fall back to .agentx-init/reports when an explicit report directory is provided.
116. All validators must return nonzero exit codes on validation failure and zero only on complete validation success.
117. All validators must include clear failure messages that identify the specific report, field, and reason.
118. All validators must reject unsupported report_type values.
119. All validators must reject missing schema_version once schema versions are introduced.
120. All validators must be tested with at least one valid fixture and at least one invalid fixture.
121. Validator negative tests must use real generated reports when available and must not pass using minimal stubs that hide missing final artifacts.
122. Validator tests must fail if they create placeholder reports that allow validators to pass without final proof artifacts.
123. Validator tests must verify both positive behavior and negative rejection behavior for every required validator.
124. Validator tests must include report-dir override tests to catch path-doubling and fallback bugs.
125. Report generators must use deterministic serialization with sorted keys where practical and stable path formatting.
126. No report generator may write a PASS verdict before the required evidence exists, except temporary first-pass files that are overwritten and explicitly excluded from final validation.
127. Report generators must not accept caller-supplied verdict overrides for final reports.
128. Report generators must record their own generator path, generator hash, input report hashes, and output report hashes.
129. Report generators must fail if required input reports are missing instead of creating optimistic PASS reports.
130. Report generators must not hide failures by converting missing inputs into empty lists with PASS verdicts.
131. Final FUNCTIONAL_RUNTIME_MVP classification must require all proof validators, report validators, replay validators, traceability validators, transcript validators, source-safety validators, anti-false-PASS audit, idempotency check, and test-evolve to pass.
132. Final FUNCTIONAL_RUNTIME_MVP classification must fail if any proof report claims a lower classification such as FUNCTIONAL_SCAFFOLD_WITH_MVP_VERTICAL_SLICE.
133. Final FUNCTIONAL_RUNTIME_MVP classification must fail if any final report has verdict FAIL, BLOCKED, UNKNOWN, PARTIAL, NEEDS_REVIEW, OPEN, or MVP_BLOCKER.
134. Final proof target must end with one final validator that validates the complete report set after all generation and recording steps are finished.
135. Final proof target must print and store a single machine-readable final verdict file.
136. Final verdict file must include commit, working tree status, proof target name, final validator results, and classification.
137. Final verdict file must be impossible to mark PASS unless all required validators ran successfully in the final transcript.
138. Final proof target must fail if the repository is on a detached or unexpected ref and the final verdict does not record the exact ref.
139. Final proof target must fail if untracked source files affect test or validator behavior.
140. Final proof target must fail if ignored files under source directories affect test or validator behavior.
141. Final proof target must record git status --porcelain before and after proof execution.
142. Final proof target must fail if source files changed between the first and last proof command.
143. Final proof target must fail if generated reports are committed source inputs rather than reproducible outputs unless they are explicitly marked as fixtures.
144. Final proof target must separate source fixtures from generated proof artifacts so validators cannot confuse stale fixtures with current proof output.
145. Final proof target must fail if a report references a file path that is absolute, temporary, or machine-local unless the referenced file is copied into the durable proof bundle.
146. Final proof target must validate on a path containing spaces to expose quoting and shell-command assumptions.
147. Final proof target must validate from a clean environment with no hidden dependency on local shell aliases, user config files, or existing environment variables.
148. Final proof target must document and validate all required environment variables.
149. Final proof target must fail if PYTHONPATH is missing required paths or contains unexpected paths that shadow project modules.
150. Final proof target must fail if imports resolve from outside the repository for project-local packages.
151. Final proof target must record the Python executable path and version used for every command.
152. Final proof target must record installed third-party package versions if any are used.
153. Dependency proof must fail if runtime code imports packages that are not declared in dependency metadata.
154. Dependency proof must fail if tests pass only because undeclared packages are installed globally.
155. Dependency proof must distinguish standard-library imports from project-local imports and third-party imports.
156. Dependency proof must fail if validators or proof generators use network access unless network access is explicitly in scope.
157. Offline proof must verify that all proof commands succeed with network disabled or with no network-dependent steps.
158. Security proof must fail if report generators or validators execute arbitrary shell strings derived from report content.
159. Security proof must fail if command execution uses shell=True without a fixed, audited command list.
160. Security proof must fail if a report path can escape the report directory through path traversal.
161. Security proof must fail if a symlink inside the report directory can redirect evidence validation outside the proof bundle.
162. Security proof must fail if validators follow symlinks without checking that resolved paths remain inside allowed roots.
163. Security proof must fail if report generators overwrite source files, tests, validators, or Makefile targets.
164. Security proof must fail if generated artifacts can overwrite each other silently during the same run.
165. Event log proof must include event schema version, event count, event hash, and required event types for each scenario.
166. Event log validator must fail if required event order is impossible or missing.
167. Event log validator must fail if events reference unknown run_id, goal_id, action_id, or agent_id values.
168. State proof must include state schema version, state record count, state hash, and required state transitions for each scenario.
169. State validator must fail if state records are empty, stale, out of order, or do not match the final verdict.
170. Transaction proof must show commit for safe scenario and no unsafe promotion commit for unsafe scenario.
171. Transaction validator must fail if rollback-required actions do not produce rollback evidence.
172. Rollback proof must show retained failed artifacts and rollback events when execution fails or is denied.
173. Review proof must show that human-review approval is separate from generated-agent self-approval.
174. Review validator must fail if reviewer identity is missing, equal to the generated agent for unsafe approval, or not recorded in the event/state proof.
175. Policy proof must show which policy rule allowed or denied the action.
176. Policy validator must fail if a PASS verdict has no matching ALLOW policy evidence.
177. Capability proof must show that the acting agent has the required capability for the target.
178. Capability validator must fail if execution succeeds without matching capability evidence.
179. Contract proof must show that the action type has a registered contract and that runtime inputs satisfy it.
180. Contract validator must fail if contract validation is skipped, missing, or only documented without execution evidence.
181. Invariant proof must include every invariant evaluated, its input context hash, and pass/fail result.
182. Invariant validator must fail if no_self_promotion is not evaluated in the unsafe scenario.
183. Circuit breaker proof must include trip reason, trip timestamp, run_id, and reset status.
184. Circuit breaker validator must fail if an unsafe scenario is denied without a corresponding safety event.
185. Report schema validation must use explicit JSON schemas or equivalent strict field checks for every machine-readable report.
186. Report schema validation must reject additional unknown fields when they could affect verdict semantics.
187. Report schema validation must reject missing required fields even if the report verdict says PASS.
188. Report schema validation must reject invalid enum values for status, verdict, classification, and resolution.
189. Report schema validation must reject inconsistent casing such as Pass, pass, or passed where PASS is required.
190. Report schema validation must reject null values for required proof fields.
191. Report schema validation must reject empty strings for required IDs, paths, hashes, commands, and verdicts.
192. Report schema validation must reject duplicate requirement IDs in traceability.
193. Report schema validation must reject duplicate proof object IDs where IDs are expected to be unique.
194. Report schema validation must reject duplicate attack IDs in anti-false-PASS audit.
195. Report schema validation must reject mismatched counts such as attacks_tested not equaling the length of attack_results.
196. Report schema validation must reject total_requirements not equaling the number of traceability rows.
197. Report schema validation must reject file_count not equaling the number of files listed in a source manifest.
198. Traceability validator must fail if implementation_refs, test_refs, validator_refs, or evidence_refs are strings in one report and objects in another without a documented schema.
199. Traceability validator must verify that every implementation_ref exists and is within the repository.
200. Traceability validator must verify that every test_ref exists and was executed with exit_code 0.
201. Traceability validator must verify that every validator_ref exists and was executed with exit_code 0 after the relevant report was generated.
202. Traceability validator must fail if a requirement maps only to a broad directory when a specific file is required for proof.
203. Traceability validator must fail if process-level requirements bypass behavioral proof without a separate validator proving them.
204. Traceability validator must fail if OUT_OF_SCOPE rows are used for requirements that are mandatory for FUNCTIONAL_RUNTIME_MVP.
205. Acceptance validator must verify that every required component appears exactly once.
206. Acceptance validator must verify that every required component maps to a traceability requirement.
207. Acceptance validator must fail if acceptance rows are generated before the final traceability matrix and not regenerated afterward.
208. Acceptance validator must fail if any accepted non-PASS status is not explicitly listed and justified.
209. Proof bundle validator must verify that acceptance_rows inside the proof bundle match the final acceptance matrix exactly.
210. Proof bundle validator must verify that requirement_trace_proof hash matches the final traceability matrix.
211. Proof bundle validator must verify that anti_false_pass_proof hash matches the final anti-false-PASS audit report.
212. Proof bundle validator must verify that source_mutation_proof hash matches the final source mutation report.
213. Proof bundle validator must verify that replay_proofs correspond one-to-one with replay manifest files.
214. Proof bundle validator must fail if scenario_proofs are derived from replay report rows instead of actual scenario command results.
215. Scenario proof must include command result, pytest node/test path, exit_code, scenario export marker hash, and final verdict.
216. Scenario proof must fail if scenario export markers are missing or were produced by a different run.
217. Scenario export must copy evidence before temporary scenario cleanup and must fail if copied evidence is missing after cleanup.
218. Scenario export must include hashes for promotion decisions, invariant results, safety events, state records, event log, and artifacts.
219. Scenario export must fail if artifact refs include files that no longer exist at final validation time.
220. Test proof must fail if pytest collected zero tests for any required test command.
221. Test proof must fail if tests are skipped, xfailed, or deselected in a way that removes required proof coverage.
222. Test proof must fail if pytest markers exclude required system tests from the proof run.
223. Test proof must record collected test count and passed/failed/skipped counts for every pytest command.
224. Test proof must reject a pytest command as proof if it exits 0 but collected zero tests.
225. Test proof must reject a test command as proof if stdout/stderr summaries are too truncated to verify collection and results and no full log is stored.
226. Validator proof must record collected validator count and require every required validator to run in the final proof path.
227. Validator proof must include positive-control validation of clean reports and negative-control validation of corrupted reports.
228. Validator proof must fail if a validator imports reports at module import time before --report-dir is parsed correctly.
229. Validator proof must fail if a validator uses global REPORT_DIR that cannot be overridden in tests.
230. Validator proof must fail if validators behave differently when imported versus executed as scripts.
231. Makefile proof target must fail immediately on any command failure and must not continue after a failed validator.
232. Makefile proof target must use set -e or equivalent fail-fast behavior where multi-command shell blocks are used.
233. Makefile proof target must avoid hiding failures behind pipes, subshells, or commands prefixed with '-' unless explicitly justified.
234. Makefile proof target must fail if REC records a command failure but the surrounding make target continues.
235. Makefile proof target must ensure the command recorder returns the wrapped command exit code.
236. Makefile proof target must record commands in the same order they are executed.
237. Makefile proof target must not regenerate reports after final validation unless it runs final validation again.
238. Makefile proof target must not overwrite the transcript after validating the transcript unless it validates the rewritten transcript.
239. Makefile proof target must include a final all-in-one validator that checks cross-report consistency after all files are finalized.
240. Final all-in-one validator must verify cross-file consistency among proof bundle, evidence manifest, acceptance matrix, traceability matrix, transcript, replay reports, compatibility report, gap report, source safety report, anti-false-PASS report, and idempotency report.
241. Final all-in-one validator must fail if any report has a later timestamp than the final proof bundle unless the proof bundle is regenerated and revalidated afterward.
242. Final all-in-one validator must fail if report timestamps imply validation occurred before the report being validated was generated.
243. Final all-in-one validator must fail if any report references an old hash of a report that was subsequently rewritten.
244. Final all-in-one validator must fail if generated markdown and generated JSON disagree on verdict, counts, hashes, or classification.
245. Markdown reports must be treated as views of JSON reports and regenerated from JSON after final JSON validation.
246. Markdown reports must not be used as source of truth for validator decisions.
247. Final verdict must be generated only after the final all-in-one validator passes.
248. Final proof target must be safe to run twice in the same working tree without relying on manual cleanup.
249. Final proof target must fail if stale reports from a previous run are reused instead of being regenerated.
250. Final proof target must delete or quarantine previous proof outputs before starting a new proof run.
251. Final proof target must tag every generated report with a unique proof_run_id.
252. Final proof target must fail if reports from different proof_run_id values are mixed in the final proof set.
253. Final proof target must fail if scenario evidence from one proof run is referenced by another proof run.
254. Final proof target must include start_time, end_time, proof_run_id, git_commit, and report_dir in every machine-readable report.
255. Final proof target must fail if any report has a proof_run_id missing, empty, duplicated incorrectly, or inconsistent with the final verdict file.
256. Final proof target must write reports atomically using temporary files and rename, not partial in-place writes.
257. Final proof target must fail if any final report file is zero bytes.
258. Final proof target must fail if any final report is not valid UTF-8 where text encoding is expected.
259. Final proof target must fail if JSON reports contain NaN, Infinity, comments, trailing commas, or non-standard JSON.
260. Final proof target must fail if JSON report hashes change after parsing and canonical re-serialization when canonical JSON is required.
261. Final proof target must specify whether hashes are computed over raw bytes or canonical JSON and must use the same method consistently.
262. Hash validation must reject hashes with wrong algorithm, wrong length, uppercase/lowercase mismatch when strict lowercase is required, or non-hex characters.
263. Hash validation must fail if the hash field is present but empty, null, placeholder, all zeros, or marked TODO.
264. Hash validation must fail if two different files are assigned the same evidence id without explicit content-addressed justification.
265. Hash validation must fail if one evidence id points to different hashes across reports.
266. Hash validation must fail if one file path has different hashes across reports within the same proof_run_id.
267. Report path validation must reject backslashes, mixed separators, drive letters, home-directory expansion, environment-variable expansion, and shell globbing in proof paths unless explicitly normalized.
268. Report path validation must normalize and compare resolved paths to allowed roots before opening files.
269. Report path validation must reject case-variant duplicate paths on case-insensitive filesystems.
270. Report path validation must reject Unicode-normalization duplicate paths where the same logical file can appear under different byte spellings.
271. Report path validation must reject hidden control characters, newlines, tabs, and terminal escape sequences in paths, IDs, verdicts, and commands.
272. Report path validation must reject evidence paths that depend on the current user's home directory.
273. Report path validation must reject evidence paths that depend on /tmp unless the evidence is copied into the durable proof bundle.
274. Report path validation must reject evidence paths that point into pytest cache, __pycache__, .git internals, or editor swap files.
275. Report path validation must reject evidence paths that are directories when a file is required.
276. Report path validation must reject evidence paths that are special files, sockets, FIFOs, or device nodes.
277. Report path validation must reject symlink loops.
278. Report path validation must reject hardlink-based evidence aliasing when it hides source mutation or overwrite behavior.
279. Report path validation must fail if two report entries reference the same physical inode under different paths without justification.
280. Atomic write proof must fail if final reports can be observed in partially written states during proof generation.
281. Atomic write proof must include failure handling for interrupted report generation.
282. Atomic write proof must fail if interrupted generation can leave stale PASS reports in place.
283. Atomic write proof must fail if a failed proof run leaves a final verdict file that still says PASS.
284. Failure-mode proof must run at least one intentionally failing proof substep and verify that the top-level proof target fails closed.
285. Failure-mode proof must verify that failed validators do not still produce PASS final verdicts.
286. Failure-mode proof must verify that report generation stops or marks BLOCKED when required inputs are missing.
287. Failure-mode proof must verify that missing scenario evidence cannot be replaced by empty placeholder files.
288. Failure-mode proof must verify that corrupted JSON cannot be silently ignored and replaced with default PASS data.
289. Failure-mode proof must verify that malformed command transcript entries cannot be skipped.
290. Failure-mode proof must verify that invalid enum values cannot be coerced to PASS.
291. Failure-mode proof must verify that missing hashes cannot be treated as warnings only.
292. Concurrency proof must fail if two proof runs executing concurrently can mix reports or overwrite each other's artifacts.
293. Concurrency proof must isolate report directories by proof_run_id or use locking that prevents concurrent writes to the same final report directory.
294. Concurrency proof must fail if lock acquisition failure is ignored.
295. Concurrency proof must fail if partial files from a concurrent run are accepted as evidence.
296. Concurrency proof must fail if artifact IDs are generated from timestamps alone and can collide.
297. Determinism proof must control random seeds or prove that random choices do not affect final verdicts.
298. Determinism proof must fail if generated IDs, ordering, or hashes vary across runs without being classified as legitimate nondeterministic metadata.
299. Determinism proof must sort unordered collections before hashing and reporting.
300. Determinism proof must fail if dictionary iteration order, filesystem listing order, or pytest collection order changes the final proof verdict.
301. Determinism proof must record locale, timezone, and clock source when timestamps are used.
302. Determinism proof must fail if timezone differences alter report hashes outside explicitly normalized timestamp fields.
303. Determinism proof must fail if locale differences alter sorting, formatting, casing, or decimal representation in proof reports.
304. Determinism proof must fail if wall-clock time is used as proof evidence rather than metadata.
305. Determinism proof must fail if future timestamps appear in generated reports.
306. Determinism proof must fail if report timestamps are earlier than the proof run start_time or later than end_time.
307. Determinism proof must fail if duration_seconds is negative, null, nonnumeric, or impossible.
308. Exit-code proof must verify that wrappers propagate exact subprocess exit codes.
309. Exit-code proof must fail if a command exits nonzero but the recorder stores zero.
310. Exit-code proof must fail if a command is killed by signal and recorded as ordinary success.
311. Exit-code proof must record timeout, signal, and exception status separately from numeric exit_code.
312. Timeout proof must fail if commands can hang indefinitely without a recorded failure.
313. Timeout proof must record configured timeouts for long-running proof commands.
314. Timeout proof must fail if a timeout is treated as PASS because no failing output was produced.
315. Timeout proof must fail if a timed-out command leaves partial PASS reports that are later accepted.
316. Logging proof must ensure stdout and stderr are captured before process termination.
317. Logging proof must fail if large stdout or stderr is truncated without storing a full hashed log.
318. Logging proof must fail if binary output corrupts the command transcript.
319. Logging proof must escape terminal control characters in human-readable reports.
320. Logging proof must fail if secret-looking environment values are copied into public proof reports without redaction.
321. Redaction proof must not redact fields needed for reproducibility, such as command names, paths, hashes, exit codes, and validator names.
322. Environment proof must record environment variables that materially affect Python, pytest, Make, locale, timezone, and import resolution.
323. Environment proof must fail if proof commands rely on environment variables that are not declared in the final proof metadata.
324. Environment proof must fail if environment variables can force validators to skip checks or force PASS in final proof mode.
325. Environment proof must reject debug flags, bypass flags, or compatibility flags that weaken final validation unless explicitly recorded and prohibited for FUNCTIONAL_RUNTIME_MVP.
326. Environment proof must fail if AGENTX_MVP_NO_FORCED_PASS is absent or false during final traceability generation.
327. Environment proof must fail if any final report was generated with a bypass, fixture, demo, mock-only, or forced-pass environment flag enabled.
328. Mock-boundary proof must clearly distinguish allowed test doubles from forbidden fake proof evidence.
329. Mock-boundary proof must fail if runtime MVP claims are proven only by mocks where real runtime behavior is required.
330. Mock-boundary proof must fail if a mock policy engine, mock promotion gate, or mock artifact store is used without being declared as in-scope implementation.
331. Mock-boundary proof must fail if fixture reports are accepted as generated reports.
332. Mock-boundary proof must fail if tests monkeypatch validators to pass.
333. Mock-boundary proof must fail if tests monkeypatch replay_execute.py, proof collectors, report generators, or command recorders in the final proof path.
334. Import-integrity proof must fail if a test-local module shadows a production module in the final proof path.
335. Import-integrity proof must record resolved module file paths for critical modules.
336. Import-integrity proof must fail if critical modules resolve from tests, temporary directories, or installed packages instead of the repository source.
337. Import-integrity proof must fail if duplicate module names exist in source and tests and the wrong one is imported.
338. Import-integrity proof must fail if stale .pyc files are used after source changes.
339. Build-cleanliness proof must remove __pycache__, .pytest_cache, generated reports, and temporary proof directories before clean-run validation.
340. Build-cleanliness proof must fail if clean-run validation depends on caches created by a previous run.
341. Build-cleanliness proof must fail if pytest cache changes test selection or outcomes in the proof run.
342. Build-cleanliness proof must fail if generated code is imported as source without being tracked or regenerated deterministically.
343. Requirement identity proof must ensure requirement IDs are immutable and not silently renumbered between reports.
344. Requirement identity proof must fail if the same requirement text appears under multiple IDs without explicit de-duplication.
345. Requirement identity proof must fail if a requirement ID changes status between reports without an explicit evidence-backed transition.
346. Requirement identity proof must fail if a requirement is removed from the final traceability matrix without being recorded as removed with rationale.
347. Requirement identity proof must fail if total PASS count is computed before duplicate or removed requirements are reconciled.
348. Coverage proof must map every required validator to at least one requirement and every requirement to at least one validator unless explicitly exempted.
349. Coverage proof must fail if any mandatory runtime path is not covered by tests, validators, or scenario evidence.
350. Coverage proof must fail if code paths for denial, rollback, replay mismatch, artifact overwrite denial, and validator failure are not exercised.
351. Coverage proof must distinguish line coverage from behavioral proof and must not use line coverage alone as MVP evidence.
352. Regression proof must include a minimal regression test for each loophole fixed by the corrective list.
353. Regression proof must fail if a corrected loophole can be reintroduced without a test failing.
354. Regression proof must include path-doubling regression coverage for --report-dir.
355. Regression proof must include manufactured replay-success regression coverage.
356. Regression proof must include OPEN-gap acceptance regression coverage.
357. Regression proof must include final-transcript-after-last-validator regression coverage.
358. Regression proof must include forced-PASS/self-referential-row regression coverage.
359. Regression proof must include stale-proof-bundle regression coverage.
360. Regression proof must include missing-evidence PASS-row regression coverage.
361. Regression proof must include report-dir fallback regression coverage.
362. Regression proof must include zero-test pytest command regression coverage.
363. Regression proof must include stale previous-run report reuse regression coverage.
364. Regression proof must include mixed proof_run_id regression coverage.
365. Proof dependency graph must list which reports depend on which input reports, commands, tests, validators, source files, and evidence artifacts.
366. Proof dependency graph validator must fail if a final PASS verdict depends on a report generated after the verdict was computed.
367. Proof dependency graph validator must fail if any dependency cycle is not explicitly declared as self-referential and handled by a second-pass regeneration.
368. Proof dependency graph validator must fail if any required report is both an input and output of the same proof step without an atomic two-phase update.
369. Proof dependency graph validator must verify that final proof bundle hashes are computed after every dependent report is finalized.
370. Proof dependency graph validator must fail if final verdict references any report whose hash is absent from the dependency graph.
371. Proof dependency graph validator must fail if report generation order differs from declared dependency order.
372. Proof dependency graph validator must fail if a downstream report is not regenerated after an upstream evidence report changes.
373. Proof dependency graph must include markdown reports as derived outputs and must prove they were regenerated after their JSON source reports.
374. Evidence cycle detection must reject proof objects that cite themselves as their only evidence.
375. Evidence cycle detection must reject proof objects whose evidence chain eventually loops back without external runtime, test, or validator proof.
376. Evidence cycle detection must require at least one non-report runtime artifact, test execution, command transcript entry, or validator result for every PASS proof object.
377. Evidence cycle detection must fail if two PASS reports only cite each other and no independent evidence.
378. Evidence cycle detection must fail if a report cites the final verdict file as evidence for itself.
379. Evidence cycle detection must fail if final verdict is used as evidence for any upstream proof row.
380. Requirement source-of-truth proof must identify the canonical Functional Runtime MVP requirement list and its hash.
381. Requirement source-of-truth proof must fail if requirements are inferred only from generated reports.
382. Requirement source-of-truth proof must fail if requirement text differs across source spec, traceability matrix, acceptance matrix, and proof bundle without a recorded migration.
383. Requirement source-of-truth proof must fail if new requirements are silently added after validation without re-running traceability.
384. Requirement source-of-truth proof must fail if mandatory requirements are hidden by changing their category to process, optional, deferred, or out-of-scope.
385. Requirement source-of-truth proof must fail if requirement IDs are generated nondeterministically.
386. Requirement source-of-truth proof must fail if the canonical requirement file is untracked, missing, stale, or not hashed in the proof bundle.
387. Requirement source-of-truth proof must record the exact version of the corrective gap list used as acceptance input.
388. Corrective-list coverage proof must map every corrective numbered item to implementation_refs, test_refs, validator_refs, and evidence_refs.
389. Corrective-list coverage proof must fail if any corrective item is unimplemented, untested, unvalidated, or not evidenced.
390. Corrective-list coverage proof must fail if any corrective item is merged into a broad row that hides missing evidence.
391. Corrective-list coverage proof must fail if any corrective item is marked duplicate without naming the duplicate item and showing equivalent evidence.
392. Corrective-list coverage proof must fail if any corrective item is marked not-applicable without a rationale and validator enforcing that rationale.
393. Corrective-list coverage proof must include an item_count and fail if it differs from the actual numbered list count.
394. Corrective-list coverage proof must fail if numbering gaps, duplicate numbers, or reordered items break traceability.
395. Corrective-list coverage proof must preserve stable corrective item IDs even if the human-readable order changes.
396. Corrective-list coverage proof must fail if final reports are generated from an older corrective list than the one used by the final verdict.
397. Cross-report consistency validator must compare all shared identifiers across reports, including proof_run_id, git_commit, scenario_id, action_id, agent_id, artifact_id, evidence_id, requirement_id, attack_id, and validator_id.
398. Cross-report consistency validator must fail if the same scenario_id has different final verdicts across scenario report, replay report, proof bundle, and acceptance matrix.
399. Cross-report consistency validator must fail if the same artifact_id has different hashes, paths, overwrite policies, or promotion states across reports.
400. Cross-report consistency validator must fail if the same validator_id has different command paths, versions, or hashes across transcript and proof reports.
401. Cross-report consistency validator must fail if the same attack_id has different expected rejectors or outcomes across anti-false-PASS JSON and markdown.
402. Cross-report consistency validator must fail if scenario counts differ across replay manifests, replay report, scenario proofs, and acceptance review.
403. Cross-report consistency validator must fail if requirement pass counts differ across traceability, acceptance matrix, proof bundle, and final verdict.
404. Cross-report consistency validator must fail if evidence manifest lists a file hash that differs from the proof bundle report_hashes entry.
405. Cross-report consistency validator must fail if command transcript references a validator result that is absent from validator proof.
406. Cross-report consistency validator must fail if a final report has a proof_run_id that does not match the report directory proof_run_id.
407. Schema migration proof must define how reports from older schema versions are rejected or migrated.
408. Schema migration proof must fail if validators accept older report schemas silently.
409. Schema migration proof must fail if report generators emit different schema versions for reports in the same final proof set without compatibility rules.
410. Schema migration proof must fail if schema_version is changed without updating validator tests.
411. Schema migration proof must fail if unknown schema versions default to latest behavior.
412. Schema migration proof must include negative tests for unsupported schema_version values.
413. Version provenance proof must record source file hashes for every validator and report generator used in the final proof run.
414. Version provenance proof must fail if a validator file changed after it produced a PASS result.
415. Version provenance proof must fail if a report generator file changed after generating the report it claims.
416. Version provenance proof must fail if tests changed after their passing transcript entries were recorded.
417. Version provenance proof must fail if Makefile or proof orchestration scripts changed after command transcript creation.
418. Version provenance proof must fail if final proof uses mixed validator versions from different commits.
419. Version provenance proof must fail if Python imports cached old code after the source hash changed.
420. Version provenance proof must include hashes for Makefile, proof scripts, validator scripts, replay executor, scenario tests, artifact store, policy registry, promotion gate, invariant engine, circuit breaker, and state/event recorder.
421. Threat-model proof must explicitly list what false-PASS classes are in scope for this MVP.
422. Threat-model proof must fail if anti-false-PASS attacks do not cover every in-scope false-PASS class.
423. Threat-model proof must fail if newly discovered false-PASS classes are not added as attacks or explicitly deferred with rationale.
424. Threat-model proof must distinguish accidental stale proof, intentional report tampering, validator bug, generator bug, transcript forgery, evidence deletion, replay fabrication, and path confusion.
425. Threat-model proof must fail if any in-scope threat class has no validator or negative test.
426. Threat-model proof must fail if false-PASS audit only mutates JSON reports and never mutates underlying evidence files.
427. Threat-model proof must include at least one attack where evidence file content changes but report metadata remains unchanged.
428. Threat-model proof must include at least one attack where report metadata changes but evidence file content remains unchanged.
429. Threat-model proof must include at least one attack where command transcript is internally consistent but inconsistent with final reports.
430. Threat-model proof must include at least one attack where final reports are internally consistent but stale relative to source commit.
431. Threat-model proof must include at least one attack where final reports are internally consistent but mixed across proof_run_id values.
432. Tamper-evidence proof must fail if a report can be changed after validation without invalidating final verdict hashes.
433. Tamper-evidence proof must fail if evidence files can be changed after proof bundle generation without invalidating proof bundle validation.
434. Tamper-evidence proof must fail if markdown can be changed to claim PASS while JSON remains FAIL without final consistency validation catching it.
435. Tamper-evidence proof must fail if JSON can be changed to claim PASS while markdown remains stale without final consistency validation catching it.
436. Tamper-evidence proof must fail if command transcript can be edited after transcript validation without final all-in-one validation catching it.
437. Tamper-evidence proof must fail if final verdict file can be edited after generation without a detached verification command failing.
438. Detached verification command must validate an existing proof bundle from disk without regenerating reports.
439. Detached verification command must fail if any proof file is changed after generation.
440. Detached verification command must fail if any referenced evidence file is removed after generation.
441. Detached verification command must fail if the repository commit differs from the commit recorded in the proof bundle unless source files are not used for detached verification and this is explicitly declared.
442. Detached verification command must not call report generators, scenario tests, or proof collectors.
443. Detached verification command must be included in the final transcript after proof generation.
444. Detached verification command must be included in anti-false-PASS negative tests.
445. Regeneration command must regenerate reports from scratch and must not rely on detached verification outputs.
446. Regeneration command must fail if detached verification artifacts are mistakenly used as fresh proof inputs.
447. Report signing is not required, but report hash chaining must provide equivalent local tamper detection for this MVP.
448. Hash chaining proof must include previous-step hash, input hash set, output hash set, and command transcript entry for every proof stage.
449. Hash chaining proof must fail if any stage omits inputs that materially affect its outputs.
450. Hash chaining proof must fail if any stage reports output hashes before files are fully written and closed.
451. Hash chaining proof must fail if any stage reads from directories outside its declared input roots.
452. Hash chaining proof must fail if any stage writes outside its declared output roots.
453. Hash chaining proof must fail if any final output is not reachable from the initial source hash and executed command transcript chain.
454. Functional behavior proof must distinguish unit-test proof, integration-test proof, scenario proof, and report-validation proof.
455. Functional behavior proof must fail if a functional requirement is proven only by a validator that checks a generated report instead of runtime behavior.
456. Functional behavior proof must fail if a runtime behavior is proven only by code inspection without executing the behavior, unless explicitly classified as static-only.
457. Functional behavior proof must fail if scenario tests assert only final status and not the required intermediate mechanisms.
458. Functional behavior proof must fail if unsafe denial is proven only by absence of promotion without a recorded denial event.
459. Functional behavior proof must fail if safe promotion is proven only by presence of artifact without recorded approval and promotion decision.
460. Functional behavior proof must fail if artifact preservation is proven only by metadata without reading artifact bytes.
461. Functional behavior proof must fail if policy enforcement is proven only by policy file presence without an executed policy decision.
462. Functional behavior proof must fail if circuit breaker behavior is proven only by class existence without an executed trip event.
463. Functional behavior proof must fail if invariant behavior is proven only by function existence without captured invariant evaluation.
464. Functional behavior proof must fail if state persistence is proven only by in-memory objects and no durable state record.
465. Functional behavior proof must fail if event logging is proven only by logger calls and no durable event log.
466. Functional behavior proof must fail if replay is proven only by matching stored verdicts and no reconstructed execution.
467. Functional behavior proof must fail if idempotency is proven only by one successful run.
468. Functional behavior proof must fail if compatibility is proven only by Makefile target presence.
469. Functional behavior proof must fail if source safety is proven only by git status after the run and not by before/after source manifests.
470. Completeness proof must fail if any required runtime subsystem is represented only by placeholder class, TODO, NotImplementedError, pass statement, or stub return.
471. Completeness proof must fail if any TODO or FIXME lies on a path required for FUNCTIONAL_RUNTIME_MVP and is not classified with rationale.
472. Completeness proof must fail if a function required for MVP returns a constant PASS, ALLOW, DENY, or match=True without input-dependent logic.
473. Completeness proof must fail if a validator checks only top-level verdict and not supporting evidence.
474. Completeness proof must fail if a report generator fills missing data with default PASS values.
475. Completeness proof must fail if an exception handler converts validation errors into warnings during final proof mode.
476. Completeness proof must fail if broad try/except blocks hide failed evidence collection.
477. Completeness proof must fail if missing optional fields become required evidence in downstream reports without validation.
478. Completeness proof must fail if CLI commands have help text but no runtime implementation.
479. Completeness proof must fail if tests only verify that files exist and not that their contents are semantically valid.
480. Root-of-trust proof must define which files are trusted inputs, which files are generated outputs, and which files are external evidence.
481. Root-of-trust proof must fail if generated outputs are treated as trusted inputs for their own validation.
482. Root-of-trust proof must fail if a proof report is accepted without a chain back to tracked source files and executed commands.
483. Root-of-trust proof must fail if the canonical proof command is not documented in one place.
484. Root-of-trust proof must fail if multiple proof commands can produce conflicting final verdicts without a declared precedence rule.
485. Root-of-trust proof must fail if a human-readable document is treated as authoritative over machine-readable JSON for final validation.
486. Root-of-trust proof must fail if a generated file can modify the requirement source of truth.
487. Root-of-trust proof must fail if validators trust fields that are produced by the same report being validated without independent recomputation.
488. Root-of-trust proof must fail if validation depends on report comments, prose, badges, headings, or filenames instead of structured fields and evidence hashes.
489. Root-of-trust proof must fail if the final PASS can be produced by editing only reports without re-running runtime scenarios.
490. Proof-mode guard must disable demo mode, fixture mode, tutorial mode, dry-run success mode, and compatibility bypass mode.
491. Proof-mode guard must fail if any code path detects proof mode and weakens validation instead of strengthening it.
492. Proof-mode guard must fail if an environment variable can suppress validators, skip attacks, skip replay, skip tests, or skip evidence hashing.
493. Proof-mode guard must fail if a command-line flag can mark reports PASS without executing the required behavior.
494. Proof-mode guard must fail if report generators have --force-pass, --accept-missing, --skip-validation, --best-effort, or equivalent flags enabled in final proof mode.
495. Proof-mode guard must fail if validators have --warn-only, --allow-missing, --ignore-errors, --best-effort, or equivalent flags enabled in final proof mode.
496. Proof-mode guard must fail if expected-failure tests are allowed to count as proof of successful runtime behavior.
497. Proof-mode guard must fail if skipped tests are counted as passed tests.
498. Proof-mode guard must fail if xfail tests are counted as proof unless the requirement explicitly concerns expected failure behavior.
499. Proof-mode guard must fail if test deselection removes any required test from the proof run.
500. Proof-mode guard must fail if pytest -k, -m, or ignore options exclude required proof tests in the final transcript.
501. CLI contract proof must verify that every proof-related CLI returns stable JSON or stable exit-code semantics.
502. CLI contract proof must fail if a CLI prints PASS while returning nonzero exit code.
503. CLI contract proof must fail if a CLI returns zero exit code while printing validation errors.
504. CLI contract proof must fail if a CLI writes reports to an undocumented default directory.
505. CLI contract proof must fail if a CLI ignores explicit output-dir, report-dir, proof-run-id, or root arguments.
506. CLI contract proof must fail if a CLI accepts relative report-dir differently depending on cwd without recording the resolved path.
507. CLI contract proof must fail if a CLI silently creates missing input directories during validation.
508. CLI contract proof must fail if a validator CLI mutates the report files it validates.
509. CLI contract proof must fail if a verification command produces new proof evidence instead of only validating existing evidence.
510. CLI contract proof must fail if command help text advertises options that are not implemented.
511. CLI contract proof must fail if command arguments are parsed differently in Makefile use than in direct CLI use.
512. Make-variable proof must record all Make variables that affect the final proof run.
513. Make-variable proof must fail if overriding a Make variable can skip required validation while still producing PASS.
514. Make-variable proof must fail if default Make variables point outside the repository or report directory.
515. Make-variable proof must fail if Make recipes depend on the user's current shell instead of explicitly declared shell behavior.
516. Make-variable proof must fail if Make recipes use unquoted paths that break when repository path contains spaces.
517. Make-variable proof must fail if Make recipes swallow errors with `|| true` in final proof paths.
518. Make-variable proof must fail if Make recipes use `continue-on-error` style behavior for validators.
519. Make-variable proof must fail if Make recipes use wildcards that can accidentally include stale reports from previous runs.
520. Make-variable proof must fail if Make recipes copy reports without verifying copy success and copied hashes.
521. Make-variable proof must fail if Make recipes delete evidence before final validation can read and hash it.
522. Input validation proof must fail if scenario inputs can omit required fields and still receive PASS.
523. Input validation proof must fail if scenario inputs accept unknown fields that change behavior without being recorded.
524. Input validation proof must fail if scenario inputs accept invalid actor IDs, reviewer IDs, artifact IDs, action IDs, or capability IDs.
525. Input validation proof must fail if scenario inputs accept empty strings, whitespace-only IDs, duplicate IDs, or IDs with path separators.
526. Input validation proof must fail if scenario inputs accept null where a concrete value is required.
527. Input validation proof must fail if scenario inputs accept negative durations, invalid timestamps, or impossible state transitions.
528. Input validation proof must fail if scenario inputs accept policy names, invariant names, or capability names that are not registered.
529. Input validation proof must fail if unsafe scenario input can be changed to safe semantics while retaining the unsafe scenario label.
530. Input validation proof must fail if safe scenario input can skip required approval or promotion steps while retaining the safe scenario label.
531. Input validation proof must include negative tests for malformed scenario manifests.
532. State-machine proof must define allowed state transitions for generated agents, review requests, promotion decisions, artifacts, and runs.
533. State-machine proof must fail if a final state can be reached without all required prior states.
534. State-machine proof must fail if denied actions can transition into promoted state.
535. State-machine proof must fail if safe actions can be marked promoted before approval state is recorded.
536. State-machine proof must fail if rollback state can be recorded without naming the failed or denied action.
537. State-machine proof must fail if state records have duplicate sequence numbers or non-monotonic ordering.
538. State-machine proof must fail if event sequence and state sequence disagree.
539. State-machine proof must fail if final verdict contradicts terminal state.
540. State-machine proof must fail if an action has multiple terminal states without a recorded supersession rule.
541. State-machine proof must fail if promotion gate result is missing from the state-machine transition that claims promotion.
542. Event-log integrity proof must use append-only or hash-chained event records for scenario evidence.
543. Event-log integrity proof must fail if event records can be reordered without detection.
544. Event-log integrity proof must fail if event records can be removed without changing the event log hash.
545. Event-log integrity proof must fail if event records can be duplicated without detection.
546. Event-log integrity proof must fail if event timestamps are non-monotonic within a run without an explicit clock-skew note.
547. Event-log integrity proof must fail if event producer identity is missing.
548. Event-log integrity proof must fail if safety-critical events have no causal link to the action being evaluated.
549. Event-log integrity proof must fail if event payload hashes do not match persisted payload files.
550. Event-log integrity proof must fail if event log summary counts do not match full event records.
551. Event-log integrity proof must fail if human-readable event summaries are used without validating underlying event JSON.
552. Policy-registry proof must include policy registry source hash, loaded policy set hash, policy evaluation input hash, and policy decision output hash.
553. Policy-registry proof must fail if policy rules are loaded from untracked generated reports instead of tracked source or declared fixtures.
554. Policy-registry proof must fail if policy evaluation can default to ALLOW when policy lookup fails.
555. Policy-registry proof must fail if policy evaluation can default to PASS when policy registry is empty.
556. Policy-registry proof must fail if policy rule ordering changes the decision and ordering is not deterministic.
557. Policy-registry proof must fail if multiple matching policy rules conflict and no conflict-resolution rule is recorded.
558. Policy-registry proof must fail if policy denial has no recorded reason code.
559. Policy-registry proof must fail if allowed action has no recorded rule id.
560. Policy-registry proof must fail if the unsafe self-promotion scenario is denied by an unrelated policy rule instead of the intended self-promotion rule.
561. Policy-registry proof must fail if the safe scenario passes because no policy was evaluated.
562. Capability-registry proof must include capability registry source hash, loaded capability set hash, and capability evaluation evidence.
563. Capability-registry proof must fail if missing capability defaults to allowed.
564. Capability-registry proof must fail if unknown capability names are accepted.
565. Capability-registry proof must fail if wildcard capabilities allow the unsafe self-promotion scenario without explicit restriction proof.
566. Capability-registry proof must fail if capability checks occur after irreversible side effects.
567. Capability-registry proof must fail if capability evidence is not linked to the exact action id.
568. Capability-registry proof must fail if capability decisions are logged but not enforced.
569. Capability-registry proof must fail if capability enforcement can be bypassed through replay, direct artifact write, or direct promotion command.
570. Invariant-engine proof must include invariant registry hash, evaluated invariant list, input context hash, and result hash.
571. Invariant-engine proof must fail if an invariant raises an exception and the exception is treated as PASS.
572. Invariant-engine proof must fail if invariant evaluation order changes final verdict without deterministic ordering.
573. Invariant-engine proof must fail if required invariants are configured but not executed.
574. Invariant-engine proof must fail if invariant results are logged but not enforced.
575. Invariant-engine proof must fail if no_self_promotion invariant is evaluated with incomplete context.
576. Invariant-engine proof must fail if generated agent identity and reviewer identity are compared using display names instead of stable IDs.
577. Invariant-engine proof must fail if case differences, whitespace, aliases, or alternate IDs allow self-promotion to pass.
578. Invariant-engine proof must fail if invariant input context can be altered after invariant evaluation without invalidating evidence.
579. Invariant-engine proof must fail if invariant results are stored only in memory and not persisted.
580. Promotion-gate proof must include gate input hash, reviewer decision hash, invariant result hash, policy decision hash, capability decision hash, and gate output hash.
581. Promotion-gate proof must fail if gate output is not a deterministic function of recorded gate inputs.
582. Promotion-gate proof must fail if promotion can happen through any path that bypasses the gate.
583. Promotion-gate proof must fail if gate denial does not prevent artifact activation.
584. Promotion-gate proof must fail if gate approval can occur without reviewer evidence.
585. Promotion-gate proof must fail if reviewer approval is not tied to the exact artifact hash being promoted.
586. Promotion-gate proof must fail if artifact content changes after approval but before promotion.
587. Promotion-gate proof must fail if approval for one artifact hash is reused for another artifact hash.
588. Promotion-gate proof must fail if approval for one action id is reused for another action id.
589. Promotion-gate proof must fail if denied action leaves active promoted state behind.
590. Artifact lifecycle proof must define states for created, stored, reviewed, approved, promoted, denied, rolled_back, and retained.
591. Artifact lifecycle proof must fail if an artifact skips required lifecycle states.
592. Artifact lifecycle proof must fail if denied artifacts are deleted before proof evidence is captured.
593. Artifact lifecycle proof must fail if promoted artifacts are not content-hash verified at activation time.
594. Artifact lifecycle proof must fail if artifact metadata hash and artifact content hash disagree.
595. Artifact lifecycle proof must fail if metadata can be edited to point to different content without detection.
596. Artifact lifecycle proof must fail if artifact IDs are mutable.
597. Artifact lifecycle proof must fail if artifact IDs collide across proof runs.
598. Artifact lifecycle proof must fail if artifact retention policy deletes evidence needed for final validation.
599. Artifact lifecycle proof must fail if artifact overwrite denial is enforced in tests but not in the runtime path used by scenarios.
600. Replay isolation proof must ensure replay cannot write to source directories, final proof reports, or original scenario evidence.
601. Replay isolation proof must fail if replay modifies original event logs or state records.
602. Replay isolation proof must fail if replay writes into the same artifact store namespace as original execution without isolation.
603. Replay isolation proof must fail if replay depends on mutable current global state instead of manifest-pinned inputs.
604. Replay isolation proof must fail if replay depends on current policy registry when the manifest requires historical policy registry hash.
605. Replay isolation proof must fail if replay depends on current source files whose hashes differ from the recorded run source hashes.
606. Replay isolation proof must fail if replay result changes when unrelated current repository files change.
607. Replay isolation proof must fail if replay can call network, clock-sensitive external APIs, or nondeterministic random sources.
608. Replay isolation proof must fail if replay reconstructs missing evidence using defaults instead of failing.
609. Replay isolation proof must fail if replay creates new evidence and treats it as original execution evidence.
610. Mutation-testing proof must include mutations to code paths, not only mutations to generated reports.
611. Mutation-testing proof must fail if changing critical runtime logic from DENY to ALLOW is not detected by tests or validators.
612. Mutation-testing proof must fail if changing artifact overwrite policy from deny to allow is not detected.
613. Mutation-testing proof must fail if changing replay comparison from strict to always true is not detected.
614. Mutation-testing proof must fail if changing validator exit codes to always zero is not detected.
615. Mutation-testing proof must fail if changing command recorder exit-code capture to always zero is not detected.
616. Mutation-testing proof must fail if changing gap resolution default to FIXED is not detected.
617. Mutation-testing proof must fail if changing traceability forced-pass guard is not detected.
618. Mutation-testing proof must fail if changing evidence hash comparison to string existence check is not detected.
619. Mutation-testing proof must fail if changing report-dir resolution to append .agentx-init/reports twice is not detected.
620. Mutation-testing proof must record mutation operators, mutated files, expected failures, actual failures, and surviving mutations.
621. CI proof must include a workflow or documented local equivalent that runs the final proof target from a clean checkout.
622. CI proof must fail if the final proof target is only known to pass on the developer's local machine.
623. CI proof must record operating system, CPU architecture, shell, Python version, and dependency versions used by the proof run.
624. CI proof must fail if local proof and CI proof produce different final verdicts for the same commit.
625. CI proof must fail if the workflow uploads stale artifacts from a previous run.
626. CI proof must fail if artifact upload happens before final all-in-one validation.
627. CI proof must fail if CI continues after failed tests, validators, replay, traceability, or anti-false-PASS audit.
628. CI proof must fail if CI uses a narrower proof command than the documented final local proof target.
629. CI proof must fail if CI passes because required tests are skipped due to unavailable optional dependencies.
630. CI proof must fail if CI caches generated proof reports and reuses them as current evidence.
631. CI proof must fail if dependency caches are restored without validating dependency lockfile or declared dependency hashes.
632. CI proof must fail if a matrix job reports PASS while another required matrix job fails or is missing.
633. CI proof must fail if GitHub Actions or other automation marks success without storing the final verdict artifact.
634. CI proof must fail if generated reports are not retained long enough for independent inspection.
635. CI proof must fail if workflow permissions allow proof scripts to modify repository source during validation.
636. CI proof must fail if workflow permissions or tokens are written into proof artifacts.
637. Container reproducibility proof must define whether the proof is supported on bare metal, container, or both.
638. Container reproducibility proof must fail if Dockerfile or container recipe is stale relative to declared dependencies.
639. Container reproducibility proof must fail if container proof requires network access after dependencies are installed.
640. Container reproducibility proof must fail if container proof depends on mounted host caches not declared in proof metadata.
641. Container reproducibility proof must fail if running inside a container changes proof semantics without being recorded.
642. Container reproducibility proof must fail if UID, file permissions, or umask differences alter proof outputs without normalization.
643. Container reproducibility proof must fail if generated files are owned by unexpected users and cannot be validated or cleaned.
644. Filesystem portability proof must fail if proof relies on case-sensitive paths without detecting case-insensitive collisions.
645. Filesystem portability proof must fail if proof relies on POSIX-only symlink behavior without declaring Linux-only support.
646. Filesystem portability proof must fail if newline conversion changes hashes without explicit binary-mode hashing.
647. Filesystem portability proof must fail if CRLF and LF versions of the same report produce inconsistent validation results without normalization rules.
648. Filesystem portability proof must fail if file permission bits are part of evidence semantics but are not recorded.
649. Filesystem portability proof must fail if executable scripts lack executable bits where direct execution is required.
650. Filesystem portability proof must fail if proof output path length can exceed common filesystem limits without detection.
651. Filesystem portability proof must fail if filenames use characters unsupported by declared target platforms.
652. Lockfile proof must include dependency lockfile hash when a lockfile exists.
653. Lockfile proof must fail if dependency metadata changes after a passing proof transcript is recorded.
654. Lockfile proof must fail if undeclared optional extras change test or validator behavior.
655. Lockfile proof must fail if dependency resolution is nondeterministic across proof runs.
656. Lockfile proof must fail if dependency installation modifies tracked source files.
657. Lockfile proof must fail if the proof target installs or upgrades dependencies during final validation without recording the resulting dependency state.
658. Lockfile proof must fail if dependency versions are read from the current environment but not recorded in the final proof metadata.
659. Packaging proof must fail if the package cannot be installed from a clean checkout using the declared installation method.
660. Packaging proof must fail if installed package behavior differs from source-tree behavior used by tests.
661. Packaging proof must fail if console entry points are declared but not exercised by proof commands.
662. Packaging proof must fail if import paths work only because tests run from the repository root.
663. Packaging proof must fail if package data files required by runtime proof are missing from packaging metadata.
664. Packaging proof must fail if generated reports are included in the package as static data used by validators.
665. Database/state-store proof must define whether state is file-based, SQLite-based, in-memory, or another store.
666. Database/state-store proof must fail if durable state proof uses in-memory state while claiming persistence.
667. Database/state-store proof must fail if schema migrations are required but not executed in the proof path.
668. Database/state-store proof must fail if old state-store files from previous runs affect current scenario outcomes.
669. Database/state-store proof must fail if unsafe scenario state persists in a way that contaminates safe scenario proof.
670. Database/state-store proof must fail if safe scenario proof relies on state created by unsafe scenario proof unless explicitly chained and evidenced.
671. Database/state-store proof must fail if state cleanup deletes evidence before hashing and final validation.
672. Database/state-store proof must fail if transactional boundaries are not recorded for promotion, denial, rollback, and artifact writes.
673. Database/state-store proof must fail if partial transaction commits are possible after validator or runtime failure.
674. Database/state-store proof must fail if concurrent writes can corrupt state without detection.
675. Retry proof must record every retry of tests, validators, replay, proof generation, or scenario execution.
676. Retry proof must fail if a failing command is retried and only the later passing attempt is reported.
677. Retry proof must fail if retries are used to hide flakiness in final proof mode.
678. Retry proof must fail if flaky tests are allowed to pass by eventually succeeding without a flakiness classification.
679. Retry proof must fail if final verdict ignores earlier failed attempts in the same proof_run_id.
680. Retry proof must fail if command transcript omits failed attempts that occurred before a successful retry.
681. Flakiness proof must fail if repeated final proof runs produce inconsistent verdicts.
682. Flakiness proof must fail if repeated scenario runs produce different safety events, state transitions, or artifact hashes without a documented nondeterminism source.
683. Flakiness proof must fail if tests rely on sleep timing instead of deterministic synchronization for proof-critical behavior.
684. Flakiness proof must fail if clock-sensitive tests pass only under a narrow timing window.
685. Flakiness proof must fail if ordering-dependent tests pass only when executed alone.
686. Flakiness proof must fail if parallel pytest execution changes proof results.
687. Flakiness proof must fail if random test order changes proof results.
688. Parallelism proof must define whether proof commands are serial or parallel.
689. Parallelism proof must fail if parallel proof commands write to the same report files without locking.
690. Parallelism proof must fail if parallel tests share artifact store, state store, or report directory without isolation.
691. Parallelism proof must fail if parallel replay modifies shared manifests or state records.
692. Parallelism proof must fail if order of parallel completion changes final proof report contents.
693. Parallelism proof must fail if proof_run_id is not propagated into parallel workers.
694. Error taxonomy proof must classify failures as validation_failure, infrastructure_failure, runtime_failure, replay_failure, evidence_failure, schema_failure, or security_failure.
695. Error taxonomy proof must fail if unknown exceptions are converted into generic PASS or warning states.
696. Error taxonomy proof must fail if infrastructure failures are counted as successful anti-false-PASS attack rejections.
697. Error taxonomy proof must fail if validation failures and runtime denials are conflated.
698. Error taxonomy proof must fail if failed proof substeps lack machine-readable failure codes.
699. Error taxonomy proof must fail if final verdict does not list blocking failure codes when classification is not FUNCTIONAL_RUNTIME_MVP.
700. Partial-success proof must fail if PARTIAL, DEGRADED, WARNING, or BEST_EFFORT statuses are accepted as PASS.
701. Partial-success proof must fail if warnings exist on proof-critical components and final classification still claims FUNCTIONAL_RUNTIME_MVP without explicit waiver rules.
702. Partial-success proof must fail if waivers are allowed without requirement ID, owner, rationale, expiry, and validator enforcement.
703. Partial-success proof must fail if a waived item is mandatory for the claimed MVP classification.
704. Partial-success proof must fail if warnings are written only to human-readable markdown and absent from JSON.
705. Waiver proof must fail if waivers can be created by report generators automatically.
706. Waiver proof must fail if a waiver can downgrade an MVP_BLOCKER to PASS.
707. Waiver proof must fail if a waiver references a requirement, gap, or evidence item that does not exist.
708. Waiver proof must fail if expired waivers are accepted.
709. Waiver proof must fail if waiver metadata is not included in final proof hashes.
710. Golden-fixture proof must distinguish golden input fixtures from generated proof outputs.
711. Golden-fixture proof must fail if generated proof outputs are used as golden fixtures for the same proof run.
712. Golden-fixture proof must fail if golden fixtures are updated automatically during final proof validation.
713. Golden-fixture proof must fail if golden fixture updates are not reviewed and hashed as source changes.
714. Golden-fixture proof must fail if fixture contents contain PASS verdicts that validators trust without recomputation.
715. Golden-fixture proof must fail if fixture paths overlap with final report output paths.
716. Snapshot-test proof must fail if snapshots are accepted as proof of runtime correctness without semantic validators.
717. Snapshot-test proof must fail if snapshots are auto-updated during proof mode.
718. Snapshot-test proof must fail if snapshot diffs are ignored in final proof mode.
719. Snapshot-test proof must fail if snapshot tests compare only markdown formatting and not machine-readable semantics.
720. Documentation proof must fail if README or prompt claims FUNCTIONAL_RUNTIME_MVP while final verdict file claims a lower classification.
721. Documentation proof must fail if user-facing docs describe proof commands that do not exist or do not run.
722. Documentation proof must fail if docs mention deprecated proof commands that can still produce misleading PASS reports.
723. Documentation proof must fail if docs tell users to inspect generated markdown instead of running detached verification.
724. Documentation proof must fail if docs omit known limitations that are recorded as OUT_OF_SCOPE or BLOCKED.
725. Documentation proof must fail if docs imply CI passed when no CI evidence exists.
726. Release proof must fail if a release tag points to a commit without a passing final proof verdict.
727. Release proof must fail if release artifacts are built from a different commit than the final proof verdict.
728. Release proof must fail if release notes claim FUNCTIONAL_RUNTIME_MVP without linking to final proof artifact hashes.
729. Release proof must fail if version number changes after final proof without rerunning proof.
730. Release proof must fail if packaged release contains generated PASS reports that are not marked as historical artifacts.
731. Release proof must fail if source archive excludes files required to reproduce the proof.
732. Branch proof must record branch name, commit hash, and whether the proof was run on a protected or temporary branch.
733. Branch proof must fail if final proof is run on one branch but final verdict is applied to another branch without matching commit hash.
734. Branch proof must fail if merge commits change source after proof without rerunning proof.
735. Branch proof must fail if rebasing invalidates proof commit ancestry and proof metadata does not record the exact commit.
736. Branch proof must fail if proof artifacts from an unmerged branch are used to claim status on main.
737. Branch proof must fail if dirty working tree changes are not included in source hashes.
738. Subprocess isolation proof must fail if proof commands run in-process and share mutable global state that affects later validators.
739. Subprocess isolation proof must fail if validator results depend on import order from earlier tests.
740. Subprocess isolation proof must fail if global singleton state from one scenario contaminates another scenario.
741. Subprocess isolation proof must fail if monkeypatches remain active across proof steps.
742. Subprocess isolation proof must fail if environment mutations from one command affect another command without being recorded.
743. Subprocess isolation proof must fail if cwd mutations from one command affect another command without being recorded.
744. Subprocess isolation proof must fail if proof target uses Python modules already imported before source hash verification.
745. Clean-process proof must run critical validators as independent subprocesses in final proof mode.
746. Clean-process proof must fail if independent subprocess validation disagrees with in-process validation.
747. Clean-process proof must fail if validators require prior in-process setup that is not reproduced by the CLI.
748. Clean-process proof must fail if validators only pass when called from a specific test harness.
749. Clean-process proof must fail if final proof depends on hidden pytest fixtures not available to users.
750. Performance-bound proof must define maximum acceptable runtime for proof-critical commands or explicitly state that no bound is claimed.
751. Performance-bound proof must fail if timeouts are absent for commands that can hang.
752. Performance-bound proof must fail if proof generation scales with stale report accumulation instead of current proof scope.
753. Performance-bound proof must fail if anti-false-PASS audit skips attacks because they are too slow without marking proof incomplete.
754. Performance-bound proof must fail if replay proof uses shortcuts solely to avoid executing required replay behavior.
755. Resource-bound proof must record disk space used by proof artifacts.
756. Resource-bound proof must fail if proof artifacts grow unbounded across repeated runs.
757. Resource-bound proof must fail if cleanup removes current evidence instead of only stale evidence.
758. Resource-bound proof must fail if proof commands require excessive memory due to loading unrelated historical reports.
759. Resource-bound proof must fail if report generators scan outside the repository and accidentally include user files.
760. Resource-bound proof must fail if validators read arbitrary large files from evidence paths without size limits or hashing policy.
761. LLM-integration proof must define whether any proof-critical step uses an LLM, and must fail if LLM output is treated as deterministic evidence without validation.
762. LLM-integration proof must fail if an LLM can write PASS reports directly.
763. LLM-integration proof must fail if an LLM can modify validators, tests, proof generators, or source code during the final proof run.
764. LLM-integration proof must fail if LLM-generated summaries are used as evidence instead of machine-validated artifacts.
765. LLM-integration proof must fail if an LLM can decide that missing evidence is acceptable.
766. LLM-integration proof must fail if an LLM can classify discovered gaps as BENIGN, OUT_OF_SCOPE, or FIXED without validator-enforced rationale.
767. LLM-integration proof must fail if LLM prompts, responses, model name, model version, and temperature are omitted when LLM output affects implementation or proof artifacts.
768. LLM-integration proof must fail if nondeterministic LLM output affects final verdict reproducibility.
769. LLM-integration proof must fail if prompt-injection text inside reports, logs, artifacts, or source comments can alter proof behavior.
770. LLM-integration proof must fail if proof scripts execute instructions found inside generated reports or logs.
771. Prompt-injection proof must include malicious strings inside evidence files, command logs, markdown reports, JSON reports, and scenario artifacts.
772. Prompt-injection proof must fail if validators treat prose instructions inside evidence as commands.
773. Prompt-injection proof must fail if report parsers evaluate report content as Python, shell, Jinja, YAML tags, or another executable format.
774. Prompt-injection proof must fail if markdown links, HTML comments, front matter, or fenced code blocks can affect validation outcomes.
775. Prompt-injection proof must fail if JSON fields not defined by schema can instruct validators to skip checks.
776. Prompt-injection proof must fail if attacker-controlled filenames can change shell command behavior.
777. Prompt-injection proof must fail if attacker-controlled artifact content can change replay behavior outside declared manifest fields.
778. Prompt-injection proof must fail if attacker-controlled event payloads can change report generation logic.
779. Prompt-injection proof must fail if attacker-controlled stdout/stderr can forge transcript entries.
780. Prompt-injection proof must fail if command logs containing PASS text can be accepted as proof without structured exit-code validation.
781. Tool-adapter proof must define every external tool used by the runtime MVP and proof path.
782. Tool-adapter proof must fail if tool outputs are accepted without schema validation.
783. Tool-adapter proof must fail if tool failures are converted into default PASS values.
784. Tool-adapter proof must fail if a missing tool is silently replaced by a mock in final proof mode.
785. Tool-adapter proof must fail if a tool version changes without updating proof metadata.
786. Tool-adapter proof must fail if a tool command is looked up from PATH without recording the resolved executable path.
787. Tool-adapter proof must fail if a malicious executable earlier in PATH can satisfy a proof command.
788. Tool-adapter proof must fail if external tool stdout is parsed using brittle human-readable strings instead of stable structured output where available.
789. Tool-adapter proof must fail if external tool stderr warnings are ignored when they indicate partial failure.
790. Tool-adapter proof must fail if tool timeouts, signals, or crashes are not represented in machine-readable proof results.
791. Sandbox proof must define which operations are allowed and denied during runtime evolution, proof generation, replay, and validation.
792. Sandbox proof must fail if runtime evolution can write outside its workspace.
793. Sandbox proof must fail if runtime evolution can modify source code during proof scenarios unless that modification is the explicit behavior under test and is reverted or isolated.
794. Sandbox proof must fail if proof scripts can accidentally validate artifacts created outside the sandbox.
795. Sandbox proof must fail if denied operations are only logged and not actually blocked.
796. Sandbox proof must fail if path traversal through artifact names escapes sandbox roots.
797. Sandbox proof must fail if symlinks, hardlinks, bind mounts, or relative paths bypass sandbox roots.
798. Sandbox proof must fail if sandbox cleanup deletes proof evidence before final validation.
799. Sandbox proof must fail if sandbox policy differs between tests and runtime scenarios.
800. Sandbox proof must fail if sandbox allowlist is empty but actions still execute.
801. Privilege proof must fail if proof commands require root or elevated privileges without justification.
802. Privilege proof must fail if running as root masks permission bugs that occur for normal users.
803. Privilege proof must fail if file permissions allow generated agents or proof artifacts to modify validators or source files.
804. Privilege proof must fail if proof artifacts are world-writable.
805. Privilege proof must fail if runtime-created files are executable when no executable permission is required.
806. Privilege proof must fail if secret files, SSH keys, tokens, or home-directory config are readable by proof scripts without being declared.
807. Privilege proof must fail if proof behavior changes based on user id, group id, or file ownership and this is not recorded.
808. Privilege proof must fail if umask differences can make proof artifacts unreadable or writable in unsafe ways.
809. Privilege proof must fail if permission errors are downgraded to warnings during final validation.
810. Privilege proof must fail if artifact promotion changes file permissions without evidence.
811. Secret-handling proof must fail if environment secrets are printed into command transcripts.
812. Secret-handling proof must fail if generated reports contain access tokens, API keys, cookies, private keys, OAuth tokens, or password-like values.
813. Secret-handling proof must fail if redaction changes hashes of proof-critical command logs without preserving a hashed unredacted artifact in a secure excluded location or an explicit reproducibility policy.
814. Secret-handling proof must fail if secret redaction can remove validation errors from logs.
815. Secret-handling proof must fail if secret-looking values are used as stable evidence IDs.
816. Secret-handling proof must fail if proof commands require real production credentials.
817. Secret-handling proof must fail if fake credentials are not clearly marked as fixtures.
818. Secret-handling proof must fail if external service credentials can change proof outcomes.
819. Secret-handling proof must fail if generated artifacts include user-specific absolute paths that leak local environment details and affect reproducibility.
820. Privacy proof must fail if proof reports contain personal data not required for technical validation.
821. Privacy proof must fail if user names, emails, tokens, or machine-specific identifiers are stored in final artifacts without need.
822. Privacy proof must fail if command transcripts include full environment dumps instead of an allowlisted environment subset.
823. Privacy proof must fail if evidence artifacts contain unrelated user files discovered by broad filesystem scans.
824. Privacy proof must fail if logs from failed commands leak sensitive local filesystem contents.
825. Privacy proof must fail if proof artifacts are intended for publication but include non-redacted private data.
826. Auditability proof must include a concise machine-readable index of all final proof artifacts.
827. Auditability proof must fail if an independent reviewer cannot locate every evidence file referenced by final verdict.
828. Auditability proof must fail if artifact names are unstable, ambiguous, or reused for different semantics.
829. Auditability proof must fail if report filenames differ from report_type fields.
830. Auditability proof must fail if multiple reports claim to be the final verdict.
831. Auditability proof must fail if older final verdict files remain in the final report directory.
832. Auditability proof must fail if final artifact index contains files not produced by the current proof_run_id.
833. Auditability proof must fail if final artifact index omits any JSON, markdown, log, manifest, or evidence file used by validators.
834. Auditability proof must fail if final artifact index includes temporary files, lock files, partial files, or editor backups.
835. Auditability proof must fail if human-readable report summaries disagree with machine-readable artifact index.
836. Artifact-index proof must include file size, SHA-256 hash, report_type, proof_run_id, git_commit, and generation step for every artifact.
837. Artifact-index proof must fail if any final artifact is larger than the declared maximum size without justification.
838. Artifact-index proof must fail if any final artifact has modification time outside the proof run time window.
839. Artifact-index proof must fail if any final artifact is modified after final detached verification.
840. Artifact-index proof must fail if any expected artifact is absent or duplicated.
841. Artifact-index proof must fail if artifacts are generated in nondeterministic names that cannot be traced to requirements or proof steps.
842. Artifact-index proof must fail if JSON and markdown pairs do not share a common base name or explicit relationship.
843. Artifact-index proof must fail if evidence files are not grouped by scenario, validator, attack, or proof stage.
844. Artifact-index proof must fail if stale artifacts are retained in the final bundle without being marked historical and excluded from verdict.
845. Artifact-index proof must fail if report directory cleanup removes the artifact index before detached verification.
846. Configuration proof must list every configuration file read during proof commands.
847. Configuration proof must fail if configuration defaults are different between tests, local proof, CI proof, and replay.
848. Configuration proof must fail if missing configuration defaults to permissive behavior.
849. Configuration proof must fail if configuration files are read from user home directories in final proof mode.
850. Configuration proof must fail if configuration loaded from environment variables is not recorded.
851. Configuration proof must fail if configuration changes after source hash collection but before scenario execution.
852. Configuration proof must fail if configuration supports aliases that let self-promotion identity checks pass incorrectly.
853. Configuration proof must fail if configuration can mark validators optional in final proof mode.
854. Configuration proof must fail if configuration can reduce anti-false-PASS attack count without blocking FUNCTIONAL_RUNTIME_MVP.
855. Configuration proof must fail if configuration can change required requirement IDs without updating the canonical requirement hash.
856. Schema-source proof must store schemas as tracked source files or generate them deterministically from tracked source.
857. Schema-source proof must fail if schemas are generated from current reports.
858. Schema-source proof must fail if validators and generators use different schema definitions.
859. Schema-source proof must fail if schema changes are not covered by schema-specific tests.
860. Schema-source proof must fail if schema files are missing from source manifest.
861. Schema-source proof must fail if schema validation is optional for any final JSON report.
862. Schema-source proof must fail if markdown-only reports have no corresponding JSON schema or are not explicitly non-authoritative.
863. Schema-source proof must fail if schemas allow arbitrary additional properties in proof-critical objects.
864. Schema-source proof must fail if schemas allow status strings outside the declared enum.
865. Schema-source proof must fail if schemas allow evidence_refs to be omitted on PASS objects.
866. Observer-effect proof must fail if enabling command recording, evidence collection, or validation changes runtime behavior being measured.
867. Observer-effect proof must fail if scenarios pass only when recording is disabled.
868. Observer-effect proof must fail if scenarios pass only when recording is enabled.
869. Observer-effect proof must fail if replay uses recorded output that the original scenario did not produce.
870. Observer-effect proof must fail if evidence collection changes artifact hashes.
871. Observer-effect proof must fail if source mutation checks ignore files created by instrumentation.
872. Observer-effect proof must fail if instrumentation hides exceptions or retries failed operations.
873. Observer-effect proof must fail if instrumentation introduces side effects not present in normal runtime.
874. Observer-effect proof must fail if proof-only code paths replace production code paths.
875. Observer-effect proof must fail if proof mode bypasses normal validation to make evidence easier to collect.
876. Negative-path proof must include runtime tests for missing policy, missing capability, missing invariant, missing artifact, missing reviewer, malformed action, stale approval, and replay mismatch.
877. Negative-path proof must fail if any required negative path returns success.
878. Negative-path proof must fail if denied negative paths do not produce durable denial evidence.
879. Negative-path proof must fail if negative-path tests assert only exceptions and not state/event/artifact side effects.
880. Negative-path proof must fail if negative-path tests use mocks that bypass actual runtime enforcement.
881. Negative-path proof must fail if negative-path evidence is excluded from final reports.
882. Negative-path proof must fail if negative-path results are not represented in traceability.
883. Negative-path proof must fail if negative-path regressions are not included in anti-false-PASS or mutation tests.
884. Boundary-condition proof must test empty inputs, duplicate inputs, maximum reasonable inputs, invalid types, invalid paths, invalid hashes, missing files, and corrupted files.
885. Boundary-condition proof must fail if empty lists of evidence, validators, requirements, attacks, scenarios, or reports are accepted as PASS.
886. Boundary-condition proof must fail if duplicate evidence refs inflate PASS counts.
887. Boundary-condition proof must fail if duplicate requirement rows inflate coverage percentages.
888. Boundary-condition proof must fail if duplicate validator entries hide a missing validator.
889. Boundary-condition proof must fail if duplicate attack entries hide an untested attack.
890. Boundary-condition proof must fail if invalid hash strings are accepted because only length is checked.
891. Boundary-condition proof must fail if invalid paths are accepted because only existence is checked.
892. Boundary-condition proof must fail if oversized reports cause validators to skip content validation.
893. Boundary-condition proof must fail if deeply nested JSON causes validators to crash and treat the crash as infrastructure success.
894. Ordering proof must fail if report rows reorder between runs and cause false hash mismatches without canonical sorting.
895. Ordering proof must fail if report rows reorder between runs and hide semantic differences through sorting that drops duplicates.
896. Ordering proof must fail if traceability rows are sorted differently across reports without stable IDs.
897. Ordering proof must fail if anti-false-PASS attack order changes expected counts or outcomes.
898. Ordering proof must fail if command transcript order is inferred from timestamps alone when timestamps have insufficient precision.
899. Ordering proof must fail if two commands have identical timestamps and no sequence number.
900. Ordering proof must fail if event order is inferred from timestamps alone without sequence numbers.
901. Ordering proof must fail if state order is inferred from file listing order.
902. Ordering proof must fail if scenario order changes proof output hashes without semantic reason.
903. Ordering proof must fail if deterministic sorting masks conflicting duplicate IDs.
904. Compatibility-scope proof must define which Python versions, OS platforms, shells, and filesystems are supported for this MVP.
905. Compatibility-scope proof must fail if compatibility report claims unsupported platforms passed.
906. Compatibility-scope proof must fail if compatibility checks are documentation-only but reported as executed.
907. Compatibility-scope proof must fail if a supported platform lacks proof evidence.
908. Compatibility-scope proof must fail if a platform-specific skip is treated as PASS.
909. Compatibility-scope proof must fail if platform assumptions are not included in final verdict metadata.
910. Compatibility-scope proof must fail if unsupported platforms are not clearly marked unsupported instead of failed or passed.
911. Compatibility-scope proof must fail if path handling differs across supported platforms and is not tested.
912. Compatibility-scope proof must fail if shell-specific syntax is used without declaring shell requirement.
913. Compatibility-scope proof must fail if proof commands depend on GNU utilities but support claims include non-GNU environments.
914. Metrics proof must define which quantitative metrics are used for final proof and which are informational only.
915. Metrics proof must fail if informational metrics are used to justify PASS without requirement-level evidence.
916. Metrics proof must fail if coverage percentages are computed from generated reports rather than executed tests and validators.
917. Metrics proof must fail if denominator values are missing, zero, duplicated, or inconsistent across reports.
918. Metrics proof must fail if pass_rate, coverage_rate, rejection_rate, or match_rate are rounded up to 100 percent when raw counts are incomplete.
919. Metrics proof must fail if summary percentages disagree with raw row counts.
920. Metrics proof must fail if a PASS summary ignores failed, skipped, blocked, warning, unknown, or unclassified rows.
921. Metrics proof must fail if counts are computed before duplicate IDs and stale rows are removed.
922. Metrics proof must fail if report generators use floating-point equality for proof-critical comparisons where integer counts are available.
923. Metrics proof must fail if dashboards or badges report PASS from stale metric files.
924. Audit-log proof must define the authoritative audit log for proof generation and runtime scenarios.
925. Audit-log proof must fail if audit log entries can be edited, deleted, or reordered without hash-chain detection.
926. Audit-log proof must fail if audit log sequence numbers have gaps without an explicit missing-entry record.
927. Audit-log proof must fail if audit log actor identity is missing for proof-critical operations.
928. Audit-log proof must fail if audit log timestamps are outside the proof_run_id time window.
929. Audit-log proof must fail if audit log records final PASS before the final all-in-one validator passed.
930. Audit-log proof must fail if audit log summary counts differ from full audit log entries.
931. Audit-log proof must fail if audit log stores only human-readable messages and no structured event fields.
932. Audit-log proof must fail if audit log validation depends on current filesystem state rather than recorded hashes.
933. Audit-log proof must fail if audit log is not included in final artifact index and proof hashes.
934. Approval-expiry proof must define whether human approvals expire or remain valid indefinitely.
935. Approval-expiry proof must fail if expired approval can authorize promotion.
936. Approval-expiry proof must fail if approval timestamp is missing or invalid.
937. Approval-expiry proof must fail if approval is recorded after promotion but used as if it occurred before promotion.
938. Approval-expiry proof must fail if approval can be reused after artifact content changes.
939. Approval-expiry proof must fail if approval can be reused after policy, capability, or invariant context changes.
940. Approval-expiry proof must fail if approval scope is broader than the exact artifact, action, agent, and proof_run_id without justification.
941. Approval-expiry proof must fail if approval revocation is ignored.
942. Approval-expiry proof must fail if approval identity is not resolved to a stable reviewer ID.
943. Approval-expiry proof must fail if approval evidence is only a markdown note and not a structured record.
944. Access-control proof must define who or what can create, modify, validate, and promote artifacts.
945. Access-control proof must fail if generated agents can modify their own review records.
946. Access-control proof must fail if generated agents can modify policy, capability, invariant, or promotion-gate configuration.
947. Access-control proof must fail if proof artifacts can be modified by runtime scenario code after validation.
948. Access-control proof must fail if validators can be overwritten by artifacts produced during scenario execution.
949. Access-control proof must fail if report generators can read untrusted runtime artifacts as executable configuration.
950. Access-control proof must fail if denied artifacts remain reachable through an activation path.
951. Access-control proof must fail if access denial is logged but not enforced.
952. Access-control proof must fail if a missing ACL defaults to allow.
953. Access-control proof must fail if wildcard allow rules override explicit deny rules without deterministic precedence.
954. Activation proof must define what it means for an artifact or generated agent to be active.
955. Activation proof must fail if an artifact is considered active without a recorded promotion event.
956. Activation proof must fail if an artifact is active by filename placement alone without promotion-state validation.
957. Activation proof must fail if denied or rolled-back artifacts remain on the active search path.
958. Activation proof must fail if activation path resolution is not hashed and recorded.
959. Activation proof must fail if two artifacts can be active for the same target without a conflict-resolution rule.
960. Activation proof must fail if activation state differs between runtime state, filesystem state, and report state.
961. Activation proof must fail if replay can activate artifacts.
962. Activation proof must fail if activation status can be inferred from stale metadata.
963. Activation proof must fail if active artifact bytes differ from the approved artifact hash.
964. Rollback-depth proof must define what state, artifacts, events, and metadata must be reverted or retained during rollback.
965. Rollback-depth proof must fail if rollback removes denial evidence required for audit.
966. Rollback-depth proof must fail if rollback restores source files but leaves active artifacts from failed actions.
967. Rollback-depth proof must fail if rollback restores artifacts but leaves state records claiming promotion.
968. Rollback-depth proof must fail if rollback event lacks before and after hashes.
969. Rollback-depth proof must fail if rollback can be marked successful without verifying final state.
970. Rollback-depth proof must fail if nested or repeated rollback actions are not represented in state/event records.
971. Rollback-depth proof must fail if rollback of one action affects unrelated artifacts or state.
972. Rollback-depth proof must fail if rollback failure is not a blocking proof failure.
973. Rollback-depth proof must fail if rollback behavior is only unit-tested and not represented in scenario proof where required.
974. Crash-recovery proof must define expected behavior after interrupted proof generation, interrupted scenario execution, and interrupted promotion.
975. Crash-recovery proof must fail if a crash during promotion can leave artifact active without approval evidence.
976. Crash-recovery proof must fail if a crash during denial can lose safety-event evidence.
977. Crash-recovery proof must fail if a crash during report generation can leave a valid-looking partial PASS report.
978. Crash-recovery proof must fail if rerunning after a crash reuses partial artifacts as final evidence.
979. Crash-recovery proof must fail if locks remain permanently stuck after interrupted proof execution.
980. Crash-recovery proof must fail if stale lock cleanup can delete a live run's lock.
981. Crash-recovery proof must fail if proof_run_id is reused after a crash without explicit recovery metadata.
982. Crash-recovery proof must fail if crash recovery changes final verdict without recording recovery actions.
983. Crash-recovery proof must include negative tests for interrupted writes or simulated mid-run failure.
984. Data-retention proof must define which generated artifacts are retained, which are temporary, and which are excluded from final proof.
985. Data-retention proof must fail if temporary artifacts are referenced by final reports.
986. Data-retention proof must fail if retained artifacts are deleted before detached verification.
987. Data-retention proof must fail if excluded artifacts affect final verdict.
988. Data-retention proof must fail if historical artifacts are mixed with current proof artifacts.
989. Data-retention proof must fail if cleanup rules can delete current proof_run_id evidence.
990. Data-retention proof must fail if cleanup rules leave stale PASS reports in the final report directory.
991. Data-retention proof must fail if retention policy is different between local proof and CI proof.
992. Data-retention proof must fail if artifact retention is undocumented for release artifacts.
993. Data-retention proof must fail if proof evidence depends on ephemeral process memory.
994. Archive proof must define whether final proof artifacts may be compressed, bundled, or relocated.
995. Archive proof must fail if compression changes file bytes but hashes are not recomputed or separately recorded.
996. Archive proof must fail if archive extraction can overwrite files outside the target directory.
997. Archive proof must fail if archive entries contain absolute paths, parent-directory traversal, symlinks, hardlinks, or device files.
998. Archive proof must fail if detached verification cannot validate artifacts after extraction.
999. Archive proof must fail if archive manifest differs from artifact index.
1000. Archive proof must fail if archive creation occurs before final verification.
1001. Archive proof must fail if archive contents are modified after final verdict without invalidating archive hash.
1002. Archive proof must fail if archive omits logs needed to verify command transcript hashes.
1003. Archive proof must fail if archive includes stale historical artifacts as current proof evidence.
1004. Human-readable consistency proof must fail if headings, badges, summaries, or status icons disagree with JSON verdicts.
1005. Human-readable consistency proof must fail if markdown tables omit failing rows present in JSON.
1006. Human-readable consistency proof must fail if markdown reports collapse distinct warnings, blockers, or failed checks into a generic PASS section.
1007. Human-readable consistency proof must fail if markdown links point to files not present in the final artifact index.
1008. Human-readable consistency proof must fail if markdown report order changes requirement identity.
1009. Human-readable consistency proof must fail if generated markdown includes manual edits not reflected in JSON.
1010. Human-readable consistency proof must fail if markdown says 'done' or 'complete' while final classification is lower than FUNCTIONAL_RUNTIME_MVP.
1011. Human-readable consistency proof must fail if markdown uses stale timestamps or commit hashes.
1012. Human-readable consistency proof must fail if markdown renders escaped or truncated data that changes validation meaning.
1013. Human-readable consistency proof must fail if markdown is used as a fallback when JSON validation fails.
1014. API-contract proof must define stable internal APIs used by proof generators, validators, replay executor, artifact store, policy registry, invariant engine, and promotion gate.
1015. API-contract proof must fail if proof-critical APIs return untyped dictionaries with undocumented fields.
1016. API-contract proof must fail if API return schemas can omit verdict, errors, evidence_refs, or hashes while still being accepted.
1017. API-contract proof must fail if API exceptions are swallowed and replaced with default success objects.
1018. API-contract proof must fail if API consumers ignore returned errors.
1019. API-contract proof must fail if API consumers trust caller-supplied status instead of recomputing status.
1020. API-contract proof must fail if API version changes are not reflected in report schema versions.
1021. API-contract proof must fail if backward compatibility shims weaken validation.
1022. API-contract proof must fail if production APIs and test-helper APIs have the same names and can be confused.
1023. API-contract proof must fail if API methods mutate input objects in ways that alter later proof evidence unexpectedly.
1024. Serialization proof must define canonical serialization for JSON, logs, manifests, and hash inputs.
1025. Serialization proof must fail if the same proof object serializes differently across Python versions without normalization.
1026. Serialization proof must fail if datetime serialization omits timezone or proof_run_id context.
1027. Serialization proof must fail if binary artifacts are decoded as text for hashing.
1028. Serialization proof must fail if text files are hashed after newline normalization in one place and raw-byte hashing in another.
1029. Serialization proof must fail if key order affects semantic validation.
1030. Serialization proof must fail if duplicate JSON keys are accepted.
1031. Serialization proof must fail if parsers accept comments or trailing commas in final proof JSON.
1032. Serialization proof must fail if numeric fields accept strings in some validators but numbers in others without schema rules.
1033. Serialization proof must fail if boolean fields accept truthy strings such as 'yes', 'true', or 'PASS' without strict schema rules.
1034. Internationalization proof must fail if non-ASCII paths, IDs, or report text break validation under supported environments.
1035. Internationalization proof must fail if Unicode confusables can create duplicate-looking IDs or paths.
1036. Internationalization proof must fail if normalization differences allow two different requirement IDs to appear identical.
1037. Internationalization proof must fail if case folding of reviewer or agent identity is inconsistent.
1038. Internationalization proof must fail if locale-specific date parsing affects proof reports.
1039. Internationalization proof must fail if locale-specific decimal separators affect metrics.
1040. Internationalization proof must fail if sorting order changes under different locales and affects hashes.
1041. Internationalization proof must fail if validators assume ASCII-only content where UTF-8 is allowed.
1042. Internationalization proof must fail if terminal encoding changes command transcript hashes without binary log capture.
1043. Internationalization proof must fail if invalid UTF-8 is silently replaced during proof-critical parsing.
1044. Deprecation proof must list deprecated proof reports, validators, commands, and fields.
1045. Deprecation proof must fail if deprecated reports are still accepted as final evidence.
1046. Deprecation proof must fail if deprecated commands can produce PASS final verdicts.
1047. Deprecation proof must fail if deprecated fields override current fields.
1048. Deprecation proof must fail if deprecated validators are still included in required validator lists without rationale.
1049. Deprecation proof must fail if old generated reports remain in final artifact index without historical marking.
1050. Deprecation proof must fail if report generators write both old and new schemas without compatibility validation.
1051. Deprecation proof must fail if migration from old reports can silently convert FAIL to PASS.
1052. Deprecation proof must fail if docs advertise deprecated proof paths as current.
1053. Deprecation proof must fail if anti-false-PASS attacks target only deprecated reports and miss current reports.
1054. Minimality proof must distinguish required proof artifacts from optional diagnostic artifacts.
1055. Minimality proof must fail if optional diagnostic artifacts are required for validators to pass.
1056. Minimality proof must fail if final proof bundle cannot be verified after optional diagnostics are removed.
1057. Minimality proof must fail if required artifacts are not named as required in the artifact index.
1058. Minimality proof must fail if optional artifacts contain conflicting verdicts.
1059. Minimality proof must fail if validators scan optional artifacts and accidentally use stale PASS evidence.
1060. Minimality proof must fail if final verdict changes when optional artifacts are removed.
1061. Minimality proof must fail if optional artifacts are generated before required artifacts and influence required verdicts.
1062. Minimality proof must fail if diagnostic summaries are more authoritative than structured proof files.
1063. Minimality proof must fail if proof bundle includes irrelevant files that make independent review ambiguous.
1064. Assurance-level proof must define the minimum evidence level required for each requirement: static check, unit test, integration test, scenario execution, replay, negative test, mutation test, or independent verifier.
1065. Assurance-level proof must fail if a high-assurance requirement is satisfied only by a lower-assurance evidence type.
1066. Assurance-level proof must fail if safety-critical requirements lack at least one positive and one negative behavioral test.
1067. Assurance-level proof must fail if anti-false-PASS requirements lack both report-corruption and code-mutation coverage.
1068. Assurance-level proof must fail if any requirement's evidence type is unspecified.
1069. Assurance-level proof must fail if evidence levels are downgraded after implementation without a recorded rationale.
1070. Assurance-level proof must fail if the final classification does not state which assurance level was achieved.
1071. Assurance-level proof must fail if a requirement marked safety-critical is excluded from final gating.
1072. Assurance-level proof must fail if a non-safety-critical label is used to avoid testing a safety-critical path.
1073. Assurance-level proof must fail if final proof claims are broader than the evidence level actually demonstrated.
1074. Invariant completeness proof must list all invariants required for safe evolution, not only no_self_promotion.
1075. Invariant completeness proof must fail if required invariants are discovered in source or docs but absent from invariant registry.
1076. Invariant completeness proof must fail if invariant registry entries have no corresponding tests.
1077. Invariant completeness proof must fail if an invariant has tests but is not wired into runtime enforcement.
1078. Invariant completeness proof must fail if an invariant is evaluated after the action it should have prevented.
1079. Invariant completeness proof must fail if invariant severity is missing or does not affect final decision.
1080. Invariant completeness proof must fail if an invariant result can be overridden by a later permissive policy decision without explicit precedence.
1081. Invariant completeness proof must fail if invariant failures are recorded but not propagated to promotion gate verdict.
1082. Invariant completeness proof must fail if invariant inputs omit any identity, artifact, policy, or action field used by the invariant.
1083. Invariant completeness proof must fail if invariant evidence cannot be traced to a specific scenario run.
1084. Decision precedence proof must define ordering and precedence among policy denial, capability denial, invariant failure, human approval, promotion gate, and circuit breaker.
1085. Decision precedence proof must fail if human approval can override a hard safety denial.
1086. Decision precedence proof must fail if policy allow can override invariant failure without explicit rule.
1087. Decision precedence proof must fail if capability allow can override circuit breaker trip.
1088. Decision precedence proof must fail if later PASS decisions erase earlier DENY decisions from state or event evidence.
1089. Decision precedence proof must fail if conflicting decisions resolve differently across runtime, replay, and validators.
1090. Decision precedence proof must fail if conflict-resolution rules are documented but not enforced in code.
1091. Decision precedence proof must fail if conflict-resolution rules are enforced but not recorded in proof evidence.
1092. Decision precedence proof must fail if final verdict lacks a decision trace explaining why the action was allowed or denied.
1093. Decision precedence proof must fail if the decision trace cannot be recomputed from recorded evidence.
1094. Causal-chain proof must link every final verdict to a causal chain from input action through policy, capability, invariant, review, promotion gate, artifact/state/event effects, and final report.
1095. Causal-chain proof must fail if any chain segment is missing, unordered, or references a different action_id.
1096. Causal-chain proof must fail if an artifact is promoted without a causal chain to approval and gate decision.
1097. Causal-chain proof must fail if an unsafe action is denied without a causal chain to the denial mechanism.
1098. Causal-chain proof must fail if scenario proof contains events that do not belong to the final action decision.
1099. Causal-chain proof must fail if report generators synthesize a causal chain not present in runtime state/events.
1100. Causal-chain proof must fail if replay causal chain differs from original execution causal chain.
1101. Causal-chain proof must fail if causal-chain hashes are absent from the proof bundle.
1102. Causal-chain proof must fail if causal-chain evidence is only prose.
1103. Causal-chain proof must fail if causal-chain validator does not independently recompute chain consistency.
1104. Property-based proof must include property tests for validators where fixed fixtures are insufficient.
1105. Property-based proof must fail if validators accept arbitrary malformed reports that are not covered by hand-written fixtures.
1106. Property-based proof must generate invalid combinations of verdicts, hashes, IDs, paths, counts, and statuses.
1107. Property-based proof must fail if generated invalid reports can produce validator exit code 0.
1108. Property-based proof must record random seeds for any generated cases.
1109. Property-based proof must fail if property tests are nondeterministic and seeds are not recorded.
1110. Property-based proof must fail if property tests shrink failing cases but final logs omit the minimal counterexample.
1111. Property-based proof must fail if property tests are skipped in the final proof target.
1112. Property-based proof must fail if property tests use broad assumptions that discard all generated cases.
1113. Property-based proof must fail if property tests exercise only schema shape and not semantic consistency.
1114. Fuzzing proof must define whether parser fuzzing is in scope for final JSON, markdown, transcript, manifest, and log parsers.
1115. Fuzzing proof must fail if proof-critical parsers crash on malformed input and the crash is not reported as validation failure.
1116. Fuzzing proof must fail if malformed inputs can be partially parsed into PASS reports.
1117. Fuzzing proof must fail if duplicate-key JSON, deeply nested JSON, oversized arrays, invalid UTF-8, and control characters are not rejected or handled deterministically.
1118. Fuzzing proof must fail if markdown parser edge cases can hide FAIL rows or inject PASS rows.
1119. Fuzzing proof must fail if transcript parser accepts truncated, concatenated, or reordered command entries.
1120. Fuzzing proof must fail if manifest parser accepts mixed schema versions without rejecting or migrating.
1121. Fuzzing proof must record corpus location, seed, parser targets, and failure results.
1122. Fuzzing proof must fail if fuzzing failures are reported only as warnings.
1123. Fuzzing proof must fail if fuzz corpus files are confused with final proof reports.
1124. Differential validation proof must compare at least two independent validation paths for final verdict-critical claims where practical.
1125. Differential validation proof must fail if report generator and validator share the same helper that can contain the same bug without an independent check.
1126. Differential validation proof must fail if acceptance verdict is validated only by the code that generated it.
1127. Differential validation proof must fail if replay proof is validated only by replay report fields rather than replay execution.
1128. Differential validation proof must fail if traceability totals are accepted without independent row recounting.
1129. Differential validation proof must fail if hash manifests are accepted without independently rehashing files.
1130. Differential validation proof must fail if command transcript summary is accepted without checking full log hashes.
1131. Differential validation proof must fail if anti-false-PASS audit is validated only by its own verdict field.
1132. Differential validation proof must fail if independent validators disagree and final verdict still claims PASS.
1133. Differential validation proof must record which independent checks validated each proof-critical report.
1134. Oracle proof must define the expected oracle for safe and unsafe scenario outcomes.
1135. Oracle proof must fail if scenario expected outcomes are generated from the implementation under test.
1136. Oracle proof must fail if expected safe or unsafe verdicts can be changed by editing generated reports.
1137. Oracle proof must fail if expected outcomes are embedded only in test names or prose.
1138. Oracle proof must fail if runtime result is compared against itself instead of an external expected outcome.
1139. Oracle proof must fail if replay expected outcome is copied from replay output instead of original oracle plus original run evidence.
1140. Oracle proof must fail if anti-false-PASS expected rejectors are inferred after validators run instead of declared before attacks.
1141. Oracle proof must fail if mutation-test expected failures are inferred from observed failures instead of declared mutation intent.
1142. Oracle proof must fail if oracle files are untracked or generated during final proof.
1143. Oracle proof must fail if oracle files are not hashed in the final proof bundle.
1144. Self-test proof must require every validator to have a self-test proving it fails on a minimal invalid input.
1145. Self-test proof must fail if any validator has no negative self-test.
1146. Self-test proof must fail if a validator's negative self-test does not assert nonzero exit code.
1147. Self-test proof must fail if a validator's positive self-test does not assert zero exit code on a complete valid fixture.
1148. Self-test proof must fail if validator self-tests use a different CLI path than the final proof target.
1149. Self-test proof must fail if validator self-tests bypass argument parsing.
1150. Self-test proof must fail if validator self-tests monkeypatch filesystem checks that are required in production validation.
1151. Self-test proof must fail if validator self-tests use temporary fixtures with weaker schema than real reports.
1152. Self-test proof must fail if validator self-tests are not included in the final test command.
1153. Self-test proof must fail if a validator can be deleted while self-test proof still passes.
1154. Semantic-drift proof must compare current implementation behavior with the stated Functional Runtime MVP semantics.
1155. Semantic-drift proof must fail if implementation changes the meaning of FUNCTIONAL_RUNTIME_MVP without updating requirements.
1156. Semantic-drift proof must fail if report field names remain the same but semantics change.
1157. Semantic-drift proof must fail if a verdict value such as PASS, DENIED, BLOCKED, or FIXED is redefined without migration.
1158. Semantic-drift proof must fail if scenario names remain the same but inputs or expected behaviors change.
1159. Semantic-drift proof must fail if docs, tests, validators, and reports define different meanings for the same status.
1160. Semantic-drift proof must fail if a lower classification is renamed to sound equivalent to FUNCTIONAL_RUNTIME_MVP.
1161. Semantic-drift proof must fail if compatibility language hides an unimplemented requirement.
1162. Semantic-drift proof must fail if requirement wording is weakened after implementation without traceable review.
1163. Semantic-drift proof must fail if final verdict uses marketing language rather than exact classification enum.
1164. Reviewability proof must ensure the final proof bundle can be audited without executing arbitrary code.
1165. Reviewability proof must fail if critical evidence is available only by running custom scripts.
1166. Reviewability proof must fail if critical evidence is stored only in binary formats without documented reader or hashes.
1167. Reviewability proof must fail if proof artifacts lack a README or index explaining verification order.
1168. Reviewability proof must fail if report names do not indicate their role in the proof.
1169. Reviewability proof must fail if artifact index does not identify source, generated, fixture, and diagnostic files.
1170. Reviewability proof must fail if proof evidence is split across unrelated directories without manifest links.
1171. Reviewability proof must fail if the final verdict cannot be checked by one documented detached verification command.
1172. Reviewability proof must fail if detached verification requires undocumented environment setup.
1173. Reviewability proof must fail if final proof claims cannot be traced from final verdict to evidence in under the documented artifact graph.
1174. Machine-readability proof must ensure every proof-critical markdown claim has an equivalent machine-readable JSON field.
1175. Machine-readability proof must fail if a markdown-only PASS claim affects final judgment.
1176. Machine-readability proof must fail if a markdown-only exception or caveat is absent from JSON.
1177. Machine-readability proof must fail if JSON contains PASS but markdown contains a caveat saying manual review is required.
1178. Machine-readability proof must fail if JSON omits explanatory fields needed to distinguish rejection reason from infrastructure error.
1179. Machine-readability proof must fail if anti-false-PASS markdown contains attack results not present in JSON.
1180. Machine-readability proof must fail if traceability markdown contains rows not present in JSON.
1181. Machine-readability proof must fail if proof bundle JSON omits evidence shown in markdown.
1182. Machine-readability proof must fail if final verdict JSON and final verdict markdown are generated by different sources.
1183. Machine-readability proof must fail if report consumers parse markdown tables as the authoritative data source.
1184. Changelog proof must record which corrective-list items were addressed by the current commit or proof run.
1185. Changelog proof must fail if changes to validators, proof generators, or runtime safety code are not represented in traceability.
1186. Changelog proof must fail if a corrective item is removed without rationale.
1187. Changelog proof must fail if a corrective item is split or merged without stable IDs.
1188. Changelog proof must fail if current proof claims to address a corrective item but no relevant file changed and no prior evidence is cited.
1189. Changelog proof must fail if a changed proof-critical file has no related test or validator evidence.
1190. Changelog proof must fail if generated reports are updated but source, tests, and validators are unchanged for a claimed fix.
1191. Changelog proof must fail if implementation changes are not reflected in source mutation and source manifest proof.
1192. Changelog proof must fail if final proof does not identify the prior classification and new classification.
1193. Changelog proof must fail if regression tests for prior false-PASS bugs are missing from the changelog evidence.
1194. Risk-register proof must track remaining risks after final proof.
1195. Risk-register proof must fail if remaining risks include an MVP blocker while final classification claims FUNCTIONAL_RUNTIME_MVP.
1196. Risk-register proof must fail if risks are listed only in prose and not machine-readable JSON.
1197. Risk-register proof must fail if a risk has no owner, status, severity, rationale, and mitigation evidence.
1198. Risk-register proof must fail if high-severity risks are marked accepted automatically.
1199. Risk-register proof must fail if accepted risks are not included in final proof hashes.
1200. Risk-register proof must fail if risk status conflicts with gap status.
1201. Risk-register proof must fail if risk mitigation claims are not linked to tests or validators.
1202. Risk-register proof must fail if risk severity is downgraded without rationale.
1203. Risk-register proof must fail if new anti-false-PASS findings are not added to the risk register when unresolved.
1204. Escalation proof must define what happens when proof cannot establish FUNCTIONAL_RUNTIME_MVP.
1205. Escalation proof must fail if failed proof runs still produce artifacts named final_pass or complete.
1206. Escalation proof must fail if non-MVP classifications do not include blocking reasons.
1207. Escalation proof must fail if blockers are not mapped to corrective-list items or requirements.
1208. Escalation proof must fail if a BLOCKED item lacks next-action guidance or rationale.
1209. Escalation proof must fail if a failed proof leaves stale PASS badges, docs, or final verdict files.
1210. Escalation proof must fail if proof failure returns exit code 0.
1211. Escalation proof must fail if downstream automation cannot distinguish failure classes.
1212. Escalation proof must fail if CI artifacts for failed proof are not retained for diagnosis.
1213. Escalation proof must fail if failed anti-false-PASS attacks are reported as partial success.
1214. Evidence freshness proof must define which evidence can be reused across commits and which evidence must be regenerated per commit.
1215. Evidence freshness proof must fail if runtime scenario evidence is reused after any runtime source file changes.
1216. Evidence freshness proof must fail if validator evidence is reused after any validator source file changes.
1217. Evidence freshness proof must fail if replay evidence is reused after replay executor source changes.
1218. Evidence freshness proof must fail if proof-bundle evidence is reused after proof collector source changes.
1219. Evidence freshness proof must fail if traceability evidence is reused after requirements, tests, validators, or source refs change.
1220. Evidence freshness proof must fail if compatibility evidence is reused after Makefile, dependencies, OS, shell, or Python version changes.
1221. Evidence freshness proof must fail if evidence lacks generated_at, source_hashes, command_hash, and proof_run_id.
1222. Evidence freshness proof must fail if evidence generated outside the current proof_run_id is not explicitly marked reusable and independently verified.
1223. Evidence freshness proof must fail if stale evidence is copied into the final report directory during proof generation.
1224. Evidence ownership proof must identify which proof step owns each generated file.
1225. Evidence ownership proof must fail if two proof steps write the same output file without explicit handoff rules.
1226. Evidence ownership proof must fail if a validator writes evidence files that later validators treat as runtime evidence.
1227. Evidence ownership proof must fail if report generators and validators both claim ownership of the same artifact.
1228. Evidence ownership proof must fail if ownership metadata is missing for any final artifact.
1229. Evidence ownership proof must fail if an artifact owner differs across artifact index, proof bundle, and report metadata.
1230. Evidence ownership proof must fail if generated files are modified by a step that is not their declared owner.
1231. Evidence ownership proof must fail if source files are owned by proof-generation steps.
1232. Evidence ownership proof must fail if runtime scenario code owns proof reports.
1233. Evidence ownership proof must fail if detached verification mutates files it does not own.
1234. Independent-review proof must include a reviewer-facing checklist generated from machine-readable proof results.
1235. Independent-review proof must fail if the checklist includes PASS claims not supported by JSON evidence.
1236. Independent-review proof must fail if the checklist omits blockers, warnings, waivers, out-of-scope items, or unresolved risks.
1237. Independent-review proof must fail if the checklist cannot be regenerated deterministically from final reports.
1238. Independent-review proof must fail if manual-review fields can change final verdict without a validator.
1239. Independent-review proof must fail if reviewer notes are stored only in transient logs.
1240. Independent-review proof must fail if reviewer approval is required but no reviewer identity is recorded.
1241. Independent-review proof must fail if reviewer identity is free text without stable ID.
1242. Independent-review proof must fail if reviewer comments can override validator failures.
1243. Independent-review proof must fail if independent review is claimed but no independent verification command was executed.
1244. Machine-actionability proof must ensure downstream automation can consume final verdict without parsing prose.
1245. Machine-actionability proof must fail if final verdict is available only in markdown.
1246. Machine-actionability proof must fail if classification, verdict, commit, proof_run_id, and blocking reasons are not exposed in a single JSON file.
1247. Machine-actionability proof must fail if downstream status cannot distinguish PASS, FAIL, BLOCKED, PARTIAL, UNKNOWN, and OUT_OF_SCOPE.
1248. Machine-actionability proof must fail if final JSON omits links to artifact index and detached verification result.
1249. Machine-actionability proof must fail if command-line exit code and final verdict JSON disagree.
1250. Machine-actionability proof must fail if CI status and final verdict JSON disagree.
1251. Machine-actionability proof must fail if automation can mark success from absence of errors rather than explicit PASS verdict.
1252. Machine-actionability proof must fail if machine-readable blockers are collapsed into human-readable summary only.
1253. Machine-actionability proof must fail if required fields have inconsistent names across final verdict versions.
1254. Evidence prioritization proof must define precedence when multiple evidence sources conflict.
1255. Evidence prioritization proof must fail if newer failing evidence is ignored in favor of older passing evidence.
1256. Evidence prioritization proof must fail if runtime evidence conflicts with report evidence and final verdict remains PASS.
1257. Evidence prioritization proof must fail if validator evidence conflicts with acceptance matrix and final verdict remains PASS.
1258. Evidence prioritization proof must fail if replay evidence conflicts with original scenario evidence and final verdict remains PASS.
1259. Evidence prioritization proof must fail if CI evidence conflicts with local evidence and final verdict does not state which evidence is authoritative.
1260. Evidence prioritization proof must fail if markdown evidence conflicts with JSON and JSON is not treated as authoritative.
1261. Evidence prioritization proof must fail if duplicate evidence items have different verdicts without conflict resolution.
1262. Evidence prioritization proof must fail if final verdict is computed before conflicts are reconciled.
1263. Evidence prioritization proof must fail if conflict resolution is manual and unvalidated.
1264. Semantic-validation proof must check that evidence content proves the claimed requirement, not only that an evidence file exists.
1265. Semantic-validation proof must fail if evidence_refs point to unrelated files.
1266. Semantic-validation proof must fail if evidence_refs point to tests that do not assert the requirement being claimed.
1267. Semantic-validation proof must fail if evidence_refs point to validators that do not inspect the relevant fields.
1268. Semantic-validation proof must fail if evidence_refs point to source files but no executed test or static check covers the relevant code.
1269. Semantic-validation proof must fail if evidence text contains the requirement ID but does not demonstrate the behavior.
1270. Semantic-validation proof must fail if broad proof files are reused as evidence for every requirement without row-level mapping.
1271. Semantic-validation proof must fail if a single PASS verdict is used as evidence for subrequirements not independently checked.
1272. Semantic-validation proof must fail if requirement evidence is based only on filename conventions.
1273. Semantic-validation proof must fail if requirement evidence points to generated output created by the requirement row itself.
1274. Runtime-vs-proof separation must ensure runtime scenario code cannot read final proof reports before producing behavior.
1275. Runtime-vs-proof separation must fail if runtime behavior changes based on existing generated reports.
1276. Runtime-vs-proof separation must fail if scenario code checks whether it is being validated and returns PASS behavior only then.
1277. Runtime-vs-proof separation must fail if runtime code imports validator modules.
1278. Runtime-vs-proof separation must fail if runtime code imports proof generator modules.
1279. Runtime-vs-proof separation must fail if runtime code writes acceptance, traceability, anti-false-PASS, or final verdict reports.
1280. Runtime-vs-proof separation must fail if proof code modifies runtime state before scenario execution.
1281. Runtime-vs-proof separation must fail if proof code pre-creates artifacts that runtime scenario should create.
1282. Runtime-vs-proof separation must fail if runtime and proof outputs share filenames that allow one to overwrite the other.
1283. Runtime-vs-proof separation must fail if replay execution reads final verdict as input.
1284. Scenario isolation proof must ensure each scenario has its own state store, artifact store, event log, and report export directory.
1285. Scenario isolation proof must fail if safe and unsafe scenarios share mutable stores without explicit reset and evidence.
1286. Scenario isolation proof must fail if scenario order affects final verdict.
1287. Scenario isolation proof must fail if running only the unsafe scenario differs from running it after the safe scenario without explanation.
1288. Scenario isolation proof must fail if running only the safe scenario differs from running it after the unsafe scenario without explanation.
1289. Scenario isolation proof must fail if scenario IDs are reused across different scenario definitions.
1290. Scenario isolation proof must fail if scenario evidence directories collide.
1291. Scenario isolation proof must fail if scenario cleanup deletes another scenario's evidence.
1292. Scenario isolation proof must fail if scenario export can include files from another scenario.
1293. Scenario isolation proof must fail if scenario proof lacks scenario definition hash.
1294. Scenario-definition proof must store each scenario input definition as tracked source or generated deterministically from tracked source.
1295. Scenario-definition proof must fail if scenario definitions are generated from runtime results.
1296. Scenario-definition proof must fail if scenario expected outcomes are stored only in test code comments.
1297. Scenario-definition proof must fail if scenario inputs can be mutated during execution without preserving original input hash.
1298. Scenario-definition proof must fail if scenario labels do not match expected behavior.
1299. Scenario-definition proof must fail if two scenarios have identical inputs but different expected outcomes without rationale.
1300. Scenario-definition proof must fail if a required scenario is absent from the canonical scenario list.
1301. Scenario-definition proof must fail if scenario list changes after traceability generation.
1302. Scenario-definition proof must fail if scenario hashes are not included in replay manifests.
1303. Scenario-definition proof must fail if replay uses a different scenario definition hash than original execution.
1304. Evidence-query proof must support answering which evidence proves a specific requirement, scenario, attack, validator, or artifact.
1305. Evidence-query proof must fail if evidence cannot be queried by requirement ID.
1306. Evidence-query proof must fail if evidence cannot be queried by corrective-list item ID.
1307. Evidence-query proof must fail if evidence cannot be queried by attack ID.
1308. Evidence-query proof must fail if evidence cannot be queried by scenario ID.
1309. Evidence-query proof must fail if evidence cannot be queried by validator ID.
1310. Evidence-query proof must fail if query results include stale or unrelated evidence.
1311. Evidence-query proof must fail if query results differ from final traceability matrix.
1312. Evidence-query proof must fail if query results omit required hashes and file paths.
1313. Evidence-query proof must fail if query command mutates evidence or reports.
1314. Evidence-query proof must be included in detached verification or marked as diagnostic only.
1315. Validator-composition proof must define how multiple validators combine into a final decision.
1316. Validator-composition proof must fail if one passing validator can override another failing validator.
1317. Validator-composition proof must fail if validator order changes final verdict.
1318. Validator-composition proof must fail if optional validators are counted as required validators in PASS totals.
1319. Validator-composition proof must fail if required validators can be disabled by configuration.
1320. Validator-composition proof must fail if a validator is counted as run when it only appears in a list.
1321. Validator-composition proof must fail if validator subprocess failure is reported as validator PASS.
1322. Validator-composition proof must fail if validator warnings are ignored for proof-critical checks.
1323. Validator-composition proof must fail if validators are run on different report directories within the same final validation pass.
1324. Validator-composition proof must fail if validator result aggregation has no schema.
1325. Report-lifecycle proof must define states for draft, generated, validated, finalized, archived, and historical reports.
1326. Report-lifecycle proof must fail if a draft report is used as final evidence.
1327. Report-lifecycle proof must fail if an unvalidated report is included in final proof bundle.
1328. Report-lifecycle proof must fail if a finalized report is modified without returning to generated state and rerunning validation.
1329. Report-lifecycle proof must fail if archived reports are included in current verdict.
1330. Report-lifecycle proof must fail if historical reports are indistinguishable from current reports.
1331. Report-lifecycle proof must fail if report state is stored only in filename and not in metadata.
1332. Report-lifecycle proof must fail if report lifecycle state conflicts across artifact index, report metadata, and final verdict.
1333. Report-lifecycle proof must fail if final verdict references any report not in finalized state.
1334. Report-lifecycle proof must fail if lifecycle transitions are not recorded in audit log.
1335. Proof-scope proof must state exactly what FUNCTIONAL_RUNTIME_MVP includes and excludes.
1336. Proof-scope proof must fail if final proof claims broad production readiness beyond the MVP scope.
1337. Proof-scope proof must fail if excluded features are accidentally tested as passing features.
1338. Proof-scope proof must fail if included MVP features are marked future-work.
1339. Proof-scope proof must fail if scope exclusions conflict with mandatory requirements.
1340. Proof-scope proof must fail if known non-MVP gaps are mixed with MVP blockers.
1341. Proof-scope proof must fail if scope is defined only in prose and not represented in machine-readable final verdict.
1342. Proof-scope proof must fail if scope changes after acceptance criteria were generated.
1343. Proof-scope proof must fail if scope differs between README, requirements, traceability, and final verdict.
1344. Proof-scope proof must fail if scope is ambiguous enough to classify the same item as PASS or OUT_OF_SCOPE.
1345. SLO proof must define whether performance, latency, memory, or scalability are in scope for the MVP.
1346. SLO proof must fail if performance is out of scope but performance claims are made in docs or final verdict.
1347. SLO proof must fail if performance is in scope but no measured evidence is captured.
1348. SLO proof must fail if measured performance lacks environment metadata.
1349. SLO proof must fail if benchmarks run with different code than final proof scenarios.
1350. SLO proof must fail if benchmark failures are ignored because functional tests pass.
1351. SLO proof must fail if resource leaks are visible across repeated proof runs and not classified.
1352. SLO proof must fail if proof commands produce unbounded logs or artifacts.
1353. SLO proof must fail if timeout settings are longer than documented acceptable bounds without rationale.
1354. SLO proof must fail if memory or disk limits differ between local and CI proof without being recorded.
1355. Formal-claim proof must state each final claim as a machine-readable claim with subject, predicate, object, scope, and evidence.
1356. Formal-claim proof must fail if a final report contains claims that are not represented in the claim index.
1357. Formal-claim proof must fail if a claim has no evidence chain to executed commands or trusted source.
1358. Formal-claim proof must fail if claim wording is broader than its evidence scope.
1359. Formal-claim proof must fail if two final claims contradict each other.
1360. Formal-claim proof must fail if a claim depends on an OUT_OF_SCOPE item.
1361. Formal-claim proof must fail if claims use vague terms such as complete, done, robust, production-ready, or secure without mapped criteria.
1362. Formal-claim proof must fail if human-readable claims in markdown are not included in machine-readable claim validation.
1363. Formal-claim proof must fail if final classification is implied by prose but absent from the structured final verdict.
1364. Formal-claim proof must fail if any claim is accepted because no counterevidence was found rather than because positive evidence exists.
1365. Counterevidence proof must collect and classify evidence that contradicts PASS claims.
1366. Counterevidence proof must fail if known failing tests, validator failures, audit failures, or stale reports are excluded from final decision.
1367. Counterevidence proof must fail if counterevidence is present but not linked to the affected requirement or corrective item.
1368. Counterevidence proof must fail if a final PASS claim does not explicitly state that no blocking counterevidence was found.
1369. Counterevidence proof must fail if counterevidence from previous failed attempts in the same proof_run_id is hidden.
1370. Counterevidence proof must fail if counterevidence is downgraded to informational without rationale.
1371. Counterevidence proof must fail if final verdict generation does not inspect counterevidence reports.
1372. Counterevidence proof must fail if anti-false-PASS surviving attacks are not treated as counterevidence.
1373. Counterevidence proof must fail if mutation survivors are not treated as counterevidence.
1374. Counterevidence proof must fail if unresolved risks are not treated as counterevidence for affected claims.
1375. Evidence-graph reachability proof must verify that every evidence node used by final verdict is reachable from a trusted root.
1376. Evidence-graph reachability proof must fail if any required evidence node is isolated.
1377. Evidence-graph reachability proof must fail if any final PASS node has no path to source, command, test, validator, or runtime evidence.
1378. Evidence-graph reachability proof must fail if graph traversal ignores failed nodes.
1379. Evidence-graph reachability proof must fail if graph cycles hide missing roots.
1380. Evidence-graph reachability proof must fail if the graph contains dangling references.
1381. Evidence-graph reachability proof must fail if the graph contains references to artifacts outside the artifact index.
1382. Evidence-graph reachability proof must fail if graph nodes use non-unique IDs.
1383. Evidence-graph reachability proof must fail if graph edges lack typed relationship labels.
1384. Evidence-graph reachability proof must fail if final verdict is not the sink node of the evidence graph.
1385. Proof-query API must provide machine-readable queries for claim by ID, requirement by ID, corrective item by ID, attack by ID, scenario by ID, validator by ID, artifact by ID, and evidence by hash.
1386. Proof-query API must fail if a query returns multiple current artifacts for an ID that should be unique.
1387. Proof-query API must fail if a query returns historical artifacts as current artifacts.
1388. Proof-query API must fail if a query omits blocking counterevidence.
1389. Proof-query API must fail if a query result cannot be validated by detached verification.
1390. Proof-query API must fail if a query mutates reports, caches stale answers, or generates new evidence.
1391. Proof-query API must fail if query output is markdown-only.
1392. Proof-query API must fail if query output lacks proof_run_id and git_commit.
1393. Proof-query API must fail if query output includes files absent from artifact index.
1394. Proof-query API must fail if query output disagrees with final evidence graph.
1395. Runtime-entrypoint proof must identify the production-like entrypoints exercised by scenario tests.
1396. Runtime-entrypoint proof must fail if scenario tests call private helper functions while bypassing public runtime entrypoints.
1397. Runtime-entrypoint proof must fail if CLI entrypoints and Python API entrypoints produce different decisions for the same action.
1398. Runtime-entrypoint proof must fail if proof scenarios use a special test-only entrypoint that skips policy, capability, invariant, review, or promotion logic.
1399. Runtime-entrypoint proof must fail if replay uses a different decision engine than original execution.
1400. Runtime-entrypoint proof must fail if safe and unsafe scenarios exercise different entrypoint stacks without rationale.
1401. Runtime-entrypoint proof must fail if entrypoint behavior changes when called from tests versus Makefile.
1402. Runtime-entrypoint proof must fail if runtime entrypoint is only smoke-tested and not semantically asserted.
1403. Runtime-entrypoint proof must fail if entrypoint arguments are not recorded in scenario evidence.
1404. Runtime-entrypoint proof must fail if entrypoint version or source hash is absent from proof metadata.
1405. Side-effect proof must enumerate all permitted side effects for safe scenario, unsafe scenario, replay, validation, and report generation.
1406. Side-effect proof must fail if any unlisted side effect occurs.
1407. Side-effect proof must fail if unsafe scenario creates active artifacts, modifies source, or mutates policy configuration.
1408. Side-effect proof must fail if replay produces side effects outside its isolated replay output directory.
1409. Side-effect proof must fail if validators create or alter runtime evidence.
1410. Side-effect proof must fail if report generators alter runtime state.
1411. Side-effect proof must fail if scenario execution changes files not represented in source mutation or artifact lifecycle reports.
1412. Side-effect proof must fail if side-effect tracking relies only on git status and ignores untracked or ignored files.
1413. Side-effect proof must fail if side-effect allowlist uses broad directory globs without file-level validation.
1414. Side-effect proof must fail if side-effect reports omit before/after hashes.
1415. File-watch proof must snapshot allowed roots before and after proof-critical phases.
1416. File-watch proof must fail if new files appear outside declared output roots.
1417. File-watch proof must fail if files disappear from declared evidence roots before final validation.
1418. File-watch proof must fail if file modifications occur after their owning phase has ended.
1419. File-watch proof must fail if file watcher excludes hidden files that can affect proof behavior.
1420. File-watch proof must fail if file watcher follows symlinks outside allowed roots.
1421. File-watch proof must fail if file watcher ignores permission, type, or link-count changes where they affect evidence.
1422. File-watch proof must fail if file watcher has no baseline for comparison.
1423. File-watch proof must fail if file watcher results are not included in final proof hashes.
1424. File-watch proof must fail if file watcher detects changes but final verdict remains PASS without rationale.
1425. State-reconstruction proof must reconstruct final state from event log and compare it to persisted state records.
1426. State-reconstruction proof must fail if reconstructed state differs from persisted state.
1427. State-reconstruction proof must fail if final verdict can be derived from persisted state but not from event log.
1428. State-reconstruction proof must fail if final verdict can be derived from event log but not from persisted state.
1429. State-reconstruction proof must fail if reconstruction requires hidden in-memory values.
1430. State-reconstruction proof must fail if reconstruction order is ambiguous.
1431. State-reconstruction proof must fail if reconstruction ignores denied, rolled-back, or failed events.
1432. State-reconstruction proof must fail if reconstruction logic is the same unchecked generator that wrote persisted state.
1433. State-reconstruction proof must fail if reconstruction is not run in final proof target.
1434. State-reconstruction proof must fail if reconstruction output is not hashed and linked in proof bundle.
1435. Intent-preservation proof must verify that each corrective-list item preserves its original intent after being converted into requirements.
1436. Intent-preservation proof must fail if a corrective item is weakened during decomposition.
1437. Intent-preservation proof must fail if a corrective item is split and one split part is untracked.
1438. Intent-preservation proof must fail if a corrective item is implemented only for one report type when the intent applies across reports.
1439. Intent-preservation proof must fail if a corrective item is satisfied by wording changes but no behavioral or validator change.
1440. Intent-preservation proof must fail if a corrective item is declared duplicate but duplicate coverage is narrower.
1441. Intent-preservation proof must fail if a corrective item is moved to documentation-only without rationale.
1442. Intent-preservation proof must fail if a corrective item targeting a loophole lacks a regression test for that loophole.
1443. Intent-preservation proof must fail if a corrective item targeting proof integrity is not included in final gating.
1444. Intent-preservation proof must fail if final acceptance ignores the corrective-list coverage report.
1445. Final-gate proof must define a single Boolean gate for FUNCTIONAL_RUNTIME_MVP.
1446. Final-gate proof must fail if multiple independent gates can disagree and no one gate is authoritative.
1447. Final-gate proof must fail if final gate reads only summary verdicts instead of validating underlying evidence hashes.
1448. Final-gate proof must fail if final gate omits any required report family.
1449. Final-gate proof must fail if final gate does not inspect detached verification output.
1450. Final-gate proof must fail if final gate can be bypassed by directly editing final verdict JSON.
1451. Final-gate proof must fail if final gate can be bypassed by running a legacy make target.
1452. Final-gate proof must fail if final gate can pass while artifact index, proof bundle, or traceability validator fails.
1453. Final-gate proof must fail if final gate has no negative test where a required validator fails.
1454. Final-gate proof must fail if final gate has no negative test where a required report is missing.
1455. Legacy-artifact proof must find and classify older report files that may still exist in the repository.
1456. Legacy-artifact proof must fail if old reports are discoverable under default report paths and can be mistaken for current reports.
1457. Legacy-artifact proof must fail if old proof bundles have the same filename as current proof bundles without proof_run_id isolation.
1458. Legacy-artifact proof must fail if legacy validators accept current reports without schema compatibility validation.
1459. Legacy-artifact proof must fail if current validators accept legacy reports silently.
1460. Legacy-artifact proof must fail if legacy make targets remain documented as proof targets.
1461. Legacy-artifact proof must fail if legacy PASS badges are not marked historical.
1462. Legacy-artifact proof must fail if cleanup does not remove or quarantine legacy artifacts before current proof generation.
1463. Legacy-artifact proof must fail if detached verification accepts legacy artifact layout as current layout.
1464. Legacy-artifact proof must fail if final artifact index contains legacy artifacts without historical status.
1465. Report-consumer proof must test every consumer of proof reports, including validators, final gate, detached verifier, query command, markdown generator, and CI artifact uploader.
1466. Report-consumer proof must fail if any consumer trusts a different source of truth for the same field.
1467. Report-consumer proof must fail if consumers disagree on current report directory.
1468. Report-consumer proof must fail if consumers disagree on schema validation strictness.
1469. Report-consumer proof must fail if consumers disagree on path normalization.
1470. Report-consumer proof must fail if consumers disagree on proof_run_id filtering.
1471. Report-consumer proof must fail if consumers disagree on whether markdown or JSON is authoritative.
1472. Report-consumer proof must fail if consumers disagree on handling warnings, waivers, blocked items, or unknown statuses.
1473. Report-consumer proof must fail if consumers disagree on stale evidence handling.
1474. Report-consumer proof must fail if consumers disagree on required report list.
1475. Report-producer proof must test every producer of proof reports, including scenario exporter, replay executor, proof collector, traceability generator, compatibility generator, gap scanner, anti-false-PASS auditor, markdown generator, and final verdict generator.
1476. Report-producer proof must fail if any producer writes reports without schema validation.
1477. Report-producer proof must fail if any producer writes reports without proof_run_id.
1478. Report-producer proof must fail if any producer writes reports without git_commit.
1479. Report-producer proof must fail if any producer writes reports without generator hash.
1480. Report-producer proof must fail if any producer writes reports without input hashes.
1481. Report-producer proof must fail if any producer writes reports after finalization.
1482. Report-producer proof must fail if any producer accepts missing inputs as empty successful outputs.
1483. Report-producer proof must fail if any producer has no negative test for missing required inputs.
1484. Report-producer proof must fail if any producer has no test proving deterministic output.
1485. Runtime-error proof must exercise expected runtime error paths and verify they produce safe states.
1486. Runtime-error proof must fail if a runtime exception leaves partial active artifacts.
1487. Runtime-error proof must fail if runtime exception bypasses event logging.
1488. Runtime-error proof must fail if runtime exception bypasses state persistence.
1489. Runtime-error proof must fail if runtime exception bypasses circuit breaker where safety-critical.
1490. Runtime-error proof must fail if runtime exception is converted into successful scenario verdict.
1491. Runtime-error proof must fail if runtime exception is only visible in stderr and not represented in structured scenario evidence.
1492. Runtime-error proof must fail if runtime error recovery deletes evidence needed for audit.
1493. Runtime-error proof must fail if retry after runtime error creates duplicate state or event records without idempotency handling.
1494. Runtime-error proof must fail if safe scenario failure is classified as unsafe denial success.
1495. Invalid-proof proof must include deliberately invalid complete proof bundles and verify detached verification rejects them.
1496. Invalid-proof proof must fail if detached verification accepts a bundle missing proof bundle JSON.
1497. Invalid-proof proof must fail if detached verification accepts a bundle missing final verdict JSON.
1498. Invalid-proof proof must fail if detached verification accepts a bundle missing artifact index.
1499. Invalid-proof proof must fail if detached verification accepts a bundle with stale git_commit.
1500. Invalid-proof proof must fail if detached verification accepts a bundle with mixed proof_run_id.
1501. Invalid-proof proof must fail if detached verification accepts a bundle with altered evidence hashes.
1502. Invalid-proof proof must fail if detached verification accepts a bundle with markdown PASS and JSON FAIL.
1503. Invalid-proof proof must fail if detached verification accepts a bundle where a required report is replaced by an empty valid JSON object.
1504. Invalid-proof proof must fail if invalid-proof fixtures are not included in anti-false-PASS coverage.
1505. Proof bootstrap proof must define how the first valid proof set is created without relying on a previously trusted proof set.
1506. Proof bootstrap proof must fail if initial proof artifacts are seeded from hand-written PASS reports.
1507. Proof bootstrap proof must fail if bootstrapping mode remains available after final proof generation.
1508. Proof bootstrap proof must fail if bootstrap exceptions are not removed or disabled before FUNCTIONAL_RUNTIME_MVP is claimed.
1509. Proof bootstrap proof must fail if bootstrap-generated reports are indistinguishable from fully validated reports.
1510. Proof bootstrap proof must fail if bootstrap rows can satisfy final acceptance without second-pass validation.
1511. Proof bootstrap proof must fail if bootstrap logic is reachable from detached verification.
1512. Proof bootstrap proof must fail if bootstrap validators trust incomplete reports that final validators should reject.
1513. Proof bootstrap proof must fail if bootstrap proof_run_id is reused as final proof_run_id.
1514. Proof bootstrap proof must fail if bootstrap artifacts remain in the final artifact index as current evidence.
1515. Evidence-liveness proof must verify that every referenced evidence file exists at final detached verification time.
1516. Evidence-liveness proof must fail if evidence exists during generation but is deleted before final verification.
1517. Evidence-liveness proof must fail if evidence is generated lazily only when a reviewer opens a report.
1518. Evidence-liveness proof must fail if evidence paths require a running process, open file descriptor, or temporary server.
1519. Evidence-liveness proof must fail if evidence depends on an in-memory cache not serialized into the proof bundle.
1520. Evidence-liveness proof must fail if evidence depends on external database rows not exported and hashed.
1521. Evidence-liveness proof must fail if evidence depends on environment-specific absolute symlink targets.
1522. Evidence-liveness proof must fail if evidence depends on untracked temporary logs.
1523. Evidence-liveness proof must fail if evidence has zero-byte content where nonempty content is required.
1524. Evidence-liveness proof must fail if evidence file size changes between generation and detached verification.
1525. Assumption register proof must list every assumption used to classify gaps, scope, compatibility, replay validity, and evidence reuse.
1526. Assumption register proof must fail if an assumption affects PASS/FAIL classification but has no validator or test.
1527. Assumption register proof must fail if assumptions are stored only in prose.
1528. Assumption register proof must fail if an assumption is contradicted by runtime evidence.
1529. Assumption register proof must fail if assumptions change after proof generation without regenerating affected reports.
1530. Assumption register proof must fail if assumptions are broader than the actual tested environment.
1531. Assumption register proof must fail if assumptions hide missing features as out-of-scope.
1532. Assumption register proof must fail if an assumption expires or is marked temporary while final classification remains FUNCTIONAL_RUNTIME_MVP.
1533. Assumption register proof must fail if final verdict omits assumption IDs that materially affect classification.
1534. Assumption register proof must fail if independent verification cannot see which assumptions were applied.
1535. Claim-downgrade proof must ensure failed or incomplete claims downgrade classification automatically.
1536. Claim-downgrade proof must fail if the system can keep FUNCTIONAL_RUNTIME_MVP after a mandatory claim fails.
1537. Claim-downgrade proof must fail if a failed safety claim downgrades only to WARNING.
1538. Claim-downgrade proof must fail if downgrade rules are implemented only in documentation.
1539. Claim-downgrade proof must fail if downgrade rules are not covered by negative tests.
1540. Claim-downgrade proof must fail if downgrade reasons are omitted from final verdict.
1541. Claim-downgrade proof must fail if multiple minor failures can accumulate without blocking classification.
1542. Claim-downgrade proof must fail if a missing required report downgrades to PARTIAL but final PASS remains possible.
1543. Claim-downgrade proof must fail if downgrade logic differs between local proof, CI proof, and detached verification.
1544. Claim-downgrade proof must fail if manual waivers can bypass downgrade rules for mandatory claims.
1545. Evidence-strength proof must rate evidence as direct, indirect, inferred, or documentary.
1546. Evidence-strength proof must fail if direct behavioral evidence is required but only indirect or documentary evidence exists.
1547. Evidence-strength proof must fail if inferred evidence is used for proof-critical PASS without explicit inference rule.
1548. Evidence-strength proof must fail if a validator accepts evidence strength labels without checking actual evidence type.
1549. Evidence-strength proof must fail if evidence strength is not included in traceability rows.
1550. Evidence-strength proof must fail if generated evidence strength is caller-supplied rather than computed.
1551. Evidence-strength proof must fail if a single weak evidence source satisfies multiple strong-evidence requirements.
1552. Evidence-strength proof must fail if evidence strength conflicts across traceability, proof bundle, and final verdict.
1553. Evidence-strength proof must fail if evidence strength is downgraded silently after a failed test.
1554. Evidence-strength proof must fail if out-of-date strong evidence is preferred over current weak or failing evidence.
1555. Proof-budget proof must define which expensive checks may be shortened and which may not.
1556. Proof-budget proof must fail if anti-false-PASS attacks are reduced to fit runtime budget without marking proof incomplete.
1557. Proof-budget proof must fail if replay is sampled instead of complete where complete replay is required.
1558. Proof-budget proof must fail if validators inspect only a subset of reports without sampling policy.
1559. Proof-budget proof must fail if sampling is nondeterministic and seed is not recorded.
1560. Proof-budget proof must fail if sample misses are counted as PASS.
1561. Proof-budget proof must fail if performance shortcuts alter safety behavior.
1562. Proof-budget proof must fail if budget limits cause proof-critical logs to be omitted.
1563. Proof-budget proof must fail if budget mode differs between local and CI proof without final verdict recording.
1564. Proof-budget proof must fail if timeout-based skipping is classified as success.
1565. Red-team proof must include intentionally adversarial proof bundles beyond the standard anti-false-PASS audit.
1566. Red-team proof must fail if adversarial bundles are not run through detached verification.
1567. Red-team proof must fail if adversarial bundles target only old report schemas.
1568. Red-team proof must fail if adversarial bundles are generated but their expected rejection reasons are not declared.
1569. Red-team proof must fail if any adversarial bundle is accepted by detached verification.
1570. Red-team proof must fail if accepted adversarial bundles are marked known issue but final MVP remains PASS.
1571. Red-team proof must include adversarial cases for path confusion, mixed proof_run_id, stale commit, missing evidence, forged transcript, forged replay, forced PASS, and duplicate IDs.
1572. Red-team proof must include adversarial cases where all top-level verdicts say PASS but deep evidence is inconsistent.
1573. Red-team proof must include adversarial cases where evidence hashes are correct but point to wrong semantic evidence.
1574. Red-team proof must include adversarial cases where valid JSON schema hides invalid semantics.
1575. Metamorphic proof must define behavior-preserving transformations that should not change proof verdict.
1576. Metamorphic proof must fail if reordering independent rows changes verdict.
1577. Metamorphic proof must fail if changing irrelevant whitespace changes semantic verdict.
1578. Metamorphic proof must fail if moving report directory to another absolute path changes semantic verdict.
1579. Metamorphic proof must fail if changing proof_run_id consistently across all reports changes anything except hashes and run metadata.
1580. Metamorphic proof must fail if adding unrelated diagnostic files changes final verdict.
1581. Metamorphic proof must fail if removing optional diagnostics changes final verdict.
1582. Metamorphic proof must fail if canonical JSON reformatting changes validator outcomes.
1583. Metamorphic proof must fail if markdown regeneration from the same JSON changes machine verdict.
1584. Metamorphic proof must fail if equivalent relative and resolved paths are handled inconsistently after normalization.
1585. Equivalence proof must define when two evidence references are considered the same evidence.
1586. Equivalence proof must fail if duplicate evidence refs inflate coverage.
1587. Equivalence proof must fail if two refs to the same file under different paths are counted as independent evidence.
1588. Equivalence proof must fail if one test result is counted multiple times through different reports.
1589. Equivalence proof must fail if one validator result is counted multiple times under different names.
1590. Equivalence proof must fail if one scenario execution is counted as both original and replay evidence.
1591. Equivalence proof must fail if one command transcript entry satisfies multiple required commands with different semantics.
1592. Equivalence proof must fail if evidence aliases hide missing independent proof.
1593. Equivalence proof must fail if hardlinks, symlinks, or copied files are treated as independent without content and provenance checks.
1594. Equivalence proof must fail if equivalent evidence is not deduplicated before metrics are computed.
1595. Temporal-consistency proof must validate that evidence chronology matches proof dependency order.
1596. Temporal-consistency proof must fail if validation timestamp precedes report generation timestamp.
1597. Temporal-consistency proof must fail if proof bundle timestamp precedes any report it hashes.
1598. Temporal-consistency proof must fail if final verdict timestamp precedes final all-in-one validation timestamp.
1599. Temporal-consistency proof must fail if detached verification timestamp precedes final verdict generation.
1600. Temporal-consistency proof must fail if scenario evidence timestamp is after replay evidence that depends on it.
1601. Temporal-consistency proof must fail if approval timestamp is after promotion timestamp.
1602. Temporal-consistency proof must fail if denial event timestamp is after final denied verdict without an update event.
1603. Temporal-consistency proof must fail if timestamps are missing sequence numbers where clock precision cannot establish order.
1604. Temporal-consistency proof must fail if timezone normalization changes chronological ordering.
1605. Evidence-normalization proof must define how paths, IDs, timestamps, hashes, verdicts, and enum values are normalized.
1606. Evidence-normalization proof must fail if validators normalize values differently than generators.
1607. Evidence-normalization proof must fail if normalization hides invalid input.
1608. Evidence-normalization proof must fail if normalization collapses two distinct IDs into one without duplicate detection.
1609. Evidence-normalization proof must fail if normalization strips characters that are semantically relevant.
1610. Evidence-normalization proof must fail if normalization changes evidence hashes after they are recorded.
1611. Evidence-normalization proof must fail if normalized and raw values are both needed but only one is stored.
1612. Evidence-normalization proof must fail if normalization rules are not versioned.
1613. Evidence-normalization proof must fail if normalization differs across platforms.
1614. Evidence-normalization proof must fail if normalization is applied before malicious input detection and hides the attack.
1615. Schema-roundtrip proof must parse and re-emit every final JSON report and verify semantic equivalence.
1616. Schema-roundtrip proof must fail if required fields are lost during roundtrip.
1617. Schema-roundtrip proof must fail if unknown proof-critical fields are dropped silently.
1618. Schema-roundtrip proof must fail if enum values change casing during roundtrip.
1619. Schema-roundtrip proof must fail if numeric counts change type during roundtrip.
1620. Schema-roundtrip proof must fail if path strings are normalized during roundtrip without explicit rules.
1621. Schema-roundtrip proof must fail if duplicate JSON keys survive first parse but are lost on re-emit without rejection.
1622. Schema-roundtrip proof must fail if markdown generation reads a different representation than validators.
1623. Schema-roundtrip proof must fail if roundtrip canonical hash differs without known nondeterministic fields.
1624. Schema-roundtrip proof must fail if schema roundtrip is not included in detached verification.
1625. Evidence-minimization proof must ensure final reports include enough evidence to verify claims but no unnecessary unrelated data.
1626. Evidence-minimization proof must fail if proof-critical evidence is absent due to over-minimization.
1627. Evidence-minimization proof must fail if unrelated files are included and can confuse validation.
1628. Evidence-minimization proof must fail if privacy redaction removes proof-critical fields.
1629. Evidence-minimization proof must fail if logs are minimized before hashing in a way that hides failures.
1630. Evidence-minimization proof must fail if minimized logs no longer allow command transcript verification.
1631. Evidence-minimization proof must fail if omitted evidence is referenced by hash only with no retrievable artifact.
1632. Evidence-minimization proof must fail if minimization differs between local and CI artifacts.
1633. Evidence-minimization proof must fail if minimized artifact index omits required verification path.
1634. Evidence-minimization proof must fail if diagnostics are removed before validators that need them run.
1635. Recovery-from-validation-failure proof must define how failed validation affects report states.
1636. Recovery-from-validation-failure proof must fail if a failed validation leaves report state finalized.
1637. Recovery-from-validation-failure proof must fail if rerunning validation after fixing reports does not create a new validation record.
1638. Recovery-from-validation-failure proof must fail if failed validation logs are overwritten by later passing validation without retention.
1639. Recovery-from-validation-failure proof must fail if failed validation artifacts are mixed into final PASS evidence.
1640. Recovery-from-validation-failure proof must fail if validation failure cleanup deletes root-cause evidence.
1641. Recovery-from-validation-failure proof must fail if validation failure returns a success exit code.
1642. Recovery-from-validation-failure proof must fail if validation failure does not identify affected reports.
1643. Recovery-from-validation-failure proof must fail if validation failure does not block final gate.
1644. Recovery-from-validation-failure proof must fail if validation recovery path is untested.
1645. Proof-contract test must verify the documented proof command, detached verification command, and query command all exist and run.
1646. Proof-contract test must fail if documentation, Makefile, and CLI disagree on command names.
1647. Proof-contract test must fail if command examples in docs are stale.
1648. Proof-contract test must fail if commands require undocumented arguments.
1649. Proof-contract test must fail if commands rely on current working directory without documenting it.
1650. Proof-contract test must fail if commands write output outside documented paths.
1651. Proof-contract test must fail if commands produce different final artifact names than documented.
1652. Proof-contract test must fail if command exit-code semantics differ from documented semantics.
1653. Proof-contract test must fail if help output omits proof-critical options.
1654. Proof-contract test must fail if proof command cannot be invoked by a new user after clean checkout and dependency install.
1655. Static-analysis proof must define which static checks are required for proof-critical code.
1656. Static-analysis proof must fail if proof-critical code contains unreachable branches that are claimed as tested behavior.
1657. Static-analysis proof must fail if proof-critical code contains dead validators, unused proof generators, or unused safety checks.
1658. Static-analysis proof must fail if a required safety function is defined but never called by runtime scenarios.
1659. Static-analysis proof must fail if a required validator is defined but never invoked by the final proof target.
1660. Static-analysis proof must fail if a required report generator is defined but its output is never consumed by final validation.
1661. Static-analysis proof must fail if duplicate implementations of the same check disagree or one is unused.
1662. Static-analysis proof must fail if a proof-critical import is unused and hides missing integration.
1663. Static-analysis proof must fail if proof-critical code paths contain placeholder comments indicating future implementation.
1664. Static-analysis proof must fail if a proof-critical exception class is never raised or never handled where required.
1665. Call-graph proof must map runtime entrypoints to policy checks, capability checks, invariant checks, promotion gate, artifact store, state store, and event logger.
1666. Call-graph proof must fail if the safe scenario bypasses any required runtime subsystem.
1667. Call-graph proof must fail if the unsafe scenario bypasses any required denial subsystem.
1668. Call-graph proof must fail if replay uses a different call graph than original execution without explicit replay-specific rationale.
1669. Call-graph proof must fail if validators only inspect reports and no runtime call graph connects scenario execution to required subsystems.
1670. Call-graph proof must fail if runtime scenario tests call lower-level helpers instead of the integrated orchestration path.
1671. Call-graph proof must fail if proof-critical functions are reachable only from tests and not from real runtime entrypoints.
1672. Call-graph proof must fail if a required safety function is called after an irreversible side effect.
1673. Call-graph proof must fail if call graph generation omits dynamic dispatch points used by the runtime.
1674. Call-graph proof must fail if call graph evidence is not linked to source hashes.
1675. Data-flow proof must trace action inputs into policy evaluation, capability evaluation, invariant evaluation, promotion decision, artifact writes, state records, event records, and reports.
1676. Data-flow proof must fail if any proof-critical decision uses a value not present in recorded input or state.
1677. Data-flow proof must fail if an action_id is lost, replaced, or regenerated before final decision.
1678. Data-flow proof must fail if artifact hash used for approval differs from artifact hash used for promotion.
1679. Data-flow proof must fail if reviewer identity used by invariant differs from reviewer identity stored in approval evidence.
1680. Data-flow proof must fail if policy decision input differs from promotion-gate input.
1681. Data-flow proof must fail if capability decision input differs from action execution input.
1682. Data-flow proof must fail if report evidence is derived from transformed data without preserving original raw evidence hash.
1683. Data-flow proof must fail if replay input data is reconstructed from final reports instead of original recorded scenario inputs.
1684. Data-flow proof must fail if a proof-critical value is manually copied across reports rather than derived from a hashed source.
1685. Taint-tracking proof must mark untrusted inputs from artifacts, logs, reports, command output, environment, and scenario manifests.
1686. Taint-tracking proof must fail if untrusted data is executed as code, shell, template, regex, or import path.
1687. Taint-tracking proof must fail if untrusted report data controls which validators run.
1688. Taint-tracking proof must fail if untrusted artifact data controls proof output paths.
1689. Taint-tracking proof must fail if untrusted command output controls final verdict.
1690. Taint-tracking proof must fail if untrusted environment variables control required proof scope.
1691. Taint-tracking proof must fail if tainted data is written into trusted source or configuration without validation.
1692. Taint-tracking proof must fail if tainted data can select a legacy proof path.
1693. Taint-tracking proof must fail if tainted data can suppress evidence collection.
1694. Taint-tracking proof must fail if tainted data can change classification labels.
1695. Regex-safety proof must fail if validators or generators use regexes that can catastrophically backtrack on malformed reports or logs.
1696. Regex-safety proof must fail if proof-critical regexes are not anchored where exact matching is required.
1697. Regex-safety proof must fail if regex matching allows partial PASS strings inside failing output.
1698. Regex-safety proof must fail if regexes parse JSON or command transcripts where structured parsing is required.
1699. Regex-safety proof must fail if regexes treat ANSI-colored output as valid structured status.
1700. Regex-safety proof must fail if regexes accept PASS within a larger invalid token.
1701. Regex-safety proof must fail if regexes are case-insensitive for status fields that require exact enum values.
1702. Regex-safety proof must fail if regexes allow newline injection to forge separate transcript entries.
1703. Regex-safety proof must fail if regexes used for paths allow parent-directory traversal.
1704. Regex-safety proof must fail if regex negative tests are absent for proof-critical parsers.
1705. Pytest-configuration proof must hash pytest.ini, pyproject.toml pytest settings, conftest.py files, and any pytest plugins used in proof runs.
1706. Pytest-configuration proof must fail if conftest.py skips, marks, monkeypatches, or rewrites proof-critical tests without traceability.
1707. Pytest-configuration proof must fail if pytest plugins alter test collection or results without being recorded.
1708. Pytest-configuration proof must fail if warnings are globally ignored for proof-critical modules.
1709. Pytest-configuration proof must fail if test discovery excludes required test directories.
1710. Pytest-configuration proof must fail if test command uses a broad path that can pass even when required tests are missing.
1711. Pytest-configuration proof must fail if required tests are marked slow and excluded from final proof.
1712. Pytest-configuration proof must fail if required tests are marked flaky and allowed to pass conditionally.
1713. Pytest-configuration proof must fail if pytest configuration differs between local proof and CI proof.
1714. Pytest-configuration proof must fail if pytest cache or last-failed mode changes final proof collection.
1715. Test-quality proof must require assertions in every proof-critical test.
1716. Test-quality proof must fail if proof-critical tests have no assertions.
1717. Test-quality proof must fail if proof-critical tests assert only that no exception occurred.
1718. Test-quality proof must fail if proof-critical tests assert only file existence without validating content.
1719. Test-quality proof must fail if proof-critical tests assert only top-level PASS without checking required evidence.
1720. Test-quality proof must fail if proof-critical tests cannot fail when the checked behavior is removed.
1721. Test-quality proof must fail if proof-critical tests overuse monkeypatching to bypass real runtime dependencies.
1722. Test-quality proof must fail if proof-critical tests use broad exception swallowing.
1723. Test-quality proof must fail if proof-critical tests depend on execution order.
1724. Test-quality proof must fail if proof-critical tests lack negative assertions for forbidden behavior.
1725. Assertion-sensitivity proof must mutate expected values in tests and verify the tests fail.
1726. Assertion-sensitivity proof must fail if changing expected unsafe verdict does not fail tests.
1727. Assertion-sensitivity proof must fail if changing expected evidence hash does not fail tests.
1728. Assertion-sensitivity proof must fail if changing expected validator exit code does not fail tests.
1729. Assertion-sensitivity proof must fail if removing expected event type does not fail tests.
1730. Assertion-sensitivity proof must fail if removing expected state transition does not fail tests.
1731. Assertion-sensitivity proof must fail if changing expected artifact overwrite policy does not fail tests.
1732. Assertion-sensitivity proof must fail if changing expected replay mismatch behavior does not fail tests.
1733. Assertion-sensitivity proof must fail if changing expected gap resolution behavior does not fail tests.
1734. Assertion-sensitivity proof must fail if changing expected final classification does not fail tests.
1735. Mutation-score proof must define acceptable mutation survival threshold for proof-critical code.
1736. Mutation-score proof must fail if any mutation in safety-critical logic survives.
1737. Mutation-score proof must fail if any mutation in final gate logic survives.
1738. Mutation-score proof must fail if any mutation in evidence hash comparison survives.
1739. Mutation-score proof must fail if any mutation in command exit-code propagation survives.
1740. Mutation-score proof must fail if any mutation in replay comparison survives.
1741. Mutation-score proof must fail if any mutation in report-dir handling survives.
1742. Mutation-score proof must fail if any mutation in schema validation survives.
1743. Mutation-score proof must fail if mutation testing excludes proof-critical files.
1744. Mutation-score proof must fail if survived mutations are not linked to risks or corrective items.
1745. Lineage proof must record parent-child relationships among generated agents, artifacts, review requests, approvals, promotions, denials, and rollbacks.
1746. Lineage proof must fail if a promoted artifact has no parent generated-agent record.
1747. Lineage proof must fail if an approval has no parent review request.
1748. Lineage proof must fail if a promotion has no parent approval.
1749. Lineage proof must fail if a denial has no parent attempted action.
1750. Lineage proof must fail if a rollback has no parent failed or denied action.
1751. Lineage proof must fail if lineage contains orphan nodes.
1752. Lineage proof must fail if lineage contains cycles where acyclic action history is required.
1753. Lineage proof must fail if lineage differs between state records and event log.
1754. Lineage proof must fail if lineage is not included in replay validation.
1755. Identity-resolution proof must define stable identity resolution for users, agents, reviewers, generated agents, and system actors.
1756. Identity-resolution proof must fail if identity comparison uses display names, emails, or labels without canonical IDs.
1757. Identity-resolution proof must fail if aliases allow self-promotion to appear as third-party approval.
1758. Identity-resolution proof must fail if identity canonicalization differs between runtime and replay.
1759. Identity-resolution proof must fail if identity canonicalization differs between invariant engine and promotion gate.
1760. Identity-resolution proof must fail if missing identity defaults to system or admin.
1761. Identity-resolution proof must fail if unknown identity can approve promotion.
1762. Identity-resolution proof must fail if identity mapping is not hashed in scenario evidence.
1763. Identity-resolution proof must fail if identity mapping changes after approval.
1764. Identity-resolution proof must fail if identity collision is not detected.
1765. Authorization-boundary proof must define which actors can request, review, approve, deny, promote, rollback, replay, and verify.
1766. Authorization-boundary proof must fail if the same actor can perform request and approval where separation is required.
1767. Authorization-boundary proof must fail if generated agent can approve its own action through an indirect role.
1768. Authorization-boundary proof must fail if system actor can bypass review without explicit policy.
1769. Authorization-boundary proof must fail if replay actor can perform promotion.
1770. Authorization-boundary proof must fail if verifier actor can mutate evidence.
1771. Authorization-boundary proof must fail if role membership is not recorded in scenario evidence.
1772. Authorization-boundary proof must fail if role changes after action start are not handled deterministically.
1773. Authorization-boundary proof must fail if missing role membership defaults to allow.
1774. Authorization-boundary proof must fail if conflicting role memberships lack precedence rules.
1775. Policy-conflict proof must include explicit tests for allow-vs-deny conflicts.
1776. Policy-conflict proof must fail if allow overrides deny by default.
1777. Policy-conflict proof must fail if deny precedence is documented but not enforced.
1778. Policy-conflict proof must fail if conflict resolution differs between runtime and replay.
1779. Policy-conflict proof must fail if policy conflict emits PASS without warning or denial reason.
1780. Policy-conflict proof must fail if multiple deny reasons are collapsed and one required denial reason is lost.
1781. Policy-conflict proof must fail if policy conflict is resolved by source ordering without stable ordering proof.
1782. Policy-conflict proof must fail if conflict tests are absent for self-promotion policy.
1783. Policy-conflict proof must fail if conflict tests do not verify final state and event records.
1784. Policy-conflict proof must fail if conflict resolution can change across Python versions.
1785. Artifact-content proof must verify actual bytes of artifacts, not only metadata.
1786. Artifact-content proof must fail if artifact metadata exists but artifact content is missing.
1787. Artifact-content proof must fail if artifact content hash is computed before final write completes.
1788. Artifact-content proof must fail if artifact content changes after metadata write.
1789. Artifact-content proof must fail if artifact content is empty where nonempty content is required.
1790. Artifact-content proof must fail if artifact MIME/type or declared format conflicts with actual content.
1791. Artifact-content proof must fail if artifact bytes are not copied into durable evidence when referenced.
1792. Artifact-content proof must fail if artifact hash is not included in approval and promotion records.
1793. Artifact-content proof must fail if artifact content validation differs between original execution and replay.
1794. Artifact-content proof must fail if artifact content is generated nondeterministically without recorded seed or input hashes.
1795. External-process proof must record process IDs, parent process IDs, command arguments, cwd, environment hash, start time, end time, and exit status for proof-critical subprocesses.
1796. External-process proof must fail if a subprocess spawns untracked child processes that affect proof artifacts.
1797. External-process proof must fail if background processes continue writing after proof phase completion.
1798. External-process proof must fail if daemonized processes produce evidence after final validation.
1799. External-process proof must fail if child process failure is not propagated to parent command result.
1800. External-process proof must fail if stdout/stderr from child processes are not captured or hashed.
1801. External-process proof must fail if subprocess working directory differs from transcript cwd.
1802. External-process proof must fail if process timeout kills parent but leaves child process writing evidence.
1803. External-process proof must fail if subprocess signal termination is reported as normal success.
1804. External-process proof must fail if external process dependencies are not declared.
1805. Cache-key proof must define cache keys for dependency caches, test caches, proof caches, and artifact caches.
1806. Cache-key proof must fail if cache key omits source hash, dependency hash, configuration hash, Python version, or platform where relevant.
1807. Cache-key proof must fail if stale cache hit can satisfy proof-critical output.
1808. Cache-key proof must fail if cache miss and cache hit produce different final verdicts.
1809. Cache-key proof must fail if cache contents are not validated before use.
1810. Cache-key proof must fail if cache can contain generated reports from another proof_run_id.
1811. Cache-key proof must fail if cache can contain artifacts from another commit.
1812. Cache-key proof must fail if cache cleanup can delete current evidence.
1813. Cache-key proof must fail if cache poisoning can insert PASS evidence.
1814. Cache-key proof must fail if proof target relies on cache to avoid executing required behavior.
1815. Parallel-replay proof must define whether replay can run concurrently and how outputs are isolated.
1816. Parallel-replay proof must fail if replay workers share mutable global state.
1817. Parallel-replay proof must fail if replay workers write to the same manifest or report file.
1818. Parallel-replay proof must fail if replay result order changes summary verdict.
1819. Parallel-replay proof must fail if one replay worker failure is hidden by other worker successes.
1820. Parallel-replay proof must fail if replay worker timeouts are treated as missing results rather than failures.
1821. Parallel-replay proof must fail if replay worker logs are not individually captured.
1822. Parallel-replay proof must fail if replay worker proof_run_id or scenario_id is missing.
1823. Parallel-replay proof must fail if parallel replay consumes original scenario state in a mutable way.
1824. Parallel-replay proof must fail if replay aggregation lacks per-scenario hashes.
1825. Supply-chain proof must identify every third-party package, executable, and plugin used by runtime, tests, validators, and proof generation.
1826. Supply-chain proof must fail if a third-party dependency is imported but absent from declared dependency metadata.
1827. Supply-chain proof must fail if proof-critical behavior depends on an undeclared transitive dependency.
1828. Supply-chain proof must fail if a dependency version is unconstrained where version changes can alter proof semantics.
1829. Supply-chain proof must fail if proof-critical dependencies are installed from mutable branches, local paths, or unpinned URLs without provenance.
1830. Supply-chain proof must fail if an executable used by the proof path is resolved from PATH without recording its absolute path and hash where practical.
1831. Supply-chain proof must fail if two different dependency versions are used across local proof, CI proof, and detached verification.
1832. Supply-chain proof must fail if dependency installation scripts modify proof-critical source or generated evidence.
1833. Supply-chain proof must fail if optional dependency absence changes final proof scope without blocking or explicit unsupported classification.
1834. Supply-chain proof must fail if dependency vulnerability or trust status is claimed without a recorded scan source and timestamp.
1835. SBOM proof must generate or record a software bill of materials for proof-critical runtime and validation code.
1836. SBOM proof must fail if proof-critical dependencies are missing from the SBOM.
1837. SBOM proof must fail if SBOM package versions disagree with the environment metadata.
1838. SBOM proof must fail if SBOM is generated after final verdict without rerunning final validation.
1839. SBOM proof must fail if SBOM is not included in artifact index and proof hashes when supply-chain claims are made.
1840. SBOM proof must fail if SBOM includes generated reports as packages or omits local project modules.
1841. SBOM proof must fail if SBOM generation failure is downgraded to warning while supply-chain proof is mandatory.
1842. SBOM proof must fail if SBOM format or schema version is not recorded.
1843. SBOM proof must fail if SBOM cannot be regenerated from the clean checkout and declared dependency metadata.
1844. SBOM proof must fail if SBOM entries cannot be mapped back to installed distributions or source files.
1845. License proof must define whether dependency license compliance is in scope for the MVP proof.
1846. License proof must fail if license compliance is claimed but dependency licenses are not enumerated.
1847. License proof must fail if packages with unknown licenses are accepted as compliant without rationale.
1848. License proof must fail if license scan results are stale relative to dependency metadata.
1849. License proof must fail if generated proof artifacts include third-party content with untracked license obligations.
1850. License proof must fail if license report is human-readable only and not included in machine-readable final artifacts when in scope.
1851. License proof must fail if license warnings are ignored while release readiness is claimed.
1852. License proof must fail if license scope differs between source checkout and packaged release.
1853. License proof must fail if optional dependencies used in proof are omitted from license evidence.
1854. License proof must fail if dependency license evidence is not hashed into final proof bundle when license claims are made.
1855. Static-type proof must define whether type checking is required for proof-critical modules.
1856. Static-type proof must fail if proof-critical modules are excluded from type checking without rationale.
1857. Static-type proof must fail if type checker errors are ignored while claiming proof-critical API stability.
1858. Static-type proof must fail if type-ignore comments appear on proof-critical paths without justification.
1859. Static-type proof must fail if untyped dictionaries are used for proof-critical report schemas where typed models or strict schemas are required.
1860. Static-type proof must fail if validators accept Any-typed report objects without schema validation.
1861. Static-type proof must fail if type checking runs on a different Python version target than the proof runtime.
1862. Static-type proof must fail if generated typed models are stale relative to JSON schemas.
1863. Static-type proof must fail if type-checker configuration excludes tests or validators that are part of final proof.
1864. Static-type proof must fail if type-checker results are not recorded in command transcript when type checking is mandatory.
1865. Lint proof must define which lint rules are mandatory for proof-critical code.
1866. Lint proof must fail if proof-critical lint errors are ignored while final proof claims maintainable implementation.
1867. Lint proof must fail if noqa or lint-disable comments appear on proof-critical validation, hashing, path handling, or subprocess code without rationale.
1868. Lint proof must fail if broad exception handling is allowed in validators or proof generators without explicit error propagation tests.
1869. Lint proof must fail if shell-injection-prone subprocess patterns are present in proof-critical code.
1870. Lint proof must fail if mutable default arguments are used in proof-critical state or report construction.
1871. Lint proof must fail if global mutable state is used in validators, replay, or final gate without isolation proof.
1872. Lint proof must fail if proof-critical functions have unreachable return branches that can mask failures.
1873. Lint proof must fail if duplicate code implements validation rules inconsistently.
1874. Lint proof must fail if lint configuration differs between local proof and CI proof.
1875. Complexity proof must define acceptable complexity limits for proof-critical validators and generators.
1876. Complexity proof must fail if a proof-critical validator is too complex to review and lacks focused tests.
1877. Complexity proof must fail if final gate logic is split across many implicit side effects without a dependency graph.
1878. Complexity proof must fail if a single generator both creates evidence and validates the same evidence without separation.
1879. Complexity proof must fail if a proof-critical function has many branches but no branch-level test coverage.
1880. Complexity proof must fail if error-handling branches are untested in validators.
1881. Complexity proof must fail if proof-critical code has hidden coupling through module-level constants.
1882. Complexity proof must fail if report schema rules are duplicated manually across validators without a shared authoritative schema.
1883. Complexity proof must fail if proof-critical code cannot be audited through artifact index, dependency graph, and call graph together.
1884. Complexity proof must fail if simplification opportunities are recorded as TODOs on proof-critical paths.
1885. Config-freeze proof must capture all configuration values at proof start and fail if they change during proof execution.
1886. Config-freeze proof must fail if configuration files are modified between scenario execution and final validation.
1887. Config-freeze proof must fail if proof commands read configuration after validation that differs from configuration used during execution.
1888. Config-freeze proof must fail if environment-derived configuration is not frozen into proof metadata.
1889. Config-freeze proof must fail if a config default is computed from current time, current user, hostname, or local path.
1890. Config-freeze proof must fail if config values are inferred from existing reports.
1891. Config-freeze proof must fail if config changes can alter required validators, required attacks, or required scenarios after proof start.
1892. Config-freeze proof must fail if config freeze metadata is absent from detached verification.
1893. Config-freeze proof must fail if config schema version is missing.
1894. Config-freeze proof must fail if config values differ between original execution and replay without being recorded as replay inputs.
1895. Clock-integrity proof must define acceptable clock sources for timestamps and durations.
1896. Clock-integrity proof must fail if system clock changes during proof execution and timestamps are proof-critical.
1897. Clock-integrity proof must fail if generated reports use local time without timezone while chronological validation is required.
1898. Clock-integrity proof must fail if monotonic durations are computed from wall-clock timestamps.
1899. Clock-integrity proof must fail if negative durations are normalized to zero.
1900. Clock-integrity proof must fail if timestamp parsing accepts ambiguous date formats.
1901. Clock-integrity proof must fail if timestamp precision is insufficient to order proof-critical steps and no sequence number is present.
1902. Clock-integrity proof must fail if future timestamps are tolerated in final reports.
1903. Clock-integrity proof must fail if CI and local proof timestamps are compared without timezone normalization.
1904. Clock-integrity proof must fail if replay uses current time for decisions that should be based on recorded original time.
1905. Host-fingerprint proof must record non-sensitive host characteristics that can affect proof behavior.
1906. Host-fingerprint proof must fail if host-specific behavior changes proof verdict and is not included in compatibility scope.
1907. Host-fingerprint proof must fail if hostname, username, or home path leaks into proof-critical evidence IDs.
1908. Host-fingerprint proof must fail if proof behavior depends on current user permissions without recording them.
1909. Host-fingerprint proof must fail if proof behavior differs across filesystems and filesystem type is not recorded.
1910. Host-fingerprint proof must fail if available CPU count changes parallel proof behavior without being recorded.
1911. Host-fingerprint proof must fail if locale, encoding, or timezone are missing from environment metadata.
1912. Host-fingerprint proof must fail if path to Python interpreter is missing or resolves through a mutable symlink without recording resolved target.
1913. Host-fingerprint proof must fail if virtual environment identity and dependency state are missing.
1914. Host-fingerprint proof must fail if container and host metadata are mixed without distinction.
1915. Noninteractivity proof must ensure final proof target can run without prompts, manual input, or interactive approval.
1916. Noninteractivity proof must fail if a proof command waits for stdin or terminal input.
1917. Noninteractivity proof must fail if proof success depends on manually editing generated reports.
1918. Noninteractivity proof must fail if proof success depends on manual cleanup between commands.
1919. Noninteractivity proof must fail if proof success depends on a GUI, TTY, clipboard, browser, or editor.
1920. Noninteractivity proof must fail if user confirmation can bypass validators.
1921. Noninteractivity proof must fail if manual review is required but not represented as structured, pre-existing evidence.
1922. Noninteractivity proof must fail if CI cannot run the same proof target because of interactive prompts.
1923. Noninteractivity proof must fail if commands behave differently when stdin is closed.
1924. Noninteractivity proof must fail if progress spinners or terminal control output corrupt structured logs.
1925. Exit-status taxonomy proof must define exact exit codes for success, validation failure, infrastructure failure, usage error, timeout, and unexpected exception.
1926. Exit-status taxonomy proof must fail if all failures collapse to the same ambiguous code while automation needs classification.
1927. Exit-status taxonomy proof must fail if usage errors can be mistaken for successful validation failures in anti-false-PASS audit.
1928. Exit-status taxonomy proof must fail if infrastructure failures can be mistaken for intended corruption rejection.
1929. Exit-status taxonomy proof must fail if timeout exit status is not distinguishable from validator rejection.
1930. Exit-status taxonomy proof must fail if signal termination is not distinguishable from normal nonzero exit.
1931. Exit-status taxonomy proof must fail if final gate ignores validator-specific exit status taxonomy.
1932. Exit-status taxonomy proof must fail if command transcript records only boolean success without raw exit status.
1933. Exit-status taxonomy proof must fail if CI status cannot distinguish validation failure from broken proof infrastructure.
1934. Exit-status taxonomy proof must fail if docs do not explain exit-code semantics for proof commands.
1935. Stdout-schema proof must define whether proof-critical CLIs emit structured JSON to stdout, stderr, or files.
1936. Stdout-schema proof must fail if automation parses prose stdout for PASS/FAIL where JSON output is available or required.
1937. Stdout-schema proof must fail if structured stdout is mixed with progress logs.
1938. Stdout-schema proof must fail if stderr contains proof-critical structured data but transcript treats stderr as diagnostic only.
1939. Stdout-schema proof must fail if a command emits malformed JSON but writes a PASS report file.
1940. Stdout-schema proof must fail if JSON-lines output has missing, duplicate, or reordered sequence numbers.
1941. Stdout-schema proof must fail if command output schema version is missing.
1942. Stdout-schema proof must fail if command output schema and report file schema disagree.
1943. Stdout-schema proof must fail if a proof command returns zero but emits a structured failure object.
1944. Stdout-schema proof must fail if a proof command returns nonzero but emits structured PASS.
1945. Golden-run proof must define a minimal known-good proof fixture and a minimal known-bad proof fixture.
1946. Golden-run proof must fail if known-good fixture does not pass detached verification.
1947. Golden-run proof must fail if known-bad fixture does not fail detached verification.
1948. Golden-run proof must fail if known-bad fixture failure reason is not the expected one.
1949. Golden-run proof must fail if fixtures are generated from the current implementation during the same proof run.
1950. Golden-run proof must fail if known-good fixture contains hand-written PASS values not derivable from evidence.
1951. Golden-run proof must fail if fixture hashes are not recorded.
1952. Golden-run proof must fail if fixtures are stale relative to schema version.
1953. Golden-run proof must fail if fixture tests are not included in final proof target.
1954. Golden-run proof must fail if golden fixtures are confused with current generated proof evidence.
1955. Proof-locality proof must require all final verification evidence to reside under the declared proof bundle or tracked source.
1956. Proof-locality proof must fail if final verification reads arbitrary files from the working tree not listed in artifact index or source manifest.
1957. Proof-locality proof must fail if final verification reads network resources.
1958. Proof-locality proof must fail if final verification reads user home configuration.
1959. Proof-locality proof must fail if final verification reads system temp files outside the proof bundle.
1960. Proof-locality proof must fail if final verification depends on currently installed package source instead of recorded dependency metadata.
1961. Proof-locality proof must fail if final verification depends on environment variables beyond the declared allowlist.
1962. Proof-locality proof must fail if final verification succeeds only in the original generating workspace.
1963. Proof-locality proof must fail if relocated proof bundle cannot be verified.
1964. Proof-locality proof must fail if proof bundle contains references to files that were not included or tracked.
1965. Upgradability proof must define how future proof schema or requirement changes will avoid silently invalidating old proof.
1966. Upgradability proof must fail if new validators cannot reject old incomplete reports.
1967. Upgradability proof must fail if old validators can mark new reports PASS while ignoring new required fields.
1968. Upgradability proof must fail if proof bundle lacks minimum_validator_version.
1969. Upgradability proof must fail if final verdict lacks proof_contract_version.
1970. Upgradability proof must fail if schema migrations do not preserve failure states.
1971. Upgradability proof must fail if new corrective items can be added without invalidating previous final PASS claims.
1972. Upgradability proof must fail if old proof artifacts are presented as satisfying new requirements.
1973. Upgradability proof must fail if version comparison uses lexical ordering where semantic ordering is required.
1974. Upgradability proof must fail if upgrade tests are absent for at least one old invalid bundle and one current valid bundle.
1975. Completeness-bound proof must state whether the corrective list is exhaustive or best-effort.
1976. Completeness-bound proof must fail if final proof claims all possible false-PASS bugs are impossible.
1977. Completeness-bound proof must fail if best-effort discovery is presented as mathematical completeness.
1978. Completeness-bound proof must fail if unknown unknowns are marked FIXED.
1979. Completeness-bound proof must fail if final classification lacks a statement of residual verification limits.
1980. Completeness-bound proof must fail if residual limitations contradict FUNCTIONAL_RUNTIME_MVP scope.
1981. Completeness-bound proof must fail if gap discovery stops after finding a fixed number of gaps without recording search budget.
1982. Completeness-bound proof must fail if search budget is too small for claimed scope and no rationale is given.
1983. Completeness-bound proof must fail if static scans omit binary, generated, or config files that affect proof behavior without rationale.
1984. Completeness-bound proof must fail if final verdict equates no new findings with absence of gaps.
1985. Git-object proof must record the exact tree hash used for the final proof run.
1986. Git-object proof must fail if the final proof commit hash exists but the working tree content differs from the recorded tree hash.
1987. Git-object proof must fail if proof-critical files are generated from untracked files not represented in the tree hash.
1988. Git-object proof must fail if submodules are present and their commits are not recorded.
1989. Git-object proof must fail if git-lfs pointers are used and the actual LFS object hashes are not verified.
1990. Git-object proof must fail if line-ending filters, clean filters, or smudge filters alter proof-critical files without recorded normalization.
1991. Git-object proof must fail if proof-critical files are replaced by Git attributes filters during checkout.
1992. Git-object proof must fail if sparse checkout excludes proof-critical files while final proof claims full coverage.
1993. Git-object proof must fail if assume-unchanged or skip-worktree flags hide modified proof-critical files.
1994. Git-object proof must fail if final proof source manifest and git tree hash disagree.
1995. Repository-layout proof must define canonical locations for source, tests, validators, scenarios, schemas, reports, artifacts, and fixtures.
1996. Repository-layout proof must fail if proof-critical files are placed outside canonical locations without explicit registration.
1997. Repository-layout proof must fail if two directories can both be interpreted as the current report directory.
1998. Repository-layout proof must fail if report generators scan parent directories and include files outside the repository.
1999. Repository-layout proof must fail if tests import proof-critical modules through relative path hacks instead of package imports.
2000. Repository-layout proof must fail if source and generated output directories overlap.
2001. Repository-layout proof must fail if fixtures and generated current reports share filenames.
2002. Repository-layout proof must fail if validator discovery depends on filesystem order rather than an explicit registry.
2003. Repository-layout proof must fail if scenario discovery depends on filesystem order rather than an explicit registry.
2004. Repository-layout proof must fail if canonical paths are not recorded in final proof metadata.
2005. Validator-registry proof must define the authoritative list of validators required for FUNCTIONAL_RUNTIME_MVP.
2006. Validator-registry proof must fail if required validators are inferred only from files present on disk.
2007. Validator-registry proof must fail if adding an unrelated validator changes final gate behavior without registry update.
2008. Validator-registry proof must fail if removing a validator file does not fail the registry check.
2009. Validator-registry proof must fail if validator registry contains validators not executed in final transcript.
2010. Validator-registry proof must fail if validator registry omits a validator required by the corrective-list coverage matrix.
2011. Validator-registry proof must fail if validator IDs are generated from filenames without stable explicit IDs.
2012. Validator-registry proof must fail if two validators declare the same validator ID.
2013. Validator-registry proof must fail if validators are grouped so broadly that individual missing validators are hidden.
2014. Validator-registry proof must fail if validator registry is generated from the final reports it validates.
2015. Scenario-registry proof must define the authoritative list of runtime scenarios required for FUNCTIONAL_RUNTIME_MVP.
2016. Scenario-registry proof must fail if scenarios are discovered only by pytest collection without a canonical scenario registry.
2017. Scenario-registry proof must fail if a required scenario is skipped because its file is missing.
2018. Scenario-registry proof must fail if a scenario is renamed without preserving stable scenario ID.
2019. Scenario-registry proof must fail if two scenario definitions share the same scenario ID.
2020. Scenario-registry proof must fail if scenario registry omits required safe, unsafe, replay, failure, rollback, artifact, and policy scenarios.
2021. Scenario-registry proof must fail if scenario registry changes after proof generation starts.
2022. Scenario-registry proof must fail if final proof uses scenario results not present in the registry.
2023. Scenario-registry proof must fail if registry marks a scenario optional while traceability marks it mandatory.
2024. Scenario-registry proof must fail if scenario registry is generated from scenario execution results.
2025. Test-registry proof must define the authoritative list of test commands and test nodes required for FUNCTIONAL_RUNTIME_MVP.
2026. Test-registry proof must fail if required tests are inferred only from the latest pytest collection.
2027. Test-registry proof must fail if required test node IDs change without a registry migration.
2028. Test-registry proof must fail if a required test file exists but no longer contains the required test node.
2029. Test-registry proof must fail if required tests pass because they were deselected from the executed command.
2030. Test-registry proof must fail if required test list is not hashed into the proof bundle.
2031. Test-registry proof must fail if test registry includes tests that are not linked to requirements or corrective items.
2032. Test-registry proof must fail if test registry omits negative tests required by safety-critical validators.
2033. Test-registry proof must fail if test registry is updated after transcript validation without rerunning tests.
2034. Test-registry proof must fail if test registry contains duplicate logical tests under different names.
2035. Schema-registry proof must define the authoritative list of report schemas required for final proof.
2036. Schema-registry proof must fail if any final JSON report has no registered schema.
2037. Schema-registry proof must fail if schema registry includes schemas not used by any final report without marking them legacy or future.
2038. Schema-registry proof must fail if schema registry omits schemas for transcript, artifact index, evidence graph, final verdict, and detached verification output.
2039. Schema-registry proof must fail if two schemas claim the same report_type and schema_version.
2040. Schema-registry proof must fail if schema registry is generated from existing reports.
2041. Schema-registry proof must fail if schema files are modified after report validation.
2042. Schema-registry proof must fail if validators and markdown generators read different schema registries.
2043. Schema-registry proof must fail if schema registry lacks hash and proof_run_id metadata.
2044. Schema-registry proof must fail if unsupported schemas are treated as warnings only.
2045. Proof-contract registry must define required commands, report types, validators, scenarios, schemas, artifacts, and final gates in one machine-readable contract.
2046. Proof-contract registry must fail if Makefile, CLI, docs, and final gate disagree with the contract.
2047. Proof-contract registry must fail if the contract is generated from successful proof output.
2048. Proof-contract registry must fail if the contract can be changed during proof execution.
2049. Proof-contract registry must fail if the contract omits corrective-list item coverage.
2050. Proof-contract registry must fail if the contract omits detached verification requirements.
2051. Proof-contract registry must fail if the contract omits anti-false-PASS requirements.
2052. Proof-contract registry must fail if the contract omits replay requirements.
2053. Proof-contract registry must fail if contract hash is absent from final verdict.
2054. Proof-contract registry must fail if final verdict claims a contract version not present in source.
2055. Gate-input proof must enumerate every input consumed by the final gate.
2056. Gate-input proof must fail if final gate reads files not listed as gate inputs.
2057. Gate-input proof must fail if final gate ignores a listed mandatory input.
2058. Gate-input proof must fail if final gate input hashes are not recorded.
2059. Gate-input proof must fail if final gate input hashes differ from artifact index hashes.
2060. Gate-input proof must fail if final gate can pass with a missing gate input.
2061. Gate-input proof must fail if final gate can pass with an unreadable gate input.
2062. Gate-input proof must fail if final gate can pass with an unsupported gate input schema.
2063. Gate-input proof must fail if final gate can pass with an input from a different proof_run_id.
2064. Gate-input proof must fail if final gate can pass after one input is replaced by a stale older version.
2065. Gate-output proof must define every output produced by the final gate.
2066. Gate-output proof must fail if final gate writes multiple final verdict files with conflicting results.
2067. Gate-output proof must fail if final gate output omits raw validator aggregation.
2068. Gate-output proof must fail if final gate output omits blocking evidence when verdict is not PASS.
2069. Gate-output proof must fail if final gate output omits proof contract version.
2070. Gate-output proof must fail if final gate output omits detached verification command and result.
2071. Gate-output proof must fail if final gate output is not included in artifact index.
2072. Gate-output proof must fail if final gate output is rewritten after detached verification.
2073. Gate-output proof must fail if final gate output is generated before all gate inputs are finalized.
2074. Gate-output proof must fail if final gate output cannot be regenerated deterministically from gate inputs.
2075. Detached-verifier contract must define the verifier's trusted inputs and forbidden inputs.
2076. Detached-verifier contract must fail if detached verifier reads generated reports outside the supplied bundle.
2077. Detached-verifier contract must fail if detached verifier reads source files not listed in the source manifest.
2078. Detached-verifier contract must fail if detached verifier calls report generators.
2079. Detached-verifier contract must fail if detached verifier calls runtime scenarios.
2080. Detached-verifier contract must fail if detached verifier calls external network services.
2081. Detached-verifier contract must fail if detached verifier writes into the proof bundle being verified.
2082. Detached-verifier contract must fail if detached verifier accepts a bundle without artifact index.
2083. Detached-verifier contract must fail if detached verifier accepts a bundle without proof contract metadata.
2084. Detached-verifier contract must fail if detached verifier result is not itself machine-readable and hashable.
2085. Evidence-hash algorithm proof must define the exact hash algorithm and encoding used for every hash field.
2086. Evidence-hash algorithm proof must fail if different reports use SHA-1, MD5, or unspecified hashes for proof-critical evidence.
2087. Evidence-hash algorithm proof must fail if hash strings omit algorithm prefix where multiple algorithms are possible.
2088. Evidence-hash algorithm proof must fail if binary and text files use inconsistent hashing semantics.
2089. Evidence-hash algorithm proof must fail if directory hashes are computed without deterministic file ordering.
2090. Evidence-hash algorithm proof must fail if hash comparison is case-insensitive when strict canonical lowercase is required.
2091. Evidence-hash algorithm proof must fail if truncated hashes are used as proof-critical equality.
2092. Evidence-hash algorithm proof must fail if hash collision assumptions are not documented for any shortened display hash.
2093. Evidence-hash algorithm proof must fail if checksum fields are mislabeled as cryptographic hashes.
2094. Evidence-hash algorithm proof must fail if final verifier does not recompute every proof-critical hash.
2095. Directory-hash proof must define canonical directory hashing for report bundles, evidence directories, and source manifests.
2096. Directory-hash proof must fail if directory hash omits empty files, file modes where relevant, symlink targets, or nested directories.
2097. Directory-hash proof must fail if directory hash includes nondeterministic metadata such as mtime without normalization.
2098. Directory-hash proof must fail if directory hash ignores file deletion.
2099. Directory-hash proof must fail if directory hash ignores unexpected extra files.
2100. Directory-hash proof must fail if directory hash changes across platforms without recorded normalization.
2101. Directory-hash proof must fail if directory hash can be reproduced only in the original filesystem location.
2102. Directory-hash proof must fail if directory hash does not distinguish files and directories with same path text under corrupted metadata.
2103. Directory-hash proof must fail if final bundle hash is absent from final verdict.
2104. Directory-hash proof must fail if detached verification does not recompute final bundle hash.
2105. Requirement-to-code proof must map every mandatory requirement to concrete runtime code paths, not only tests or reports.
2106. Requirement-to-code proof must fail if a mandatory runtime requirement maps only to generated proof code.
2107. Requirement-to-code proof must fail if a safety requirement maps only to documentation.
2108. Requirement-to-code proof must fail if a requirement maps to a file but not to a function, class, or command where practical.
2109. Requirement-to-code proof must fail if mapped code path is unreachable from runtime entrypoints.
2110. Requirement-to-code proof must fail if mapped code path is excluded by configuration during final proof.
2111. Requirement-to-code proof must fail if mapped code path belongs to a mock while runtime proof claims real behavior.
2112. Requirement-to-code proof must fail if mapped code path changed after test evidence was generated.
2113. Requirement-to-code proof must fail if mapped code path lacks both positive and negative behavioral evidence where required.
2114. Requirement-to-code proof must fail if multiple requirements map to one vague code reference without row-level explanation.
2115. Requirement-to-test proof must map every mandatory requirement to specific test node IDs.
2116. Requirement-to-test proof must fail if a mandatory requirement maps only to a test file and not a test node.
2117. Requirement-to-test proof must fail if test node ID was not collected in final pytest transcript.
2118. Requirement-to-test proof must fail if test node passed but did not assert the requirement-specific behavior.
2119. Requirement-to-test proof must fail if requirement maps to a skipped, xfailed, deselected, or quarantined test.
2120. Requirement-to-test proof must fail if requirement maps only to a snapshot test without semantic assertions.
2121. Requirement-to-test proof must fail if requirement maps only to a smoke test.
2122. Requirement-to-test proof must fail if test node changed after traceability generation.
2123. Requirement-to-test proof must fail if test result cannot be tied to source commit and proof_run_id.
2124. Requirement-to-test proof must fail if requirement-to-test mapping is generated from test names alone.
2125. Requirement-to-validator proof must map every mandatory report/proof requirement to specific validator IDs.
2126. Requirement-to-validator proof must fail if a mandatory report/proof requirement maps only to final all-in-one validator without lower-level validator where needed.
2127. Requirement-to-validator proof must fail if mapped validator did not run after the relevant report was generated.
2128. Requirement-to-validator proof must fail if mapped validator checks only schema but requirement needs semantic validation.
2129. Requirement-to-validator proof must fail if mapped validator can pass on missing evidence.
2130. Requirement-to-validator proof must fail if mapped validator has no negative test for the requirement.
2131. Requirement-to-validator proof must fail if mapped validator is optional or diagnostic.
2132. Requirement-to-validator proof must fail if mapped validator is deprecated.
2133. Requirement-to-validator proof must fail if validator mapping is created from filenames only.
2134. Requirement-to-validator proof must fail if validator source hash differs from the hash recorded when it ran.
2135. Report-generation DAG proof must define each report generation step as inputs, outputs, command, generator, and validation step.
2136. Report-generation DAG proof must fail if any report is generated by an undocumented step.
2137. Report-generation DAG proof must fail if any generation step has unrecorded inputs.
2138. Report-generation DAG proof must fail if any generation step output is not validated.
2139. Report-generation DAG proof must fail if any generated report is never consumed.
2140. Report-generation DAG proof must fail if any consumed report was not generated in the current proof_run_id.
2141. Report-generation DAG proof must fail if generation DAG has cycles not resolved by explicit fixed-point or two-pass rules.
2142. Report-generation DAG proof must fail if two-pass generation does not prove convergence.
2143. Report-generation DAG proof must fail if a generator reads its own previous output as evidence.
2144. Report-generation DAG proof must fail if DAG metadata is absent from final artifact index.
2145. Fixed-point proof must define any required multi-pass generation and convergence criteria.
2146. Fixed-point proof must fail if second-pass reports differ semantically from first-pass reports without another validation pass.
2147. Fixed-point proof must fail if convergence is assumed after a fixed number of passes without comparing outputs.
2148. Fixed-point proof must fail if convergence ignores evidence_refs, hashes, statuses, or classification.
2149. Fixed-point proof must fail if convergence is declared while final transcript changes after convergence.
2150. Fixed-point proof must fail if convergence check normalizes away semantic differences.
2151. Fixed-point proof must fail if convergence can pass with missing self-referential hashes.
2152. Fixed-point proof must fail if convergence proof is absent for reports that cite final proof bundle hashes.
2153. Fixed-point proof must fail if convergence proof is not rerun after final validator list changes.
2154. Fixed-point proof must fail if final proof target relies on fixed-point outputs but does not validate fixed-point status.
2155. Trusted-computing-base proof must list the minimal code, tools, schemas, and contracts trusted by the proof system.
2156. Trusted-computing-base proof must fail if trusted components are not hashed and recorded.
2157. Trusted-computing-base proof must fail if generated reports are included in the trusted computing base.
2158. Trusted-computing-base proof must fail if runtime artifacts are trusted without validation.
2159. Trusted-computing-base proof must fail if the final verifier trusts the proof bundle fields it is supposed to recompute.
2160. Trusted-computing-base proof must fail if trusted code changes after evidence generation.
2161. Trusted-computing-base proof must fail if the trusted component list is generated from the current proof output.
2162. Trusted-computing-base proof must fail if untrusted plugins, hooks, or extensions can affect trusted proof decisions.
2163. Trusted-computing-base proof must fail if the proof system cannot explain which components must be audited manually.
2164. Trusted-computing-base proof must fail if the final PASS depends on a component outside the declared trusted base.
2165. Attestation proof must produce a machine-readable attestation for the final proof run.
2166. Attestation proof must fail if attestation omits subject commit, tree hash, proof_run_id, contract version, command, and artifact bundle hash.
2167. Attestation proof must fail if attestation is generated before detached verification passes.
2168. Attestation proof must fail if attestation claims a different artifact bundle hash than the final artifact index.
2169. Attestation proof must fail if attestation can be regenerated after modifying artifacts without changing its subject hash.
2170. Attestation proof must fail if attestation includes a PASS predicate while final verdict is not PASS.
2171. Attestation proof must fail if attestation predicate type is not versioned.
2172. Attestation proof must fail if attestation does not name the verifier implementation and hash.
2173. Attestation proof must fail if attestation is omitted from artifact index when release or CI claims rely on it.
2174. Attestation proof must fail if attestation can be confused with historical attestations from older runs.
2175. Provenance-attestation proof must record build-style provenance for proof artifacts even if no release package is built.
2176. Provenance-attestation proof must fail if materials omit source files, schemas, validators, proof contract, dependencies, and scenario definitions.
2177. Provenance-attestation proof must fail if byproducts omit reports, logs, manifests, replay outputs, and detached verification output.
2178. Provenance-attestation proof must fail if invocation parameters omit command arguments, cwd, environment hash, and proof_run_id.
2179. Provenance-attestation proof must fail if provenance subject does not match final bundle hash.
2180. Provenance-attestation proof must fail if provenance builder identity is missing or ambiguous.
2181. Provenance-attestation proof must fail if provenance is created from stale transcript entries.
2182. Provenance-attestation proof must fail if provenance materials include files not actually used.
2183. Provenance-attestation proof must fail if provenance materials omit files that affected proof outputs.
2184. Provenance-attestation proof must fail if provenance is not verified by detached verification.
2185. Build-hook proof must fail if git hooks, pre-commit hooks, post-checkout hooks, or local automation modify proof-critical files during proof.
2186. Build-hook proof must fail if proof success depends on hooks that are not declared and hashed.
2187. Build-hook proof must fail if hooks are disabled locally but enabled in CI and proof results differ.
2188. Build-hook proof must fail if hooks generate reports outside the declared report-generation DAG.
2189. Build-hook proof must fail if hooks can update snapshots, fixtures, schemas, or reports during final proof mode.
2190. Build-hook proof must fail if hook failures are ignored.
2191. Build-hook proof must fail if hook outputs are used as evidence without artifact-index registration.
2192. Build-hook proof must fail if hooks read user-specific configuration that affects proof behavior.
2193. Build-hook proof must fail if hooks cause working-tree changes after source manifest capture.
2194. Build-hook proof must fail if hooks are not represented in command transcript when they run.
2195. Generated-code proof must identify any code generated before or during the proof.
2196. Generated-code proof must fail if generated code is used without recording generator hash, input hashes, and output hashes.
2197. Generated-code proof must fail if generated code is edited manually after generation without source manifest update.
2198. Generated-code proof must fail if generated code differs from regenerated output in clean checkout.
2199. Generated-code proof must fail if generated validators or schemas validate themselves without independent checks.
2200. Generated-code proof must fail if generated runtime code is excluded from source mutation proof.
2201. Generated-code proof must fail if generated code has weaker test coverage than hand-written proof-critical code.
2202. Generated-code proof must fail if generated code path is mistaken for generated report output and excluded from validation.
2203. Generated-code proof must fail if generated code imports stale generated modules from previous proof runs.
2204. Generated-code proof must fail if generated code is required but generator is not executable in clean checkout.
2205. Fixture-integrity proof must hash every fixture used by proof-critical tests.
2206. Fixture-integrity proof must fail if a fixture changes after test execution but before final validation.
2207. Fixture-integrity proof must fail if fixtures are loaded from outside the repository or proof bundle.
2208. Fixture-integrity proof must fail if fixtures contain current-run proof_run_id values.
2209. Fixture-integrity proof must fail if fixture expected values are derived from current implementation output.
2210. Fixture-integrity proof must fail if invalid fixtures accidentally become valid under relaxed schema changes.
2211. Fixture-integrity proof must fail if fixtures contain stale commits that are not marked historical.
2212. Fixture-integrity proof must fail if fixture update commands run during final proof.
2213. Fixture-integrity proof must fail if fixture hashes are not linked to test transcript entries.
2214. Fixture-integrity proof must fail if fixture data can be overwritten by report generation.
2215. Baseline proof must define the previous known classification and the current target classification.
2216. Baseline proof must fail if regression comparison is made against an unknown or unverified baseline.
2217. Baseline proof must fail if the baseline artifact bundle is not hash-identified.
2218. Baseline proof must fail if the baseline is newer than the current proof commit.
2219. Baseline proof must fail if improvements are claimed without comparing against baseline blockers.
2220. Baseline proof must fail if baseline failures disappear only because requirements were removed.
2221. Baseline proof must fail if baseline and current proof use different proof contracts without migration notes.
2222. Baseline proof must fail if baseline evidence is used as current evidence.
2223. Baseline proof must fail if current final verdict omits unresolved baseline blockers.
2224. Baseline proof must fail if baseline comparison ignores newly introduced regressions.
2225. Regression-matrix proof must map every prior false-PASS bug to a regression test, validator, anti-false-PASS attack, or mutation test.
2226. Regression-matrix proof must fail if any prior false-PASS bug lacks a current prevention mechanism.
2227. Regression-matrix proof must fail if a regression test exists but is not run in final proof.
2228. Regression-matrix proof must fail if a regression validator exists but is not included in final gate.
2229. Regression-matrix proof must fail if a regression attack exists but expected rejection reason is not verified.
2230. Regression-matrix proof must fail if a prior bug is marked obsolete without rationale.
2231. Regression-matrix proof must fail if regression coverage is documented only in prose.
2232. Regression-matrix proof must fail if regression IDs are unstable across reports.
2233. Regression-matrix proof must fail if regression closure evidence is older than the fixing source change.
2234. Regression-matrix proof must fail if regression matrix is not included in final proof hashes.
2235. Failure-injection proof must inject failures into policy evaluation, capability evaluation, invariant evaluation, artifact write, state write, event write, replay, validator, and report generation.
2236. Failure-injection proof must fail if any injected failure still allows final PASS.
2237. Failure-injection proof must fail if injected runtime failures produce active artifacts.
2238. Failure-injection proof must fail if injected evidence-write failures are invisible to final validators.
2239. Failure-injection proof must fail if injected validator failures are converted into warnings.
2240. Failure-injection proof must fail if injected replay failures are hidden by cached replay reports.
2241. Failure-injection proof must fail if injected report-generation failures leave stale previous PASS reports.
2242. Failure-injection proof must fail if injected transcript-recording failures still permit final PASS.
2243. Failure-injection proof must fail if injection points are mocked differently than real runtime paths.
2244. Failure-injection proof must record injection point, expected failure class, observed failure class, and final classification impact.
2245. Fault-containment proof must show that failure in one proof phase cannot corrupt unrelated proof evidence.
2246. Fault-containment proof must fail if a failed scenario corrupts safe scenario evidence.
2247. Fault-containment proof must fail if a failed replay corrupts original execution evidence.
2248. Fault-containment proof must fail if a failed validator rewrites reports consumed by later validators.
2249. Fault-containment proof must fail if a failed report generator leaves partial outputs accepted by final gate.
2250. Fault-containment proof must fail if a failed cleanup deletes source or fixtures.
2251. Fault-containment proof must fail if a failed proof run contaminates the next proof run.
2252. Fault-containment proof must fail if failure isolation differs between local and CI execution.
2253. Fault-containment proof must fail if proof_run_id isolation is absent from temporary directories.
2254. Fault-containment proof must fail if failure artifacts are not retained for diagnosis when final verdict is non-PASS.
2255. Strict-mode proof must define a final strict mode that rejects warnings, missing optional proof evidence, and deprecated paths for MVP-critical checks.
2256. Strict-mode proof must fail if final proof runs outside strict mode.
2257. Strict-mode proof must fail if validators silently downgrade to permissive mode.
2258. Strict-mode proof must fail if report generators emit best-effort reports in strict mode.
2259. Strict-mode proof must fail if strict mode can be disabled by environment variable, config, or CLI flag during final proof.
2260. Strict-mode proof must fail if strict mode is not recorded in every final report.
2261. Strict-mode proof must fail if detached verification does not enforce strict mode.
2262. Strict-mode proof must fail if strict mode differs between original proof and detached verification.
2263. Strict-mode proof must fail if strict mode allows unknown statuses.
2264. Strict-mode proof must fail if strict mode lacks negative tests.
2265. Version-skew proof must check that source, tests, validators, schemas, proof contract, reports, and docs all refer to compatible versions.
2266. Version-skew proof must fail if reports cite an older proof contract than final verifier expects.
2267. Version-skew proof must fail if docs cite a newer proof command than Makefile implements.
2268. Version-skew proof must fail if schemas cite a newer report_type than generators emit.
2269. Version-skew proof must fail if validators cite a different schema version than final reports.
2270. Version-skew proof must fail if tests expect old behavior but requirements demand new behavior.
2271. Version-skew proof must fail if replay manifests use older scenario definitions than current traceability requires.
2272. Version-skew proof must fail if corrective-list coverage uses an older corrective-list version than final verdict.
2273. Version-skew proof must fail if CI workflow uses an older proof target than local docs.
2274. Version-skew proof must fail if final proof mixes artifacts from incompatible contract versions.
2275. Report-diff proof must produce semantic diffs between first-pass and final reports where multi-pass generation is used.
2276. Report-diff proof must fail if semantic diffs include status, verdict, evidence_refs, hashes, counts, or classification changes after final validation.
2277. Report-diff proof must fail if report diffs are only text diffs and miss JSON semantic changes.
2278. Report-diff proof must fail if report diffs ignore removed rows.
2279. Report-diff proof must fail if report diffs ignore duplicate IDs.
2280. Report-diff proof must fail if report diffs ignore reordered rows where order encodes sequence.
2281. Report-diff proof must fail if report diffs normalize away changed evidence paths.
2282. Report-diff proof must fail if report diffs omit proof_run_id and git_commit comparisons.
2283. Report-diff proof must fail if report diffs are not hashed into proof bundle.
2284. Report-diff proof must fail if report-diff generator reads stale previous diff files.
2285. Execution-plan proof must generate a plan of proof steps before executing them.
2286. Execution-plan proof must fail if executed commands differ from the precomputed plan without recording a plan update.
2287. Execution-plan proof must fail if the plan omits required validators, attacks, scenarios, replay steps, or final gates.
2288. Execution-plan proof must fail if a proof step executes out of order relative to declared dependencies.
2289. Execution-plan proof must fail if a step not in the plan writes final artifacts.
2290. Execution-plan proof must fail if skipped planned steps do not block final PASS.
2291. Execution-plan proof must fail if the plan is generated from successful outputs after execution.
2292. Execution-plan proof must fail if plan hash is absent from final verdict.
2293. Execution-plan proof must fail if detached verification cannot compare transcript to plan.
2294. Execution-plan proof must fail if plan changes are not represented in audit log.
2295. Run-summary proof must provide a concise machine-readable summary of commands run, reports generated, validators passed, validators failed, attacks tested, attacks rejected, scenarios executed, and final classification.
2296. Run-summary proof must fail if summary counts disagree with detailed reports.
2297. Run-summary proof must fail if summary omits failed or skipped items.
2298. Run-summary proof must fail if summary is generated before all detailed reports are finalized.
2299. Run-summary proof must fail if summary can be edited without invalidating final bundle hash.
2300. Run-summary proof must fail if summary uses human-friendly labels instead of exact enum values.
2301. Run-summary proof must fail if summary omits proof_run_id, git_commit, proof contract version, and detached verification result.
2302. Run-summary proof must fail if summary says all checks passed while any validator report contains warnings on mandatory checks.
2303. Run-summary proof must fail if summary includes reports from historical proof runs.
2304. Run-summary proof must fail if summary is used as evidence without linking to detailed evidence.
2305. Proof namespacing proof must define a unique namespace for every report, artifact, scenario, validator, requirement, corrective item, and attack.
2306. Proof namespacing proof must fail if two object types reuse the same bare ID and can be confused without type prefix.
2307. Proof namespacing proof must fail if IDs are only unique within one report but not globally unique where cross-report references are used.
2308. Proof namespacing proof must fail if an evidence_id can refer to both a file and a command result.
2309. Proof namespacing proof must fail if scenario_id and replay_id are interchangeable without explicit relationship metadata.
2310. Proof namespacing proof must fail if attack_id values collide with validator_id or requirement_id values.
2311. Proof namespacing proof must fail if generated IDs depend on list position rather than stable semantic identity.
2312. Proof namespacing proof must fail if ID prefixes are documented but not enforced by schema validation.
2313. Proof namespacing proof must fail if cross-report references omit object type.
2314. Proof namespacing proof must fail if renamed objects lose traceability to their prior IDs.
2315. Reference-integrity proof must validate every cross-report reference as a typed edge.
2316. Reference-integrity proof must fail if a report references an object that exists only in markdown.
2317. Reference-integrity proof must fail if a reference points to a deprecated object without historical marker.
2318. Reference-integrity proof must fail if a reference points to an object from a different proof contract version.
2319. Reference-integrity proof must fail if a reference points to an object with mismatched proof_run_id.
2320. Reference-integrity proof must fail if references are validated only by string containment.
2321. Reference-integrity proof must fail if an object is renamed but reverse references are not updated.
2322. Reference-integrity proof must fail if required reverse references are missing.
2323. Reference-integrity proof must fail if dangling references are downgraded to warnings.
2324. Reference-integrity proof must fail if reference validation is not part of detached verification.
2325. Canonical-status proof must define one canonical enum set for verdict, status, classification, resolution, severity, and evidence strength.
2326. Canonical-status proof must fail if different reports use semantically equivalent but different status values.
2327. Canonical-status proof must fail if status aliases such as OK, GOOD, TRUE, SUCCESS, or COMPLETE are accepted where PASS is required.
2328. Canonical-status proof must fail if failure statuses are collapsed into nonblocking informational statuses.
2329. Canonical-status proof must fail if BLOCKED and OUT_OF_SCOPE are treated the same.
2330. Canonical-status proof must fail if DENIED_AND_RECORDED is treated as PASS outside the unsafe scenario context.
2331. Canonical-status proof must fail if FIXED is accepted without evidence while BENIGN or OUT_OF_SCOPE require rationale.
2332. Canonical-status proof must fail if UNKNOWN can be promoted to PASS by aggregation.
2333. Canonical-status proof must fail if optional status values appear in mandatory proof rows.
2334. Canonical-status proof must fail if status transition rules are absent.
2335. Status-transition proof must define allowed transitions between OPEN, FIXED, BENIGN, OUT_OF_SCOPE, BLOCKED, PASS, FAIL, WARNING, and UNKNOWN.
2336. Status-transition proof must fail if OPEN can transition directly to PASS without closure evidence.
2337. Status-transition proof must fail if FAIL can transition to PASS without new evidence after the failure timestamp.
2338. Status-transition proof must fail if BLOCKED can transition to PASS without resolving the blocker.
2339. Status-transition proof must fail if OUT_OF_SCOPE can transition to PASS without being moved into scope and tested.
2340. Status-transition proof must fail if WARNING can transition to PASS without proof-critical warning analysis.
2341. Status-transition proof must fail if UNKNOWN can transition to BENIGN without rationale.
2342. Status-transition proof must fail if status transitions are inferred from latest row only and old failure row is lost.
2343. Status-transition proof must fail if status transition history is not included in final proof artifacts.
2344. Status-transition proof must fail if transition validator is not included in final gate.
2345. Decision-table proof must define a decision table for final classification based on all proof families.
2346. Decision-table proof must fail if a classification outcome is produced by ad hoc code not represented in the table.
2347. Decision-table proof must fail if any row of the decision table is unreachable or untested.
2348. Decision-table proof must fail if any required failure combination lacks a defined classification outcome.
2349. Decision-table proof must fail if the table allows FUNCTIONAL_RUNTIME_MVP with any mandatory failure.
2350. Decision-table proof must fail if the table treats infrastructure failure as proof success.
2351. Decision-table proof must fail if the table ignores anti-false-PASS weak rejections.
2352. Decision-table proof must fail if the table ignores unresolved corrective-list items.
2353. Decision-table proof must fail if decision-table version is absent from final verdict.
2354. Decision-table proof must fail if detached verification uses a different decision table than final gate.
2355. Proof-policy proof must define policy-as-code rules for final proof acceptance.
2356. Proof-policy proof must fail if proof acceptance rules are implemented only as scattered if-statements with no inspectable policy.
2357. Proof-policy proof must fail if proof policy can be changed by generated reports.
2358. Proof-policy proof must fail if proof policy is not hashed and versioned.
2359. Proof-policy proof must fail if proof policy permits manual override of mandatory failures.
2360. Proof-policy proof must fail if proof policy omits required proof families.
2361. Proof-policy proof must fail if proof policy accepts unknown report types.
2362. Proof-policy proof must fail if proof policy differs between generation and detached verification.
2363. Proof-policy proof must fail if proof policy has no negative tests.
2364. Proof-policy proof must fail if final verdict omits proof policy ID and hash.
2365. Source-to-proof drift proof must compare source code comments, README claims, requirement docs, and final proof reports for contradictory MVP statements.
2366. Source-to-proof drift proof must fail if source comments mark code as stub while proof claims implementation complete.
2367. Source-to-proof drift proof must fail if README says a validator is required but proof contract omits it.
2368. Source-to-proof drift proof must fail if docs say replay is deterministic but replay evidence allows nondeterminism.
2369. Source-to-proof drift proof must fail if docs say no forced PASS but generators contain forced-pass code paths.
2370. Source-to-proof drift proof must fail if docs say clean checkout proof is supported but proof target depends on generated artifacts.
2371. Source-to-proof drift proof must fail if docs say CI proof exists but no CI evidence artifact is present.
2372. Source-to-proof drift proof must fail if final proof claims lower scope than docs advertise without updating docs.
2373. Source-to-proof drift proof must fail if source TODOs contradict final PASS claims.
2374. Source-to-proof drift proof must fail if drift report is not included in final proof hashes.
2375. Proof-linter proof must lint proof artifacts themselves for forbidden phrases, placeholders, weak statuses, stale markers, and TODOs.
2376. Proof-linter proof must fail if final reports contain TBD, TODO, FIXME, PLACEHOLDER, TEMP, MOCK, FAKE, DEMO, or FORCE_PASS in proof-critical fields.
2377. Proof-linter proof must fail if final reports contain phrases such as assume, probably, should, maybe, best effort, or manual check required in proof-critical rows.
2378. Proof-linter proof must fail if final reports contain empty rationale fields.
2379. Proof-linter proof must fail if proof-critical fields contain human prose where structured values are required.
2380. Proof-linter proof must fail if evidence_refs contain ellipses, wildcard paths, or abbreviated hashes.
2381. Proof-linter proof must fail if command fields contain shell comments that hide skipped commands.
2382. Proof-linter proof must fail if markdown contains unchecked checkboxes for mandatory items.
2383. Proof-linter proof must fail if markdown contains checked boxes not backed by JSON PASS rows.
2384. Proof-linter proof must fail if lint findings are not linked to affected reports.
2385. Runtime-contract proof must define preconditions, postconditions, and side effects for each runtime action exercised by MVP scenarios.
2386. Runtime-contract proof must fail if an action can execute without satisfying its preconditions.
2387. Runtime-contract proof must fail if an action returns success without satisfying postconditions.
2388. Runtime-contract proof must fail if action side effects differ from contract.
2389. Runtime-contract proof must fail if contract validation is disabled in proof mode.
2390. Runtime-contract proof must fail if contracts are checked only in tests and not runtime entrypoints.
2391. Runtime-contract proof must fail if postcondition failures are logged but not propagated.
2392. Runtime-contract proof must fail if contract schemas are generated from implementation return values.
2393. Runtime-contract proof must fail if replay skips contract checks.
2394. Runtime-contract proof must fail if contracts are not mapped to traceability requirements.
2395. Proof-contract-change proof must require a changelog entry for every proof contract modification.
2396. Proof-contract-change proof must fail if contract changes weaken validation without explicit migration and rationale.
2397. Proof-contract-change proof must fail if contract changes add required reports without adding validators.
2398. Proof-contract-change proof must fail if contract changes add validators without adding negative tests.
2399. Proof-contract-change proof must fail if contract changes remove anti-false-PASS attacks without replacement.
2400. Proof-contract-change proof must fail if contract changes alter classification rules without decision-table update.
2401. Proof-contract-change proof must fail if old proofs are not invalidated when new mandatory criteria are added.
2402. Proof-contract-change proof must fail if contract version is bumped but generated reports still cite the old version.
2403. Proof-contract-change proof must fail if contract diff is not included in final artifacts.
2404. Proof-contract-change proof must fail if contract changes are not reflected in documentation.
2405. Evidence-confidentiality proof must classify proof artifacts by public, internal, sensitive, or secret.
2406. Evidence-confidentiality proof must fail if sensitive artifacts are included in public bundles without redaction policy.
2407. Evidence-confidentiality proof must fail if secret artifacts are required for detached verification of public proof.
2408. Evidence-confidentiality proof must fail if redacted artifacts cannot be linked to unredacted hashes for internal verification.
2409. Evidence-confidentiality proof must fail if confidentiality classification is missing for any artifact.
2410. Evidence-confidentiality proof must fail if artifact classification conflicts with path or bundle destination.
2411. Evidence-confidentiality proof must fail if command logs include sensitive paths or values in public mode.
2412. Evidence-confidentiality proof must fail if public final verdict links to non-public evidence without access policy.
2413. Evidence-confidentiality proof must fail if redaction is applied after hashing without a documented two-hash model.
2414. Evidence-confidentiality proof must fail if confidentiality rules are not validated in CI or detached verification.
2415. Evidence-portability proof must prove that the final proof bundle can be moved to a different directory and verified.
2416. Evidence-portability proof must fail if absolute paths are required for detached verification.
2417. Evidence-portability proof must fail if symlink targets break after relocation.
2418. Evidence-portability proof must fail if source-relative paths are resolved against current cwd instead of bundle root.
2419. Evidence-portability proof must fail if path separators are not normalized within the bundle manifest.
2420. Evidence-portability proof must fail if bundle verification depends on the original repository being present unless explicitly declared.
2421. Evidence-portability proof must fail if external source references lack commit and hash metadata.
2422. Evidence-portability proof must fail if moved bundle cannot locate logs, manifests, or evidence files.
2423. Evidence-portability proof must fail if bundle root is inferred incorrectly when nested directories exist.
2424. Evidence-portability proof must fail if portability test is absent from final proof target.
2425. Proof-compression proof must define whether compressed proof bundles are authoritative or derived.
2426. Proof-compression proof must fail if compressed bundle and uncompressed artifact index differ.
2427. Proof-compression proof must fail if compressed bundle omits file permissions required for executable verification scripts.
2428. Proof-compression proof must fail if compressed bundle includes files not present in artifact index.
2429. Proof-compression proof must fail if extraction order affects verification.
2430. Proof-compression proof must fail if archive timestamps introduce nondeterministic bundle hashes without normalization.
2431. Proof-compression proof must fail if compressed bundle is created before final artifact index.
2432. Proof-compression proof must fail if compressed bundle hash is absent from attestation.
2433. Proof-compression proof must fail if compressed bundle can be overwritten by later proof steps.
2434. Proof-compression proof must fail if compressed bundle is treated as source evidence for its own contents.
2435. Longitudinal-proof proof must track classification changes across repeated commits or proof runs when claiming progress.
2436. Longitudinal-proof proof must fail if progress claims omit prior failing classifications.
2437. Longitudinal-proof proof must fail if current PASS ignores a regression from a prior PASS on another proof family.
2438. Longitudinal-proof proof must fail if trend data is generated from inconsistent proof contracts.
2439. Longitudinal-proof proof must fail if historical proof records can be edited without detection.
2440. Longitudinal-proof proof must fail if historical proof records are confused with current proof evidence.
2441. Longitudinal-proof proof must fail if progress metrics count added reports but not removed failures.
2442. Longitudinal-proof proof must fail if longitudinal summary omits commit hashes.
2443. Longitudinal-proof proof must fail if repeated proof runs with same commit produce different classifications and no flakiness record.
2444. Longitudinal-proof proof must fail if release claims use the best historical run rather than the current verified run.
2445. User-prompt trace proof must record the exact user acceptance prompt or corrective list that drove the proof criteria.
2446. User-prompt trace proof must fail if the implemented criteria omit any user-specified mandatory item.
2447. User-prompt trace proof must fail if implementation broadens or narrows user criteria without a recorded decision.
2448. User-prompt trace proof must fail if user criteria are converted into vague summary requirements.
2449. User-prompt trace proof must fail if prompt-derived requirements are not assigned stable IDs.
2450. User-prompt trace proof must fail if prompt-derived requirements cannot be traced to source, tests, validators, and evidence.
2451. User-prompt trace proof must fail if a newer user prompt supersedes older criteria but old criteria remain final without migration.
2452. User-prompt trace proof must fail if final proof cites a prompt file that is not included in source manifest or proof bundle.
2453. User-prompt trace proof must fail if prompt file hash differs from hash recorded in final verdict.
2454. User-prompt trace proof must fail if prompt-derived criteria are treated as optional when user marked them mandatory.
2455. Implementation-readiness proof must distinguish code present, code wired, code executed, code validated, and code release-ready.
2456. Implementation-readiness proof must fail if code present is counted as code executed.
2457. Implementation-readiness proof must fail if code wired is counted as code validated.
2458. Implementation-readiness proof must fail if code executed is counted as release-ready without validation and evidence.
2459. Implementation-readiness proof must fail if readiness levels differ across reports.
2460. Implementation-readiness proof must fail if a component marked ready has no runtime scenario or validator evidence.
2461. Implementation-readiness proof must fail if a component marked ready is excluded from final gate.
2462. Implementation-readiness proof must fail if readiness is inferred from file existence.
2463. Implementation-readiness proof must fail if readiness is set by generator input rather than computed evidence.
2464. Implementation-readiness proof must fail if final classification ignores non-ready mandatory components.
2465. Model-adapter proof must define whether model calls are in scope for Functional Runtime MVP behavior.
2466. Model-adapter proof must fail if model outputs can directly approve, promote, or mark proof artifacts PASS.
2467. Model-adapter proof must fail if model outputs are not captured with prompt hash, model identifier, parameters, and response hash when they affect runtime decisions.
2468. Model-adapter proof must fail if model nondeterminism can change final runtime scenario verdicts without deterministic replay controls.
2469. Model-adapter proof must fail if model errors, refusal, timeout, or malformed output default to ALLOW, PASS, or FIXED.
2470. Model-adapter proof must fail if model output schema is not validated before use.
2471. Model-adapter proof must fail if model output can choose which validators, policies, or invariants run.
2472. Model-adapter proof must fail if model output can modify the proof contract, final gate, or detached verifier.
2473. Model-adapter proof must fail if model-generated code is trusted before sandboxing, review, and promotion-gate validation.
2474. Model-adapter proof must fail if model call transcripts are excluded from evidence when model decisions affect behavior.
2475. Tool-call proof must define every tool-call permission available to generated agents and proof scripts.
2476. Tool-call proof must fail if generated agents can call filesystem, shell, network, git, or package-management tools outside their declared capability set.
2477. Tool-call proof must fail if tool-call arguments are not recorded and hashed for proof-critical operations.
2478. Tool-call proof must fail if tool-call results are accepted without schema validation and exit/status checking.
2479. Tool-call proof must fail if a failed tool call can be retried with broader permissions without evidence.
2480. Tool-call proof must fail if a tool call can write to source, proof reports, validators, or schemas without explicit authorization.
2481. Tool-call proof must fail if tool-call allowlists differ between tests and runtime scenarios.
2482. Tool-call proof must fail if tool-call denials are logged but do not block the attempted operation.
2483. Tool-call proof must fail if tool-call provenance is omitted from event log and state records.
2484. Tool-call proof must fail if tool-call side effects are not included in side-effect and file-watch reports.
2485. Network-egress proof must define whether network access is allowed during runtime scenarios, replay, validation, proof generation, CI, and detached verification.
2486. Network-egress proof must fail if any proof-critical command opens network connections when network is declared out of scope.
2487. Network-egress proof must fail if network responses can change proof verdicts without being recorded and hashed.
2488. Network-egress proof must fail if external APIs are used as proof evidence without stable captured fixtures.
2489. Network-egress proof must fail if DNS, proxies, mirrors, or package indexes affect proof behavior without metadata.
2490. Network-egress proof must fail if network failure defaults to PASS or skipped proof.
2491. Network-egress proof must fail if replay accesses network instead of recorded evidence.
2492. Network-egress proof must fail if detached verification accesses network.
2493. Network-egress proof must fail if CI proof uses network after dependency setup without declared reason.
2494. Network-egress proof must fail if network credentials or endpoints leak into public proof artifacts.
2495. MCP-adapter proof must define every MCP or external connector used by the agent.
2496. MCP-adapter proof must fail if MCP tool schemas are not versioned and hashed.
2497. MCP-adapter proof must fail if MCP tool responses are accepted without validating against the declared schema.
2498. MCP-adapter proof must fail if MCP tool availability changes final proof outcome.
2499. MCP-adapter proof must fail if MCP tools can mutate repository state outside the proof contract.
2500. MCP-adapter proof must fail if MCP tool errors are converted to empty successful responses.
2501. MCP-adapter proof must fail if generated agents can request arbitrary MCP tools not in their capability set.
2502. MCP-adapter proof must fail if MCP tool calls are omitted from event log and command transcript where proof-critical.
2503. MCP-adapter proof must fail if MCP response provenance is missing.
2504. MCP-adapter proof must fail if MCP tool sandbox differs between test and runtime paths.
2505. Task-queue proof must define how queued tasks are created, claimed, executed, retried, completed, failed, or cancelled.
2506. Task-queue proof must fail if two workers can claim the same proof-critical task without idempotency protection.
2507. Task-queue proof must fail if task retry creates duplicate promotions, approvals, artifacts, state records, or events.
2508. Task-queue proof must fail if task failure is acknowledged as success.
2509. Task-queue proof must fail if task ordering affects safety decisions without recorded ordering rules.
2510. Task-queue proof must fail if queued actions can bypass review or promotion gate.
2511. Task-queue proof must fail if stale tasks from prior proof_run_id execute in the current proof run.
2512. Task-queue proof must fail if task payload hashes are not recorded.
2513. Task-queue proof must fail if task cancellation deletes required audit evidence.
2514. Task-queue proof must fail if queue state is not exported and hashed for scenario evidence.
2515. Scheduler proof must define whether scheduled or background jobs are allowed during proof runs.
2516. Scheduler proof must fail if background jobs modify reports, artifacts, state, or source while proof commands run.
2517. Scheduler proof must fail if scheduled cleanup removes current evidence.
2518. Scheduler proof must fail if scheduled retry changes final verdict after final validation.
2519. Scheduler proof must fail if background jobs are not stopped or isolated for deterministic proof.
2520. Scheduler proof must fail if scheduled tasks lack proof_run_id scoping.
2521. Scheduler proof must fail if a scheduled job can promote artifacts without final gate.
2522. Scheduler proof must fail if scheduled jobs are excluded from event and audit logs.
2523. Scheduler proof must fail if scheduler state is not included in scenario evidence when scheduler behavior is in scope.
2524. Scheduler proof must fail if scheduler configuration differs between local proof and CI proof.
2525. Monitoring proof must define which runtime metrics, alerts, and health checks are in scope for the MVP.
2526. Monitoring proof must fail if health checks report healthy while required subsystems are disabled.
2527. Monitoring proof must fail if monitoring evidence is generated but not tied to proof_run_id.
2528. Monitoring proof must fail if alerts are only logged in prose and not machine-readable.
2529. Monitoring proof must fail if safety-critical alert failures are ignored by final classification.
2530. Monitoring proof must fail if metrics are scraped from stale state.
2531. Monitoring proof must fail if monitoring side effects alter runtime behavior.
2532. Monitoring proof must fail if monitoring reports PASS based on process liveness alone.
2533. Monitoring proof must fail if monitoring configuration is absent from proof metadata.
2534. Monitoring proof must fail if monitoring claims are made in documentation but not represented in final verdict scope.
2535. Observability proof must define required logs, traces, metrics, and state snapshots for each proof-critical scenario.
2536. Observability proof must fail if a scenario verdict cannot be explained from captured observability artifacts.
2537. Observability proof must fail if traces omit policy, capability, invariant, gate, artifact, state, or event spans.
2538. Observability proof must fail if trace IDs are missing from linked logs and reports.
2539. Observability proof must fail if log correlation IDs do not match scenario_id and action_id.
2540. Observability proof must fail if observability artifacts are sampled and omit proof-critical events.
2541. Observability proof must fail if observability artifacts are not hashed into the proof bundle.
2542. Observability proof must fail if sensitive data redaction removes traceability fields.
2543. Observability proof must fail if logs are only available in external systems and not exported for detached verification.
2544. Observability proof must fail if observability is claimed but not tested with a negative scenario.
2545. Backup-restore proof must define whether backup and disaster recovery are in scope for current MVP.
2546. Backup-restore proof must fail if backup claims are made without creating and validating a backup artifact.
2547. Backup-restore proof must fail if restored proof artifacts fail detached verification.
2548. Backup-restore proof must fail if restore loses hashes, proof_run_id, git_commit, or artifact index metadata.
2549. Backup-restore proof must fail if backup includes stale artifacts as current evidence.
2550. Backup-restore proof must fail if backup excludes evidence required to verify final verdict.
2551. Backup-restore proof must fail if backup restoration changes file paths in a way that breaks proof portability.
2552. Backup-restore proof must fail if backup can overwrite current proof artifacts without confirmation and hashes.
2553. Backup-restore proof must fail if backup encryption or compression prevents verification without documented keys or process.
2554. Backup-restore proof must fail if disaster-recovery docs claim support but no restore test exists.
2555. Promotion-channel proof must define all paths by which an artifact can become active or promoted.
2556. Promotion-channel proof must fail if any promotion path bypasses policy, capability, invariant, review, or final gate.
2557. Promotion-channel proof must fail if direct filesystem placement can activate an artifact.
2558. Promotion-channel proof must fail if a package install, symlink update, config change, or cache update can promote artifacts outside the promotion gate.
2559. Promotion-channel proof must fail if promotion through CLI and API paths produce different evidence.
2560. Promotion-channel proof must fail if promotion status can be changed by editing metadata only.
2561. Promotion-channel proof must fail if promotion event is written before approval evidence is durable.
2562. Promotion-channel proof must fail if promotion is possible during replay or detached verification.
2563. Promotion-channel proof must fail if promotion rollback does not remove active references.
2564. Promotion-channel proof must fail if all promotion channels are not represented in threat model and tests.
2565. Human-review workflow proof must define review states, reviewer roles, decision evidence, and escalation paths.
2566. Human-review workflow proof must fail if a review can be approved without viewing the exact artifact hash.
2567. Human-review workflow proof must fail if review request content differs from artifact content later promoted.
2568. Human-review workflow proof must fail if review comments can be edited after approval without history.
2569. Human-review workflow proof must fail if a rejected review can be converted to approved without new review event.
2570. Human-review workflow proof must fail if review state is stored only in memory.
2571. Human-review workflow proof must fail if review evidence is not included in replay and scenario proof.
2572. Human-review workflow proof must fail if review approval is not linked to policy and invariant state at approval time.
2573. Human-review workflow proof must fail if reviewer conflict-of-interest rules are absent where separation is required.
2574. Human-review workflow proof must fail if human-review bypass is possible under proof mode.
2575. Git-integration proof must define all git operations allowed during runtime and proof.
2576. Git-integration proof must fail if runtime scenarios can commit, branch, tag, merge, rebase, or push without explicit capability and audit evidence.
2577. Git-integration proof must fail if proof generation changes git index or working tree source files.
2578. Git-integration proof must fail if git status checks ignore staged changes.
2579. Git-integration proof must fail if git status checks ignore untracked files that affect proof.
2580. Git-integration proof must fail if proof uses git commit hash but not tree hash.
2581. Git-integration proof must fail if git operations are mocked while proof claims real git safety.
2582. Git-integration proof must fail if git hooks alter proof behavior without being declared.
2583. Git-integration proof must fail if submodule or worktree state is omitted.
2584. Git-integration proof must fail if generated reports are committed and later treated as fresh runtime evidence.
2585. Patch-application proof must define how patches are proposed, validated, applied, rejected, reverted, and audited.
2586. Patch-application proof must fail if a patch can be applied without sandbox validation.
2587. Patch-application proof must fail if a patch can modify proof validators before validation without meta-validation.
2588. Patch-application proof must fail if a patch can modify its own review criteria.
2589. Patch-application proof must fail if patch application changes files outside allowed paths.
2590. Patch-application proof must fail if patch conflict resolution is manual and unrecorded.
2591. Patch-application proof must fail if rejected patches leave modified files.
2592. Patch-application proof must fail if patch rollback does not restore exact prior hashes.
2593. Patch-application proof must fail if patch diff is absent from evidence.
2594. Patch-application proof must fail if patch validation and final validation run on different file contents.
2595. Policy-update proof must define how policy changes are proposed, reviewed, validated, and activated.
2596. Policy-update proof must fail if a generated agent can weaken policy rules without review.
2597. Policy-update proof must fail if policy changes take effect before validation.
2598. Policy-update proof must fail if policy update evidence lacks before/after hashes.
2599. Policy-update proof must fail if policy update changes unsafe scenario outcome without new tests.
2600. Policy-update proof must fail if policy update bypasses compatibility, replay, and anti-false-PASS checks.
2601. Policy-update proof must fail if old policy decisions are replayed under new policy without recording policy-version change.
2602. Policy-update proof must fail if policy rollback leaves mixed policy versions active.
2603. Policy-update proof must fail if policy version is missing from state and event records.
2604. Policy-update proof must fail if policy registry accepts duplicate or conflicting rule IDs.
2605. Capability-update proof must define how capability changes are proposed, reviewed, validated, and activated.
2606. Capability-update proof must fail if capability changes can grant generated agents broader permissions without review.
2607. Capability-update proof must fail if capability updates are not linked to actor IDs and action scopes.
2608. Capability-update proof must fail if capability change evidence lacks before/after hashes.
2609. Capability-update proof must fail if capability update changes unsafe scenario outcome without new negative tests.
2610. Capability-update proof must fail if capability rollback leaves stale grants.
2611. Capability-update proof must fail if capability registry accepts duplicate capability IDs.
2612. Capability-update proof must fail if capability updates are loaded from generated reports.
2613. Capability-update proof must fail if capability version is missing from state and event records.
2614. Capability-update proof must fail if capability updates bypass proof contract validation.
2615. Invariant-update proof must define how invariant changes are proposed, reviewed, validated, and activated.
2616. Invariant-update proof must fail if a generated agent can remove or weaken no_self_promotion invariant.
2617. Invariant-update proof must fail if invariant changes take effect before regression tests run.
2618. Invariant-update proof must fail if invariant update evidence lacks before/after hashes.
2619. Invariant-update proof must fail if invariant update changes unsafe scenario outcome without new anti-false-PASS and mutation tests.
2620. Invariant-update proof must fail if invariant rollback leaves mixed invariant versions active.
2621. Invariant-update proof must fail if invariant registry accepts duplicate invariant IDs.
2622. Invariant-update proof must fail if invariant severity can be lowered without review.
2623. Invariant-update proof must fail if invariant version is missing from state and event records.
2624. Invariant-update proof must fail if invariant changes bypass final proof gate.
2625. Runtime-profile proof must define supported local model, remote model, offline mode, and no-model mode profiles.
2626. Runtime-profile proof must fail if proof results from one profile are claimed for another profile without evidence.
2627. Runtime-profile proof must fail if remote-model behavior is used while claiming offline determinism.
2628. Runtime-profile proof must fail if local-model fallback changes safety decisions without proof.
2629. Runtime-profile proof must fail if profile selection is not recorded in final verdict.
2630. Runtime-profile proof must fail if required validators are skipped under a low-resource profile.
2631. Runtime-profile proof must fail if model or tool profile can weaken policy, capability, invariant, or promotion checks.
2632. Runtime-profile proof must fail if profile-specific limitations are absent from scope metadata.
2633. Runtime-profile proof must fail if CI profile differs from local profile without compatibility statement.
2634. Runtime-profile proof must fail if replay profile differs from original execution profile without recorded equivalence.
2635. Evaluation-harness proof must define benchmark, scenario, regression, mutation, replay, and anti-false-PASS evaluation suites.
2636. Evaluation-harness proof must fail if evaluation suites are not tied to the proof contract.
2637. Evaluation-harness proof must fail if evaluation suite discovery is implicit and can miss files.
2638. Evaluation-harness proof must fail if evaluation results are not mapped to requirements and corrective items.
2639. Evaluation-harness proof must fail if evaluation harness can suppress failing cases.
2640. Evaluation-harness proof must fail if evaluation harness treats infrastructure failures as skipped cases.
2641. Evaluation-harness proof must fail if evaluation harness caches passing outcomes across source changes.
2642. Evaluation-harness proof must fail if evaluation result aggregation omits failed suites.
2643. Evaluation-harness proof must fail if evaluation harness is not itself covered by negative tests.
2644. Evaluation-harness proof must fail if final verdict does not include evaluation suite versions and hashes.
2645. Multi-agent coordination proof must define how multiple generated agents, reviewers, workers, and orchestrators coordinate on one action.
2646. Multi-agent coordination proof must fail if two agents can approve each other's unsafe promotions without independent human review.
2647. Multi-agent coordination proof must fail if colluding generated agents can bypass no_self_promotion by cross-approval.
2648. Multi-agent coordination proof must fail if one generated agent can modify another generated agent's review evidence.
2649. Multi-agent coordination proof must fail if orchestrator decisions are not recorded as distinct from worker decisions.
2650. Multi-agent coordination proof must fail if agent identity, parentage, and delegation chain are missing from events.
2651. Multi-agent coordination proof must fail if delegated actions exceed the delegating agent's capability scope.
2652. Multi-agent coordination proof must fail if worker failure is hidden by orchestrator success.
2653. Multi-agent coordination proof must fail if agent handoff loses action_id, artifact_hash, policy_version, or proof_run_id.
2654. Multi-agent coordination proof must fail if concurrent agent actions produce nondeterministic final promotion state.
2655. Delegation proof must define which actors may delegate which permissions to which agents.
2656. Delegation proof must fail if a generated agent can delegate approval authority to itself directly or indirectly.
2657. Delegation proof must fail if delegated capability lacks scope, expiry, issuer, subject, and evidence hash.
2658. Delegation proof must fail if delegated permissions survive rollback or denial without rationale.
2659. Delegation proof must fail if delegation is recorded after the delegated action executes.
2660. Delegation proof must fail if delegation can be edited without invalidating dependent approvals.
2661. Delegation proof must fail if delegated authority is not checked by the promotion gate.
2662. Delegation proof must fail if replay reconstructs delegated authority from current configuration instead of recorded evidence.
2663. Delegation proof must fail if expired or revoked delegation authorizes proof-critical actions.
2664. Delegation proof must fail if delegation chain contains cycles.
2665. Consensus proof must define whether any quorum or multi-reviewer decision is required.
2666. Consensus proof must fail if quorum is claimed but reviewer count is not recorded.
2667. Consensus proof must fail if duplicate reviewer identities are counted as independent reviewers.
2668. Consensus proof must fail if reviewer aliases inflate quorum.
2669. Consensus proof must fail if one reviewer decision is counted multiple times through retries or duplicate records.
2670. Consensus proof must fail if rejected votes are omitted from the decision record.
2671. Consensus proof must fail if quorum rules differ between runtime and replay.
2672. Consensus proof must fail if quorum can be satisfied by generated agents when human reviewers are required.
2673. Consensus proof must fail if quorum evidence is stored only in markdown.
2674. Consensus proof must fail if quorum rule changes after review but before promotion.
2675. Distributed-lock proof must define lock identity, owner, lease duration, renewal, and release behavior for proof-critical shared resources.
2676. Distributed-lock proof must fail if lock expiry can allow two writers to finalize the same report or artifact.
2677. Distributed-lock proof must fail if stale lock cleanup can remove another active run's lock.
2678. Distributed-lock proof must fail if lock acquisition is not recorded in audit log.
2679. Distributed-lock proof must fail if proof continues after failing to acquire a required lock.
2680. Distributed-lock proof must fail if lock owner identity is missing from final evidence.
2681. Distributed-lock proof must fail if lock release is recorded before all protected writes complete.
2682. Distributed-lock proof must fail if lock evidence is not tied to proof_run_id.
2683. Distributed-lock proof must fail if lock behavior differs between local filesystem and CI filesystem.
2684. Distributed-lock proof must fail if lock files are treated as proof evidence.
2685. Checkpoint proof must define whether proof execution can resume from checkpoints.
2686. Checkpoint proof must fail if checkpoint resume skips required validators, replay, or anti-false-PASS attacks.
2687. Checkpoint proof must fail if checkpoints are reused after source, dependency, schema, or proof contract changes.
2688. Checkpoint proof must fail if checkpoint state lacks proof_run_id and input hashes.
2689. Checkpoint proof must fail if checkpoint resume hides earlier failed commands.
2690. Checkpoint proof must fail if checkpoint data is accepted without hash validation.
2691. Checkpoint proof must fail if checkpoint resume changes final report order or hashes without rationale.
2692. Checkpoint proof must fail if checkpoint artifacts are included as current evidence without regeneration or validation.
2693. Checkpoint proof must fail if failed checkpoint recovery leaves stale PASS reports.
2694. Checkpoint proof must fail if detached verification requires checkpoint state.
2695. Incremental-proof proof must define which proof steps may be reused incrementally and the invalidation rules for each.
2696. Incremental-proof proof must fail if incremental reuse bypasses mandatory full-run checks.
2697. Incremental-proof proof must fail if dependency graph changes do not invalidate affected proof steps.
2698. Incremental-proof proof must fail if source changes outside declared dependencies affect reused evidence.
2699. Incremental-proof proof must fail if reused validator results predate the reports they validate.
2700. Incremental-proof proof must fail if reused test results predate source or fixture changes.
2701. Incremental-proof proof must fail if reused replay results predate scenario manifest changes.
2702. Incremental-proof proof must fail if reused anti-false-PASS audit results predate validator changes.
2703. Incremental-proof proof must fail if final verdict does not clearly mark which evidence was reused and why.
2704. Incremental-proof proof must fail if detached verification cannot independently validate reuse eligibility.
2705. Backfill proof must define whether historical reports may be backfilled into new schemas.
2706. Backfill proof must fail if backfilled reports are treated as current-run generated reports.
2707. Backfill proof must fail if backfill converts missing fields into default PASS values.
2708. Backfill proof must fail if backfill omits original report hash and backfill command evidence.
2709. Backfill proof must fail if backfilled evidence predates requirements that did not exist at the time.
2710. Backfill proof must fail if backfill changes classification without running current validators.
2711. Backfill proof must fail if backfill is not marked historical.
2712. Backfill proof must fail if backfill output can satisfy current FUNCTIONAL_RUNTIME_MVP gate.
2713. Backfill proof must fail if backfill migration has no negative tests.
2714. Backfill proof must fail if backfill source reports are not preserved.
2715. Evidence-expiry proof must define whether evidence expires based on time, source changes, dependency changes, or contract changes.
2716. Evidence-expiry proof must fail if expired evidence is used in final PASS.
2717. Evidence-expiry proof must fail if expiry rules are absent for reused evidence.
2718. Evidence-expiry proof must fail if expiry timestamp is missing for time-limited evidence.
2719. Evidence-expiry proof must fail if expiry is checked against local timezone inconsistently.
2720. Evidence-expiry proof must fail if expiry can be extended by editing report metadata.
2721. Evidence-expiry proof must fail if expired approvals remain valid for promotion.
2722. Evidence-expiry proof must fail if expired waivers remain valid for final classification.
2723. Evidence-expiry proof must fail if expired dependency scans are used for release claims.
2724. Evidence-expiry proof must fail if detached verification does not enforce expiry rules.
2725. Partial-order proof must model proof steps as a partial order where independent steps may vary but dependencies cannot be violated.
2726. Partial-order proof must fail if transcript order violates required dependencies.
2727. Partial-order proof must fail if timestamp order contradicts dependency order.
2728. Partial-order proof must fail if report generation occurs before its input evidence exists.
2729. Partial-order proof must fail if validation occurs before report finalization.
2730. Partial-order proof must fail if replay occurs before original scenario evidence is exported.
2731. Partial-order proof must fail if final gate runs before detached verification when detached verification is required as input.
2732. Partial-order proof must fail if a proof step is repeated and downstream reports cite the earlier stale run.
2733. Partial-order proof must fail if order validation is absent from detached verification.
2734. Partial-order proof must fail if proof dependencies are inferred only from filenames.
2735. Hermeticity proof must define which inputs are allowed to influence proof outputs.
2736. Hermeticity proof must fail if undeclared files influence proof outputs.
2737. Hermeticity proof must fail if current date, hostname, username, random device, network, or global cache influences semantic verdict.
2738. Hermeticity proof must fail if proof command reads files outside declared source, fixture, dependency, and proof output roots.
2739. Hermeticity proof must fail if installed global packages shadow declared dependencies.
2740. Hermeticity proof must fail if shell rc files, git config, editor config, or user dotfiles alter proof behavior.
2741. Hermeticity proof must fail if proof outputs differ after moving the repository path except for declared path metadata.
2742. Hermeticity proof must fail if proof outputs differ after clearing caches that should not affect semantics.
2743. Hermeticity proof must fail if proof cannot declare its complete input set.
2744. Hermeticity proof must fail if hermeticity is claimed but not tested in a clean environment.
2745. Golden-negative proof must maintain a catalog of invalid proof bundles that must always fail verification.
2746. Golden-negative proof must fail if any invalid bundle passes detached verification.
2747. Golden-negative proof must fail if invalid bundle expected failure reasons are missing.
2748. Golden-negative proof must fail if invalid bundles are generated from current validators rather than hand-audited adversarial cases.
2749. Golden-negative proof must fail if invalid bundles do not cover path confusion, stale hashes, mixed runs, forged transcript, forged replay, weak rejection, and forced PASS.
2750. Golden-negative proof must fail if invalid bundles use deprecated schemas only.
2751. Golden-negative proof must fail if invalid bundles are not versioned with the proof contract.
2752. Golden-negative proof must fail if invalid bundle tests are not run in final proof.
2753. Golden-negative proof must fail if invalid bundle failures are counted as infrastructure failures instead of expected validation failures.
2754. Golden-negative proof must fail if invalid bundle corpus is not included in source manifest.
2755. Final-report immutability proof must freeze final report files after finalization.
2756. Final-report immutability proof must fail if reports are modified after being marked finalized.
2757. Final-report immutability proof must fail if filesystem permissions allow routine proof steps to overwrite finalized reports.
2758. Final-report immutability proof must fail if finalization marker can be edited without hash mismatch.
2759. Final-report immutability proof must fail if finalized reports are regenerated without creating a new proof_run_id or validation record.
2760. Final-report immutability proof must fail if markdown regeneration changes finalized JSON reports.
2761. Final-report immutability proof must fail if final report immutability is not checked by detached verifier.
2762. Final-report immutability proof must fail if final bundle archive can be modified after attestation.
2763. Final-report immutability proof must fail if immutable status is stored only in filename.
2764. Final-report immutability proof must fail if failure cleanup rewrites finalized reports.
2765. Evidence-availability proof must classify every referenced artifact as embedded, bundled, source-tracked, external, or unavailable.
2766. Evidence-availability proof must fail if unavailable evidence supports any PASS claim.
2767. Evidence-availability proof must fail if external evidence lacks URL, hash, retrieval timestamp, and local copy where required.
2768. Evidence-availability proof must fail if source-tracked evidence is not present at the recorded commit.
2769. Evidence-availability proof must fail if bundled evidence is missing from the artifact index.
2770. Evidence-availability proof must fail if embedded evidence is truncated without full artifact hash.
2771. Evidence-availability proof must fail if logs needed for transcript verification are external-only.
2772. Evidence-availability proof must fail if CI artifacts expire before release evidence retention requirements.
2773. Evidence-availability proof must fail if detached verifier cannot access all required evidence offline.
2774. Evidence-availability proof must fail if evidence availability differs between public and internal bundles without classification.
2775. Semantic-version proof must define semantic versioning rules for proof contracts, schemas, validators, and report formats.
2776. Semantic-version proof must fail if breaking proof-contract changes do not increment major version.
2777. Semantic-version proof must fail if non-breaking changes alter final gate behavior.
2778. Semantic-version proof must fail if report schema version and proof contract version are incompatible.
2779. Semantic-version proof must fail if validators accept future major versions silently.
2780. Semantic-version proof must fail if generators emit pre-release schema versions for final MVP proof without explicit allowance.
2781. Semantic-version proof must fail if version constraints are parsed lexically rather than semantically.
2782. Semantic-version proof must fail if final verdict omits version compatibility matrix.
2783. Semantic-version proof must fail if docs cite a version range broader than tested versions.
2784. Semantic-version proof must fail if migration tests do not cover version boundary cases.
2785. Runtime-fidelity proof must define how closely scenario tests must match real runtime usage.
2786. Runtime-fidelity proof must fail if scenario tests bypass CLI/API layers that real users must use.
2787. Runtime-fidelity proof must fail if scenario tests use in-memory stores while real runtime uses durable stores without equivalence proof.
2788. Runtime-fidelity proof must fail if scenario tests mock policy, capability, invariant, promotion, artifact, or event subsystems.
2789. Runtime-fidelity proof must fail if scenario tests run with special flags unavailable in real runtime.
2790. Runtime-fidelity proof must fail if scenario tests omit authentication or authorization required in real runtime.
2791. Runtime-fidelity proof must fail if scenario tests omit failure handling required in real runtime.
2792. Runtime-fidelity proof must fail if scenario tests do not exercise actual serialization boundaries.
2793. Runtime-fidelity proof must fail if runtime-fidelity limitations are not recorded in final scope.
2794. Runtime-fidelity proof must fail if final proof claims production-like behavior while using test-only pathways.
2795. Subsystem-health proof must require every mandatory subsystem to expose an executable health or self-check.
2796. Subsystem-health proof must fail if a subsystem health check is documentation-only.
2797. Subsystem-health proof must fail if a subsystem health check returns healthy while required configuration is missing.
2798. Subsystem-health proof must fail if a subsystem health check returns healthy while persistence is unavailable.
2799. Subsystem-health proof must fail if a subsystem health check ignores failed dependencies.
2800. Subsystem-health proof must fail if subsystem health results are not included in final proof metadata.
2801. Subsystem-health proof must fail if health checks are run before subsystem initialization and then reused.
2802. Subsystem-health proof must fail if health checks are optional for mandatory subsystems.
2803. Subsystem-health proof must fail if health-check PASS can override scenario failure.
2804. Subsystem-health proof must fail if health-check output is prose-only.
2805. Proof-explanation proof must generate a structured explanation for why final classification was assigned.
2806. Proof-explanation proof must fail if explanation omits any blocking failure when classification is below target.
2807. Proof-explanation proof must fail if explanation says requirements passed without listing evidence categories.
2808. Proof-explanation proof must fail if explanation contradicts decision table or final gate output.
2809. Proof-explanation proof must fail if explanation relies on subjective language.
2810. Proof-explanation proof must fail if explanation cannot be regenerated from machine-readable reports.
2811. Proof-explanation proof must fail if explanation hides waivers, assumptions, scope exclusions, or residual risks.
2812. Proof-explanation proof must fail if explanation omits anti-false-PASS and replay results.
2813. Proof-explanation proof must fail if explanation is not included in artifact index.
2814. Proof-explanation proof must fail if explanation is used as evidence for itself.
2815. Failure-reproduction proof must provide commands to reproduce each blocking failure when final classification is non-PASS.
2816. Failure-reproduction proof must fail if blocking failures have no reproduction command or evidence pointer.
2817. Failure-reproduction proof must fail if reproduction commands mutate evidence or source unexpectedly.
2818. Failure-reproduction proof must fail if reproduction commands rely on missing local state.
2819. Failure-reproduction proof must fail if reproduction command succeeds while the failure remains listed.
2820. Failure-reproduction proof must fail if reproduction command cannot distinguish expected validation failure from infrastructure failure.
2821. Failure-reproduction proof must fail if reproduction logs are not captured and hashed.
2822. Failure-reproduction proof must fail if reproduction commands are omitted from final non-PASS artifacts.
2823. Failure-reproduction proof must fail if failure reproduction depends on historical reports not included in bundle.
2824. Failure-reproduction proof must fail if failure reproduction is not possible in detached verification context when claimed.
2825. Targeted-fix proof must map each code change to the specific gap, requirement, risk, or regression it addresses.
2826. Targeted-fix proof must fail if a proof-critical code change has no mapped reason.
2827. Targeted-fix proof must fail if a mapped fix has no source diff, test, validator, or evidence update.
2828. Targeted-fix proof must fail if a fix modifies final gate logic without adding negative tests.
2829. Targeted-fix proof must fail if a fix modifies schema without updating schema fixtures.
2830. Targeted-fix proof must fail if a fix modifies replay without replay regression tests.
2831. Targeted-fix proof must fail if a fix modifies path handling without path-confusion regression tests.
2832. Targeted-fix proof must fail if a fix modifies command recording without exit-code propagation tests.
2833. Targeted-fix proof must fail if a fix modifies gap classification without OPEN-gap blocking tests.
2834. Targeted-fix proof must fail if a fix modifies anti-false-PASS audit without weak-rejection tests.
2835. Acceptance-freeze proof must freeze acceptance criteria before implementation evidence is evaluated.
2836. Acceptance-freeze proof must fail if acceptance criteria are edited after seeing proof results without a recorded criteria-change review.
2837. Acceptance-freeze proof must fail if failed criteria are removed from the acceptance set during the same proof run.
2838. Acceptance-freeze proof must fail if new criteria are added after final gate execution without rerunning final gate.
2839. Acceptance-freeze proof must fail if criteria freeze metadata is absent from the final verdict.
2840. Acceptance-freeze proof must fail if criteria are stored only in markdown and not in a machine-readable contract.
2841. Acceptance-freeze proof must fail if criteria freeze hash differs between traceability, proof bundle, and final verdict.
2842. Acceptance-freeze proof must fail if criteria are inferred from implementation files instead of the frozen contract.
2843. Acceptance-freeze proof must fail if criteria can be marked optional by generated reports.
2844. Acceptance-freeze proof must fail if detached verification cannot verify the frozen criteria hash.
2845. Independent-oracle proof must maintain expected safe and unsafe outcomes independent of implementation and report generators.
2846. Independent-oracle proof must fail if oracle files are generated by the same runtime code under test.
2847. Independent-oracle proof must fail if oracle expected values are copied from current scenario results.
2848. Independent-oracle proof must fail if oracle changes are not reviewed as source changes.
2849. Independent-oracle proof must fail if oracle hashes are absent from scenario evidence.
2850. Independent-oracle proof must fail if replay expected outcomes are derived from replay output.
2851. Independent-oracle proof must fail if mutation expected outcomes are inferred after mutation execution.
2852. Independent-oracle proof must fail if anti-false-PASS expected failures are inferred from observed validator failures.
2853. Independent-oracle proof must fail if oracle and scenario definition versions disagree.
2854. Independent-oracle proof must fail if oracle files are missing from clean checkout.
2855. Proof-audit trail must record who or what generated each proof artifact.
2856. Proof-audit trail must fail if generator identity is missing for any proof-critical artifact.
2857. Proof-audit trail must fail if generated artifacts lack parent command transcript entry.
2858. Proof-audit trail must fail if artifact generation is attributed to a command that did not run.
2859. Proof-audit trail must fail if the same artifact is attributed to multiple generation commands without handoff metadata.
2860. Proof-audit trail must fail if manual edits are not recorded as manual interventions.
2861. Proof-audit trail must fail if manual interventions affect final PASS without independent validator evidence.
2862. Proof-audit trail must fail if audit events are not hash chained.
2863. Proof-audit trail must fail if audit trail omits failed generation attempts.
2864. Proof-audit trail must fail if audit trail is not included in detached verification.
2865. Evidence-replayability proof must define which evidence can be reproduced by rerunning commands and which is historical-only.
2866. Evidence-replayability proof must fail if reproducible evidence cannot be reproduced from clean checkout.
2867. Evidence-replayability proof must fail if historical-only evidence is used for current runtime claims without current validation.
2868. Evidence-replayability proof must fail if reproduction commands are missing for generated reports.
2869. Evidence-replayability proof must fail if reproduction produces different hashes without accepted nondeterministic fields.
2870. Evidence-replayability proof must fail if replayability status is not included in artifact index.
2871. Evidence-replayability proof must fail if non-replayable evidence supports a mandatory current-run requirement.
2872. Evidence-replayability proof must fail if reproduction requires hidden secrets or unavailable external services.
2873. Evidence-replayability proof must fail if reproduction command modifies source files.
2874. Evidence-replayability proof must fail if reproduction command succeeds but generated semantic content differs.
2875. Assertion-to-requirement proof must link every proof-critical assertion to a requirement or corrective item.
2876. Assertion-to-requirement proof must fail if a test assertion has no mapped requirement and affects final verdict.
2877. Assertion-to-requirement proof must fail if a requirement has mapped tests but no requirement-specific assertions.
2878. Assertion-to-requirement proof must fail if one broad assertion claims to satisfy multiple unrelated requirements.
2879. Assertion-to-requirement proof must fail if assertions check implementation details but not required behavior.
2880. Assertion-to-requirement proof must fail if assertions are disabled by optimization, monkeypatch, or conditional skip.
2881. Assertion-to-requirement proof must fail if assertions are wrapped in try/except that hides failure.
2882. Assertion-to-requirement proof must fail if assertions compare against values generated by the same function under test.
2883. Assertion-to-requirement proof must fail if assertion mapping is generated from assertion text only.
2884. Assertion-to-requirement proof must fail if assertion mapping is missing from final traceability.
2885. Error-message proof must require validators to emit stable machine-readable error codes.
2886. Error-message proof must fail if anti-false-PASS expected reasons are matched only against unstable prose.
2887. Error-message proof must fail if validators return generic error codes for distinct proof-critical failures.
2888. Error-message proof must fail if infrastructure errors and semantic validation errors share the same code.
2889. Error-message proof must fail if missing evidence and hash mismatch share the same code.
2890. Error-message proof must fail if unsupported schema and malformed JSON share the same code.
2891. Error-message proof must fail if error codes are not versioned.
2892. Error-message proof must fail if error code registry is generated from observed failures.
2893. Error-message proof must fail if final reports omit error codes for non-PASS items.
2894. Error-message proof must fail if detached verification does not validate expected error codes.
2895. Provenance-redaction proof must separate public provenance from private provenance without breaking verification.
2896. Provenance-redaction proof must fail if redacted provenance cannot be linked to original hashes.
2897. Provenance-redaction proof must fail if redaction removes command identity, report identity, proof_run_id, or evidence hash.
2898. Provenance-redaction proof must fail if redaction changes final verdict semantics.
2899. Provenance-redaction proof must fail if public bundle claims cannot be verified without private-only fields and no public-verification policy exists.
2900. Provenance-redaction proof must fail if private provenance leaks into public artifacts.
2901. Provenance-redaction proof must fail if redaction rules are not deterministic and versioned.
2902. Provenance-redaction proof must fail if redaction is applied before proof-critical hashing without a documented raw-hash commitment.
2903. Provenance-redaction proof must fail if redacted and unredacted artifact indexes disagree.
2904. Provenance-redaction proof must fail if detached verification cannot validate the redacted bundle mode.
2905. Requirement-criticality proof must classify requirements by criticality.
2906. Requirement-criticality proof must fail if mandatory safety-critical requirements are treated as ordinary checklist items.
2907. Requirement-criticality proof must fail if a safety-critical requirement lacks stronger evidence than a documentation requirement.
2908. Requirement-criticality proof must fail if criticality is missing from traceability rows.
2909. Requirement-criticality proof must fail if criticality changes after evidence generation.
2910. Requirement-criticality proof must fail if criticality is caller-supplied and not checked against requirement registry.
2911. Requirement-criticality proof must fail if low-criticality waiver rules are applied to high-criticality requirements.
2912. Requirement-criticality proof must fail if final classification ignores failed high-criticality requirements.
2913. Requirement-criticality proof must fail if criticality is absent from final explanation.
2914. Requirement-criticality proof must fail if criticality definitions are prose-only.
2915. Implementation-substitution proof must detect when a mock, stub, shim, or simplified implementation replaces the intended runtime component.
2916. Implementation-substitution proof must fail if substituted components are used in final proof without explicit scope marking.
2917. Implementation-substitution proof must fail if tests pass against a fake artifact store while runtime uses a different store.
2918. Implementation-substitution proof must fail if tests pass against fake policy evaluation while runtime uses real policy.
2919. Implementation-substitution proof must fail if replay uses simplified decision code not used by original execution.
2920. Implementation-substitution proof must fail if validators validate fixture reports instead of current generated reports.
2921. Implementation-substitution proof must fail if final proof target imports test doubles before production modules.
2922. Implementation-substitution proof must fail if substitution is controlled by environment variables not recorded in proof metadata.
2923. Implementation-substitution proof must fail if substituted implementation has weaker side-effect, persistence, or security behavior.
2924. Implementation-substitution proof must fail if substitution status is not represented in final scope.
2925. Proof-attack surface proof must list every place an incorrect PASS can enter the system.
2926. Proof-attack surface proof must fail if any PASS source lacks validation.
2927. Proof-attack surface proof must fail if any PASS source can be manually edited after validation.
2928. Proof-attack surface proof must fail if any PASS source is derived from prose, filename, or unchecked config.
2929. Proof-attack surface proof must fail if PASS can enter through legacy reports.
2930. Proof-attack surface proof must fail if PASS can enter through cached reports.
2931. Proof-attack surface proof must fail if PASS can enter through generated markdown.
2932. Proof-attack surface proof must fail if PASS can enter through model output.
2933. Proof-attack surface proof must fail if PASS can enter through CI status without artifact validation.
2934. Proof-attack surface proof must fail if attack surface map is absent from threat model.
2935. Semantic-hash proof must define which fields contribute to semantic hashes and which are metadata-only.
2936. Semantic-hash proof must fail if proof-critical fields are excluded from semantic hashes.
2937. Semantic-hash proof must fail if metadata-only fields affect final classification.
2938. Semantic-hash proof must fail if semantic hash excludes evidence_refs, statuses, verdicts, classifications, IDs, or counts.
2939. Semantic-hash proof must fail if semantic hash ignores ordering where ordering is semantically meaningful.
2940. Semantic-hash proof must fail if semantic hash includes nondeterministic metadata without normalization.
2941. Semantic-hash proof must fail if two semantically different reports have the same semantic hash due to dropped fields.
2942. Semantic-hash proof must fail if semantic hash rules differ across report types without rationale.
2943. Semantic-hash proof must fail if detached verification does not recompute semantic hashes.
2944. Semantic-hash proof must fail if semantic hash version is missing.
2945. Root-cause proof must require every blocker and failed validator to include a root-cause category.
2946. Root-cause proof must fail if blockers are listed without root-cause classification.
2947. Root-cause proof must fail if root cause is unknown and final classification still claims PASS.
2948. Root-cause proof must fail if root-cause categories are prose-only.
2949. Root-cause proof must fail if the same failure is assigned conflicting root causes across reports.
2950. Root-cause proof must fail if root cause is marked fixed without fix evidence.
2951. Root-cause proof must fail if root cause is marked external without evidence that it is outside project control.
2952. Root-cause proof must fail if root cause is marked infrastructure but semantic evidence shows validation failure.
2953. Root-cause proof must fail if root cause is omitted from final explanation.
2954. Root-cause proof must fail if root cause mapping is not included in proof bundle.
2955. Proof-priority proof must assign priority to unresolved gaps and risks.
2956. Proof-priority proof must fail if high-priority unresolved gaps are compatible with FUNCTIONAL_RUNTIME_MVP.
2957. Proof-priority proof must fail if priority is missing for unresolved items.
2958. Proof-priority proof must fail if priority is downgraded without rationale.
2959. Proof-priority proof must fail if priorities differ between gap report and risk register.
2960. Proof-priority proof must fail if priority labels are prose-only.
2961. Proof-priority proof must fail if final verdict omits high-priority unresolved items.
2962. Proof-priority proof must fail if priority is generated by report order rather than explicit field.
2963. Proof-priority proof must fail if priority does not influence final classification where required.
2964. Proof-priority proof must fail if priority changes after final gate without rerun.
2965. Evidence-locality proof must ensure every proof row identifies the smallest relevant evidence span where practical.
2966. Evidence-locality proof must fail if evidence points only to a whole large log when a specific command result is required.
2967. Evidence-locality proof must fail if evidence points only to a whole source file when a function-level reference is practical.
2968. Evidence-locality proof must fail if evidence points only to a whole report when a row-level reference is required.
2969. Evidence-locality proof must fail if evidence spans include unrelated PASS text that can hide FAIL text.
2970. Evidence-locality proof must fail if evidence locality is omitted for safety-critical requirements.
2971. Evidence-locality proof must fail if validators cannot locate the exact referenced evidence span.
2972. Evidence-locality proof must fail if evidence span hashes are absent where row-level proof is required.
2973. Evidence-locality proof must fail if line numbers are stale after file regeneration.
2974. Evidence-locality proof must fail if detached verification cannot validate localized evidence refs.
2975. Human-factors proof must ensure proof output cannot easily mislead a human reviewer.
2976. Human-factors proof must fail if failed items are visually hidden below PASS summaries without machine-readable blocker index.
2977. Human-factors proof must fail if color, emoji, or formatting is the only indication of status.
2978. Human-factors proof must fail if report titles imply completion while verdict is non-PASS.
2979. Human-factors proof must fail if summaries omit the distinction between verified, inferred, skipped, and out-of-scope.
2980. Human-factors proof must fail if warnings are displayed as notes while blocking final proof.
2981. Human-factors proof must fail if historical PASS artifacts appear beside current non-PASS artifacts without labels.
2982. Human-factors proof must fail if report filenames use ambiguous terms such as final, latest, or complete without proof_run_id.
2983. Human-factors proof must fail if reviewer checklist omits how to run detached verification.
2984. Human-factors proof must fail if human-readable reports cannot be traced to JSON source.
2985. Final-answer proof must ensure any external claim about done status is derived from final verdict JSON.
2986. Final-answer proof must fail if assistant, docs, or CI comments claim done based on partial reports.
2987. Final-answer proof must fail if status summaries omit the exact classification enum.
2988. Final-answer proof must fail if status summaries omit blockers when classification is below target.
2989. Final-answer proof must fail if status summaries claim no gaps because no scan found new gaps.
2990. Final-answer proof must fail if status summaries claim completion before detached verification.
2991. Final-answer proof must fail if status summaries use old commit evidence for a new commit.
2992. Final-answer proof must fail if status summaries fail to distinguish inspected evidence from executed proof.
2993. Final-answer proof must fail if status summaries omit confidence and scope limitations.
2994. Final-answer proof must fail if status summaries are not reproducible from final artifacts.
2995. Proof-retirement proof must define when old proof artifacts, commands, validators, and schemas are retired.
2996. Proof-retirement proof must fail if retired artifacts remain accepted by current final gate.
2997. Proof-retirement proof must fail if retired commands are still documented as valid proof commands.
2998. Proof-retirement proof must fail if retired validators are still counted in coverage.
2999. Proof-retirement proof must fail if retired schemas can satisfy current requirements.
3000. Proof-retirement proof must fail if retired reports are not marked historical.
3001. Proof-retirement proof must fail if retired artifacts share names with current artifacts.
3002. Proof-retirement proof must fail if retirement metadata is absent from artifact index.
3003. Proof-retirement proof must fail if old CI jobs still report success for retired proof paths.
3004. Proof-retirement proof must fail if detached verifier accepts retired proof bundles as current.
3005. Proof-governance proof must define who may change proof policy, proof contract, validator registry, scenario registry, and final-gate rules.
3006. Proof-governance proof must fail if proof-governance changes can be made by the same generated agent being evaluated.
3007. Proof-governance proof must fail if proof-governance changes are not reviewed as separate high-criticality changes.
3008. Proof-governance proof must fail if proof-governance changes are merged without regression tests for weakened validation.
3009. Proof-governance proof must fail if proof-governance metadata is absent from the final verdict.
3010. Proof-governance proof must fail if proof-governance rules are stored only in prose.
3011. Proof-governance proof must fail if proof-governance rules differ between local proof and CI proof.
3012. Proof-governance proof must fail if proof-governance changes can retroactively mark old failed evidence as PASS.
3013. Proof-governance proof must fail if governance exceptions are not included in risk register and final proof hashes.
3014. Proof-governance proof must fail if detached verification does not enforce current proof-governance rules.
3015. Meta-validator proof must validate the validators themselves against the proof contract.
3016. Meta-validator proof must fail if a validator has no declared inputs, outputs, failure modes, and schema expectations.
3017. Meta-validator proof must fail if a validator reads reports not declared in its input contract.
3018. Meta-validator proof must fail if a validator writes files outside its declared output contract.
3019. Meta-validator proof must fail if a validator has no expected failure-code coverage.
3020. Meta-validator proof must fail if a validator can pass without reading any proof-critical input file.
3021. Meta-validator proof must fail if validator implementation contains unconditional success paths.
3022. Meta-validator proof must fail if validator implementation catches all exceptions and returns success.
3023. Meta-validator proof must fail if validators share unchecked helper logic that can mask the same bug across all validators.
3024. Meta-validator proof must fail if meta-validation is not included in final gate.
3025. Meta-generator proof must validate report generators against generator contracts.
3026. Meta-generator proof must fail if a generator has no declared required inputs.
3027. Meta-generator proof must fail if a generator can emit PASS when required inputs are missing.
3028. Meta-generator proof must fail if a generator writes unsupported schema versions.
3029. Meta-generator proof must fail if a generator accepts caller-supplied status, verdict, classification, or evidence strength for final reports.
3030. Meta-generator proof must fail if a generator produces reports that fail their own registered schemas.
3031. Meta-generator proof must fail if a generator reads stale previous outputs without declaring them as inputs.
3032. Meta-generator proof must fail if generator output is nondeterministic without recorded normalization.
3033. Meta-generator proof must fail if generator negative tests do not include missing input, malformed input, stale input, and conflicting input cases.
3034. Meta-generator proof must fail if meta-generation validation is not included in final gate.
3035. Proof-layering proof must separate runtime evidence, validation evidence, aggregation evidence, and presentation evidence.
3036. Proof-layering proof must fail if presentation evidence is used to satisfy runtime evidence requirements.
3037. Proof-layering proof must fail if aggregation reports replace lower-level evidence rather than linking to it.
3038. Proof-layering proof must fail if validation evidence is generated before runtime evidence exists.
3039. Proof-layering proof must fail if lower-layer failures are hidden by upper-layer summaries.
3040. Proof-layering proof must fail if upper-layer PASS can override lower-layer FAIL.
3041. Proof-layering proof must fail if proof layers are not represented in artifact index metadata.
3042. Proof-layering proof must fail if detached verification cannot traverse from upper-layer claims to lower-layer evidence.
3043. Proof-layering proof must fail if layer boundaries differ between local proof and CI proof.
3044. Proof-layering proof must fail if layer names are prose-only and not machine-readable.
3045. Semantic-dependency proof must identify not only file dependencies but field-level dependencies between report values.
3046. Semantic-dependency proof must fail if a final verdict field depends on a report field not listed in dependency metadata.
3047. Semantic-dependency proof must fail if a report field changes and dependent reports are not regenerated.
3048. Semantic-dependency proof must fail if dependency tracking treats all fields as equal and misses proof-critical field changes.
3049. Semantic-dependency proof must fail if metadata-only field changes trigger semantic reclassification without rationale.
3050. Semantic-dependency proof must fail if proof-critical field changes are normalized away.
3051. Semantic-dependency proof must fail if dependency metadata cannot distinguish evidence_refs from display-only links.
3052. Semantic-dependency proof must fail if detached verification ignores field-level dependencies.
3053. Semantic-dependency proof must fail if field-level dependency rules are not versioned.
3054. Semantic-dependency proof must fail if semantic dependency graph contains unknown field names.
3055. Proof-capability proof must define capabilities for proof actors separately from runtime actors.
3056. Proof-capability proof must fail if a runtime actor has proof-actor capabilities.
3057. Proof-capability proof must fail if a proof actor can promote runtime artifacts.
3058. Proof-capability proof must fail if a verifier actor can generate new evidence.
3059. Proof-capability proof must fail if a generator actor can validate its own output as final authority.
3060. Proof-capability proof must fail if proof actor permissions are not recorded.
3061. Proof-capability proof must fail if proof actor capability changes are not audited.
3062. Proof-capability proof must fail if proof actors share credentials or IDs with runtime agents.
3063. Proof-capability proof must fail if proof capabilities default to allow.
3064. Proof-capability proof must fail if proof-capability checks are absent from final gate.
3065. Adversarial-review proof must require an explicit review for false-PASS loopholes introduced by proof-system changes.
3066. Adversarial-review proof must fail if proof-system changes are reviewed only for functional correctness.
3067. Adversarial-review proof must fail if adversarial review omits path confusion, stale evidence, forged transcript, forced PASS, and weak rejection classes.
3068. Adversarial-review proof must fail if adversarial review findings are not linked to corrective items.
3069. Adversarial-review proof must fail if unresolved adversarial findings are compatible with FUNCTIONAL_RUNTIME_MVP.
3070. Adversarial-review proof must fail if adversarial review is not updated after proof contract changes.
3071. Adversarial-review proof must fail if review evidence is stored only as free-form comments.
3072. Adversarial-review proof must fail if reviewer identity and review timestamp are missing.
3073. Adversarial-review proof must fail if adversarial review can be performed by the generated agent under review.
3074. Adversarial-review proof must fail if adversarial review is absent from final artifact index.
3075. Scenario-minimization proof must keep minimal scenarios that isolate each mandatory behavior.
3076. Scenario-minimization proof must fail if a broad end-to-end scenario is the only evidence for several unrelated requirements.
3077. Scenario-minimization proof must fail if a failure in a broad scenario cannot identify which mandatory behavior failed.
3078. Scenario-minimization proof must fail if minimal negative scenarios are absent for safety-critical denial paths.
3079. Scenario-minimization proof must fail if minimal positive scenarios are absent for safe promotion paths.
3080. Scenario-minimization proof must fail if scenario complexity hides unused subsystems.
3081. Scenario-minimization proof must fail if minimal scenarios are not mapped to traceability.
3082. Scenario-minimization proof must fail if scenario minimization changes expected verdicts.
3083. Scenario-minimization proof must fail if minimized scenarios are generated from implementation outputs.
3084. Scenario-minimization proof must fail if minimized scenario definitions are not hashed.
3085. Scenario-composition proof must define how simple scenarios compose into end-to-end runtime behavior.
3086. Scenario-composition proof must fail if composed scenarios pass while component scenarios fail.
3087. Scenario-composition proof must fail if component scenarios pass but composed scenario bypasses required integration.
3088. Scenario-composition proof must fail if composition introduces side effects not present in components.
3089. Scenario-composition proof must fail if scenario composition order is undefined.
3090. Scenario-composition proof must fail if composed evidence cannot be decomposed into component evidence.
3091. Scenario-composition proof must fail if composed scenario uses different configuration than component scenarios without rationale.
3092. Scenario-composition proof must fail if composition hides a failed intermediate state.
3093. Scenario-composition proof must fail if replay validates components but not the composed scenario.
3094. Scenario-composition proof must fail if traceability cannot distinguish component and composed evidence.
3095. Evidence-saturation proof must define when enough evidence exists for a requirement.
3096. Evidence-saturation proof must fail if requirements accumulate evidence endlessly without a pass/fail decision rule.
3097. Evidence-saturation proof must fail if one evidence type is over-collected while required evidence types are missing.
3098. Evidence-saturation proof must fail if adding redundant evidence inflates confidence metrics.
3099. Evidence-saturation proof must fail if evidence sufficiency is decided manually without machine-readable criteria.
3100. Evidence-saturation proof must fail if evidence sufficiency differs between validators and final gate.
3101. Evidence-saturation proof must fail if a requirement is marked PASS before its minimum evidence set is complete.
3102. Evidence-saturation proof must fail if evidence sufficiency rules are not versioned.
3103. Evidence-saturation proof must fail if detached verification cannot recompute evidence sufficiency.
3104. Evidence-saturation proof must fail if evidence sufficiency ignores counterevidence.
3105. Confidence-calibration proof must define confidence labels if confidence is reported.
3106. Confidence-calibration proof must fail if confidence percentages are invented or manually assigned.
3107. Confidence-calibration proof must fail if confidence labels are used as PASS substitutes.
3108. Confidence-calibration proof must fail if confidence is high while mandatory evidence is missing.
3109. Confidence-calibration proof must fail if confidence ignores failed attacks or survived mutations.
3110. Confidence-calibration proof must fail if confidence is averaged across requirements with different criticality.
3111. Confidence-calibration proof must fail if confidence is reported without uncertainty or scope.
3112. Confidence-calibration proof must fail if confidence calculation is not reproducible from evidence.
3113. Confidence-calibration proof must fail if confidence is included in markdown but absent from JSON.
3114. Confidence-calibration proof must fail if confidence is used by final gate without a declared decision rule.
3115. False-negative proof must check that validators do not reject valid complete proof bundles.
3116. False-negative proof must fail if a valid complete proof bundle is rejected by detached verification.
3117. False-negative proof must fail if validator strictness rejects legitimate path relocation.
3118. False-negative proof must fail if validators reject valid report ordering that is semantically unordered.
3119. False-negative proof must fail if validators reject valid optional diagnostics removal.
3120. False-negative proof must fail if validators reject valid UTF-8 content within supported scope.
3121. False-negative proof must fail if validators reject valid platform metadata from supported environments.
3122. False-negative proof must fail if validators reject valid proof_run_id formats.
3123. False-negative proof must fail if false-negative tests are absent for strict validators.
3124. False-negative proof must fail if false-negative exceptions are handled by weakening validation globally.
3125. Proof-usability proof must ensure proof commands and reports are usable by a developer without hidden knowledge.
3126. Proof-usability proof must fail if proof command failure messages do not tell which artifact or validator failed.
3127. Proof-usability proof must fail if final verdict lacks a next-step pointer for blockers.
3128. Proof-usability proof must fail if detached verification output is too vague to locate corruption.
3129. Proof-usability proof must fail if proof artifact index lacks descriptions of artifact purpose.
3130. Proof-usability proof must fail if report directory contains many files with no grouping or index.
3131. Proof-usability proof must fail if command documentation lacks clean checkout instructions.
3132. Proof-usability proof must fail if proof target requires undocumented tool installation.
3133. Proof-usability proof must fail if proof target produces ambiguous success output.
3134. Proof-usability proof must fail if proof reports use inconsistent terminology for the same concept.
3135. Evidence-deletion proof must intentionally delete each required evidence family and verify final gate fails.
3136. Evidence-deletion proof must fail if deleting replay evidence does not block final PASS.
3137. Evidence-deletion proof must fail if deleting transcript evidence does not block final PASS.
3138. Evidence-deletion proof must fail if deleting source mutation evidence does not block final PASS.
3139. Evidence-deletion proof must fail if deleting anti-false-PASS evidence does not block final PASS.
3140. Evidence-deletion proof must fail if deleting traceability evidence does not block final PASS.
3141. Evidence-deletion proof must fail if deleting artifact index does not block detached verification.
3142. Evidence-deletion proof must fail if deleting final verdict dependencies is treated as warning.
3143. Evidence-deletion proof must fail if evidence deletion attacks are not included in negative corpus.
3144. Evidence-deletion proof must fail if deleted evidence can be silently regenerated by verifier.
3145. Evidence-substitution proof must intentionally replace each required evidence family with unrelated valid-looking evidence and verify rejection.
3146. Evidence-substitution proof must fail if unrelated passing test logs can satisfy a requirement.
3147. Evidence-substitution proof must fail if unrelated validator results can satisfy a report claim.
3148. Evidence-substitution proof must fail if evidence from another scenario can satisfy current scenario.
3149. Evidence-substitution proof must fail if evidence from another commit can satisfy current commit.
3150. Evidence-substitution proof must fail if evidence from another proof_run_id can satisfy current proof_run_id.
3151. Evidence-substitution proof must fail if evidence with matching schema but wrong semantics is accepted.
3152. Evidence-substitution proof must fail if evidence with matching hash but wrong declared type is accepted.
3153. Evidence-substitution proof must fail if evidence-substitution attacks are not included in anti-false-PASS or red-team corpus.
3154. Evidence-substitution proof must fail if substituted evidence is accepted because only file existence is checked.
3155. Proof-reduction proof must identify the minimal evidence set needed for final PASS.
3156. Proof-reduction proof must fail if removing any supposedly required evidence still allows PASS.
3157. Proof-reduction proof must fail if optional evidence is actually required for PASS.
3158. Proof-reduction proof must fail if minimal evidence set contains stale artifacts.
3159. Proof-reduction proof must fail if minimal evidence set cannot be detached-verified.
3160. Proof-reduction proof must fail if minimal evidence set omits any safety-critical behavior.
3161. Proof-reduction proof must fail if minimal evidence set differs between local and CI proof.
3162. Proof-reduction proof must fail if minimal evidence set is not recorded in final proof metadata.
3163. Proof-reduction proof must fail if proof-reduction analysis is based on report filenames rather than dependency graph.
3164. Proof-reduction proof must fail if proof-reduction result is used to delete evidence before archival.
3165. Contradiction-injection proof must inject contradictory statuses across reports and verify cross-report consistency catches them.
3166. Contradiction-injection proof must fail if acceptance says PASS while traceability says FAIL and final gate passes.
3167. Contradiction-injection proof must fail if proof bundle says one commit while final verdict says another and detached verifier passes.
3168. Contradiction-injection proof must fail if replay report says mismatch while acceptance says replay PASS and final gate passes.
3169. Contradiction-injection proof must fail if anti-false-PASS markdown says attacks accepted while JSON says rejected and final gate passes.
3170. Contradiction-injection proof must fail if artifact index hash differs from proof bundle hash and final gate passes.
3171. Contradiction-injection proof must fail if command transcript failure conflicts with validator summary success and final gate passes.
3172. Contradiction-injection proof must fail if risk register has MVP blocker while final classification says FUNCTIONAL_RUNTIME_MVP and final gate passes.
3173. Contradiction-injection proof must fail if CI status fails while final verdict claims CI PASS and final gate passes.
3174. Contradiction-injection proof must fail if contradiction-injection tests are not run in final proof target.
3175. Final-state sealing proof must seal the final proof bundle after successful verification.
3176. Final-state sealing proof must fail if new artifacts can be added to the sealed bundle without invalidating bundle hash.
3177. Final-state sealing proof must fail if sealed bundle omits the seal metadata.
3178. Final-state sealing proof must fail if seal metadata omits artifact count and bundle hash.
3179. Final-state sealing proof must fail if seal can be applied before final verification.
3180. Final-state sealing proof must fail if seal can be applied to a non-PASS bundle without clear non-PASS status.
3181. Final-state sealing proof must fail if seal is not checked by detached verification.
3182. Final-state sealing proof must fail if seal status is stored only in markdown.
3183. Final-state sealing proof must fail if sealed bundle can be partially extracted and misrepresented as complete.
3184. Final-state sealing proof must fail if seal allows historical artifacts to appear current.
3185. Context-builder proof must define exactly which inputs may enter the runtime context for each scenario.
3186. Context-builder proof must fail if context includes untracked files, stale reports, user home files, or previous-run artifacts.
3187. Context-builder proof must fail if context construction order is nondeterministic and affects runtime decisions.
3188. Context-builder proof must fail if context truncation removes policy, capability, invariant, approval, or artifact-hash evidence.
3189. Context-builder proof must fail if context includes generated proof reports that can influence runtime behavior.
3190. Context-builder proof must fail if context differs between original execution and replay without a recorded context diff.
3191. Context-builder proof must fail if context hashes are absent from scenario evidence.
3192. Context-builder proof must fail if context includes prose instructions that can override structured safety rules.
3193. Context-builder proof must fail if context redaction removes proof-critical fields.
3194. Context-builder proof must fail if context construction is not covered by negative tests.
3195. Prompt-contract proof must define allowed prompt fields, forbidden prompt fields, model/tool permissions, and expected structured outputs.
3196. Prompt-contract proof must fail if prompts can ask the model to ignore validators, policies, invariants, or proof contracts.
3197. Prompt-contract proof must fail if prompt templates are modified after prompt hash capture.
3198. Prompt-contract proof must fail if prompt templates are generated from model output during final proof.
3199. Prompt-contract proof must fail if prompt variables are unescaped and allow instruction injection.
3200. Prompt-contract proof must fail if prompt output parser accepts prose PASS claims as structured approval.
3201. Prompt-contract proof must fail if prompt contract version is missing from model-call evidence.
3202. Prompt-contract proof must fail if prompts differ between tests, runtime, and replay without compatibility proof.
3203. Prompt-contract proof must fail if prompt contract permits model self-approval of generated code.
3204. Prompt-contract proof must fail if prompt contract tests are absent from final proof.
3205. Memory-system proof must define whether short-term memory, long-term memory, vector memory, or file memory is in scope.
3206. Memory-system proof must fail if memory from prior proof runs influences current proof behavior.
3207. Memory-system proof must fail if memory entries lack source, timestamp, proof_run_id, and hash.
3208. Memory-system proof must fail if generated agents can edit safety-critical memory without review.
3209. Memory-system proof must fail if memory retrieval can inject stale or contradictory requirements.
3210. Memory-system proof must fail if memory retrieval order is nondeterministic and affects decisions.
3211. Memory-system proof must fail if memory contents are not included in replay manifests when they affect behavior.
3212. Memory-system proof must fail if memory cleanup deletes evidence needed for audit.
3213. Memory-system proof must fail if memory can mark gaps FIXED without validator evidence.
3214. Memory-system proof must fail if final proof claims memory-independent behavior while memory is enabled.
3215. Learning-system proof must define whether the agent may update rules, policies, prompts, memories, or code from experience.
3216. Learning-system proof must fail if learned changes can affect final proof without review and traceability.
3217. Learning-system proof must fail if learned changes are not separated from source-controlled changes.
3218. Learning-system proof must fail if learning updates weaken safety checks.
3219. Learning-system proof must fail if learning updates are replayed differently from original execution.
3220. Learning-system proof must fail if learning state is not versioned and hashed.
3221. Learning-system proof must fail if learned rules conflict with canonical proof contract.
3222. Learning-system proof must fail if learning from failed proof runs converts failures into accepted behavior.
3223. Learning-system proof must fail if learned state is reused across commits without freshness checks.
3224. Learning-system proof must fail if learning is claimed out of scope but enabled during proof.
3225. Self-evolution orchestrator proof must define allowed self-modification boundaries.
3226. Self-evolution orchestrator proof must fail if the orchestrator can modify validators, final gate, or proof policy before proving the change.
3227. Self-evolution orchestrator proof must fail if self-modified code runs outside sandbox before review.
3228. Self-evolution orchestrator proof must fail if proposed patches are promoted without policy, capability, invariant, and human-review evidence.
3229. Self-evolution orchestrator proof must fail if the orchestrator can evaluate its own patch as final authority.
3230. Self-evolution orchestrator proof must fail if self-evolution state is not recorded in event log and state records.
3231. Self-evolution orchestrator proof must fail if failed self-evolution attempts leave active artifacts.
3232. Self-evolution orchestrator proof must fail if rollback of self-evolution does not restore exact source and artifact hashes.
3233. Self-evolution orchestrator proof must fail if self-evolution scenarios are not included in replay.
3234. Self-evolution orchestrator proof must fail if self-evolution is implemented but not included in final scope metadata.
3235. Doc-sync proof must define which documentation is generated from proof artifacts and which documentation is source-authored.
3236. Doc-sync proof must fail if generated docs are edited manually without source JSON updates.
3237. Doc-sync proof must fail if source-authored docs claim statuses not present in final verdict.
3238. Doc-sync proof must fail if docs mention proof commands, report names, schemas, or classifications that disagree with the proof contract.
3239. Doc-sync proof must fail if docs omit mandatory limitations, out-of-scope items, or residual risks.
3240. Doc-sync proof must fail if docs are generated before final verdict and not regenerated afterward.
3241. Doc-sync proof must fail if docs use old proof_run_id, old commit, or old artifact hashes.
3242. Doc-sync proof must fail if docs link to artifacts missing from artifact index.
3243. Doc-sync proof must fail if documentation validation is not included in final gate.
3244. Doc-sync proof must fail if docs can be used as authoritative proof evidence.
3245. Schema-to-doc proof must ensure documentation of schemas is generated from schema source.
3246. Schema-to-doc proof must fail if schema docs describe fields absent from actual schemas.
3247. Schema-to-doc proof must fail if schema docs omit required fields used by final verification.
3248. Schema-to-doc proof must fail if enum values in docs differ from enum values in schemas.
3249. Schema-to-doc proof must fail if schema docs are not regenerated after schema changes.
3250. Schema-to-doc proof must fail if schema examples contain PASS reports that would fail validation.
3251. Schema-to-doc proof must fail if schema examples are accepted as proof evidence.
3252. Schema-to-doc proof must fail if schema docs omit rejection semantics.
3253. Schema-to-doc proof must fail if schema docs omit proof_run_id and git_commit requirements.
3254. Schema-to-doc proof must fail if schema docs are not included in doc-sync validation.
3255. CLI-surface proof must enumerate every command that can generate, validate, mutate, archive, or display proof artifacts.
3256. CLI-surface proof must fail if undocumented commands can produce final-looking PASS reports.
3257. CLI-surface proof must fail if legacy commands can bypass final gate.
3258. CLI-surface proof must fail if diagnostic commands mutate proof artifacts.
3259. CLI-surface proof must fail if display commands read stale default report directories.
3260. CLI-surface proof must fail if command aliases have different semantics from canonical commands.
3261. CLI-surface proof must fail if command flags can silently change proof scope.
3262. CLI-surface proof must fail if command names imply verification while only generating reports.
3263. CLI-surface proof must fail if command discovery misses entrypoints declared in packaging metadata.
3264. CLI-surface proof must fail if CLI-surface inventory is absent from final proof metadata.
3265. API-surface proof must enumerate every Python API that can create, validate, approve, deny, promote, replay, or report proof-critical behavior.
3266. API-surface proof must fail if internal APIs can bypass CLI-enforced safety checks.
3267. API-surface proof must fail if public APIs have weaker validation than CLI entrypoints.
3268. API-surface proof must fail if test APIs are importable as production APIs.
3269. API-surface proof must fail if API default arguments enable permissive behavior.
3270. API-surface proof must fail if API consumers can omit proof_run_id where it is required.
3271. API-surface proof must fail if API consumers can supply precomputed PASS statuses.
3272. API-surface proof must fail if API deprecations are not reflected in proof-retirement metadata.
3273. API-surface proof must fail if API inventory is generated from docs instead of source inspection.
3274. API-surface proof must fail if API-surface validation is absent from final gate.
3275. Storage-backend proof must define all supported artifact, report, state, event, and memory storage backends.
3276. Storage-backend proof must fail if proof results from one backend are claimed for another backend.
3277. Storage-backend proof must fail if a backend lacks overwrite, path traversal, symlink, and durability tests.
3278. Storage-backend proof must fail if backend-specific serialization changes evidence hashes without normalization.
3279. Storage-backend proof must fail if backend cleanup semantics differ and can delete current evidence.
3280. Storage-backend proof must fail if backend transaction semantics differ and can create partial PASS evidence.
3281. Storage-backend proof must fail if backend locking semantics differ and can allow concurrent corruption.
3282. Storage-backend proof must fail if backend migration changes proof evidence without migration report.
3283. Storage-backend proof must fail if backend identity and configuration are absent from final verdict.
3284. Storage-backend proof must fail if replay uses a different backend than original execution without equivalence proof.
3285. Event-schema evolution proof must define compatibility rules for old and new event records.
3286. Event-schema evolution proof must fail if new event schemas omit fields required for replay or audit.
3287. Event-schema evolution proof must fail if old event records are accepted without migration when migration is required.
3288. Event-schema evolution proof must fail if event schema migration changes safety meaning.
3289. Event-schema evolution proof must fail if event schema version is absent from event records.
3290. Event-schema evolution proof must fail if event validators accept unknown event types as informational only.
3291. Event-schema evolution proof must fail if event schema examples are used as evidence.
3292. Event-schema evolution proof must fail if event schema changes are not covered by replay regression tests.
3293. Event-schema evolution proof must fail if event schema migration lacks before/after hashes.
3294. Event-schema evolution proof must fail if event schema version differs between original execution and replay without migration evidence.
3295. State-schema evolution proof must define compatibility rules for old and new state records.
3296. State-schema evolution proof must fail if new state schemas omit terminal state, action_id, artifact_id, or proof_run_id fields.
3297. State-schema evolution proof must fail if old state records are silently accepted under new semantics.
3298. State-schema evolution proof must fail if state migration converts unsafe or failed states into safe or promoted states.
3299. State-schema evolution proof must fail if state schema version is absent from state records.
3300. State-schema evolution proof must fail if state validators accept unknown state values.
3301. State-schema evolution proof must fail if state schema changes are not covered by replay regression tests.
3302. State-schema evolution proof must fail if state migration lacks before/after hashes.
3303. State-schema evolution proof must fail if state schema docs differ from state validators.
3304. State-schema evolution proof must fail if state schema version differs between original execution and replay without migration evidence.
3305. Artifact-schema evolution proof must define compatibility rules for artifact metadata.
3306. Artifact-schema evolution proof must fail if new artifact metadata omits content hash, lifecycle state, owner, proof_run_id, or approval link.
3307. Artifact-schema evolution proof must fail if old artifact metadata is accepted without migration when fields are mandatory.
3308. Artifact-schema evolution proof must fail if artifact metadata migration changes content hash, promotion state, or approval scope incorrectly.
3309. Artifact-schema evolution proof must fail if artifact metadata schema version is absent.
3310. Artifact-schema evolution proof must fail if artifact validators accept unknown lifecycle states.
3311. Artifact-schema evolution proof must fail if artifact metadata examples are used as current proof evidence.
3312. Artifact-schema evolution proof must fail if artifact schema changes lack negative tests.
3313. Artifact-schema evolution proof must fail if artifact schema docs differ from validators.
3314. Artifact-schema evolution proof must fail if artifact schema migration is not included in final proof artifacts.
3315. Human-approval UI proof must define whether approvals can be entered through UI, CLI, file, API, or test fixture.
3316. Human-approval UI proof must fail if approval channel used in tests differs from production channel without equivalence proof.
3317. Human-approval UI proof must fail if approval UI displays a different artifact hash than the one approved.
3318. Human-approval UI proof must fail if approval UI omits policy, invariant, capability, or risk context required for review.
3319. Human-approval UI proof must fail if approval UI can submit blank, malformed, or duplicate approvals.
3320. Human-approval UI proof must fail if approval UI can be prefilled by generated agent output without reviewer confirmation.
3321. Human-approval UI proof must fail if approval UI lacks audit record of displayed content.
3322. Human-approval UI proof must fail if approval UI time, reviewer identity, and artifact hash are absent from state records.
3323. Human-approval UI proof must fail if approval UI errors are treated as approval.
3324. Human-approval UI proof must fail if UI approval claims are made while UI path is out of scope.
3325. Interactive-review proof must define whether interactive review is allowed in proof runs.
3326. Interactive-review proof must fail if interactive review results are not pre-seeded, structured, and hashed for noninteractive proof.
3327. Interactive-review proof must fail if a proof run pauses for live human approval and then claims reproducibility without recording the approval artifact.
3328. Interactive-review proof must fail if reviewer decisions entered interactively are not included in detached verification bundle.
3329. Interactive-review proof must fail if interactive and noninteractive approval paths produce different evidence.
3330. Interactive-review proof must fail if interactive prompts can be spoofed by terminal escape sequences.
3331. Interactive-review proof must fail if interactive prompt text differs from recorded review request.
3332. Interactive-review proof must fail if interactive review lacks replay semantics.
3333. Interactive-review proof must fail if interactive review can be skipped by default input.
3334. Interactive-review proof must fail if interactive review is not explicitly out of scope or fully evidenced.
3335. Data-migration proof must define whether existing project data may be migrated during proof.
3336. Data-migration proof must fail if migration changes proof-critical state without before/after evidence.
3337. Data-migration proof must fail if migration is required but not executed in clean checkout proof.
3338. Data-migration proof must fail if migration converts old PASS reports into current evidence.
3339. Data-migration proof must fail if migration drops failed, denied, or blocked history.
3340. Data-migration proof must fail if migration is not idempotent.
3341. Data-migration proof must fail if migration can run partially and leave valid-looking current state.
3342. Data-migration proof must fail if migration changes schema version without validation.
3343. Data-migration proof must fail if migration logs are not captured and hashed.
3344. Data-migration proof must fail if migration rollback is absent where migration is proof-critical.
3345. System-boundary proof must define boundaries between Agent_X core, external tools, OS, filesystem, network, user, model provider, and CI.
3346. System-boundary proof must fail if final proof attributes external-tool behavior to Agent_X without adapter evidence.
3347. System-boundary proof must fail if external failures are classified as internal PASS.
3348. System-boundary proof must fail if OS-level behavior is assumed but not included in compatibility scope.
3349. System-boundary proof must fail if CI behavior is assumed equivalent to local behavior without proof.
3350. System-boundary proof must fail if model-provider behavior is assumed deterministic without evidence.
3351. System-boundary proof must fail if user action is treated as system proof without structured record.
3352. System-boundary proof must fail if boundary-crossing data lacks validation and hash evidence.
3353. System-boundary proof must fail if out-of-boundary dependencies can affect final verdict.
3354. System-boundary proof must fail if system boundary diagram or metadata is absent from final artifacts.
