# agentx_initiator tests

This folder contains unit and component tests for `tools/agentx_initiator`.

Rules:
- No live network calls by default.
- Tests must not mutate repository source files.
- Cross-package integration tests belong in root `tests/integration/`.
